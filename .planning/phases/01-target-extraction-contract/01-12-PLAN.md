---
phase: 01-target-extraction-contract
plan: 12
type: execute
wave: 11
depends_on: [01-11]
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/phases/01-target-extraction-contract/01-SIGNOFF-REQUEST.md
autonomous: false
gap_closure: true
requirements: [TARGET-01, TARGET-02]
must_haves:
  truths:
    - "REQUIREMENTS.md no longer contradicts 01-VERIFICATION.md: TARGET-02's checkbox and its traceability row reflect the actual state — not complete until a human has signed off on the regenerated QA figures."
    - "The traceability table's TARGET-02 status names the specific outstanding condition (human visual sign-off) rather than an unqualified Complete."
    - "A single handoff document lists exactly which figures a human must open, what to look for in each, and what decision is being requested — the plan requests sign-off and does not grant it."
    - "The handoff surfaces the regenerated per-track valid fractions and the 8 > 10 > 14 > 21 ordering outcome from 01-11 so the reviewer judges against the current artifacts, not the 2026-07-20 generation."
    - "The handoff explicitly carries forward the three prior UAT findings (residual curvature, boundary sawtooth, track-10 crop-edge spike) so the reviewer can confirm each is resolved or still open under the current contract."
    - "If 01-11 ended on the non-recoverable branch, the handoff presents the override-versus-accept decision from 01-08-ORDERING-OUTCOME.md as an explicit choice rather than assuming either answer."
  artifacts:
    - ".planning/REQUIREMENTS.md"
    - ".planning/phases/01-target-extraction-contract/01-SIGNOFF-REQUEST.md"
  key_links:
    - "01-11-ORDERING-OUTCOME.md -> 01-SIGNOFF-REQUEST.md: the reviewer's decision must be framed against the regenerated numbers, so the handoff quotes them rather than restating stale ones."
    - "01-SIGNOFF-REQUEST.md -> REQUIREMENTS.md TARGET-02 row: the requirement flips to complete only after the human records sign-off, not as a side effect of this plan."
  prohibitions:
    - statement: "This plan must NOT self-certify visual QA. Sign-off is requested from a human; no task may mark TARGET-02 complete or record an approval on the reviewer's behalf."
      status: resolved
      verification: judgment
    - statement: "Extraction contract constants must NOT be adjusted based on whether the resulting width ordering or QA plots look correct; this plan changes no source and no constant."
      status: resolved
      verification: judgment
    - statement: "Extraction and persistence code must NOT write to, delete, or modify any file under data/raw/; this plan writes only to .planning/."
      status: resolved
      verification: test
    - statement: "Do NOT weaken an existing gate to make a check pass — if check_targets.py still exits non-zero, that is reported to the human, not resolved by editing the gate."
      status: resolved
      verification: judgment
---

<objective>
Close the two remaining non-code gaps. First, correct the project record: `REQUIREMENTS.md` currently marks TARGET-01 and TARGET-02 `[x]` complete and both traceability rows "Complete", which `01-VERIFICATION.md` directly contradicts — it found TARGET-02 blocked. Second, hand the visual sign-off to the human properly: verification truth 4 requires a person to confirm the QA plots are sane for all 4 tracks, and no plan may self-certify that. This plan produces an explicit, actionable sign-off request against the artifacts plan 01-11 regenerated.

Purpose: TARGET-02's own acceptance criterion is "QA overlay plots for all 4 tracks that a human reviewer confirms show no sawtooth/high-frequency jitter and no silently dropped gaps". That is the last gate between Phase 1 and Phase 2 consuming `processed_data/targets/` as ground truth.
Output: An honest requirements record and a single handoff document naming the exact figures, the exact things to look for, and the exact decision being requested.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-target-extraction-contract/01-VERIFICATION.md
@.planning/phases/01-target-extraction-contract/01-UAT.md
@.planning/phases/01-target-extraction-contract/01-08-ORDERING-OUTCOME.md
@.planning/phases/01-target-extraction-contract/01-SPEC.md
</context>

<artifacts_produced>
Artifacts this phase produces — new symbols and paths introduced by this plan (exclude these from source-drift verification; they do not exist yet):

- New planning document `.planning/phases/01-target-extraction-contract/01-SIGNOFF-REQUEST.md`.
- No new code symbols, constants, CLI flags, tests, or source files. This plan touches only `.planning/`.
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: Correct the REQUIREMENTS.md record so it stops contradicting the verification</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (lines 12-13: the TARGET-01 and TARGET-02 checkboxes and their full requirement text; lines 79-92: the Traceability table with `| TARGET-01 | Phase 1 | Complete |` and `| TARGET-02 | Phase 1 | Complete |`; lines 94-98: the Coverage block)
    - .planning/phases/01-target-extraction-contract/01-VERIFICATION.md (the Requirements Coverage table: TARGET-01 satisfied for documentation and reproducibility, TARGET-02 BLOCKED; and the closing note that the requirements table should be corrected)
    - .planning/phases/01-target-extraction-contract/01-11-ORDERING-OUTCOME.md (plan 01-11's regenerated result — the current factual basis for the corrected status)
    - .planning/phases/01-target-extraction-contract/01-SPEC.md (Requirement 2's acceptance clause (c): "QA overlay plots for all 4 tracks that a human reviewer confirms" — the reason TARGET-02 cannot be complete before sign-off regardless of the numeric outcome)
  </read_first>
  <action>
    Edit `.planning/REQUIREMENTS.md` with scoped replacements only — do not rewrite the file.

    Change TARGET-02's checkbox from `[x]` to `[ ]` and change its Traceability row's status from `Complete` to a specific, factual state naming the outstanding condition: awaiting human visual sign-off on the regenerated QA figures, referencing `01-SIGNOFF-REQUEST.md`. Do this regardless of whether plan 01-11 restored the ordering, because TARGET-02's own acceptance clause requires a human reviewer's confirmation and no automated result can substitute for it.

    Leave TARGET-01 as `[x]` / `Complete`: `01-VERIFICATION.md` truths 1 and 2 both verified, the contract is documented through Amendments A1-A5, and `extraction_params()` is provenance-locked and change-sensitive. Do not downgrade it.

    Add a single short line beneath the Traceability table recording the correction, its date, and the artifact that establishes the real state (`01-VERIFICATION.md`, plus `01-11-ORDERING-OUTCOME.md`), so the discrepancy that `01-REVIEW.md` flagged does not silently reappear. Keep the Coverage block's counts accurate — mapping coverage is unchanged (12/12 mapped); only completion status changes.

    Do not edit any other requirement, do not touch the v2 or Out of Scope sections, and do not touch any source file.
  </action>
  <verify>
    <automated>grep -q "^- \[ \] \*\*TARGET-02\*\*" .planning/REQUIREMENTS.md && grep -q "^- \[x\] \*\*TARGET-01\*\*" .planning/REQUIREMENTS.md && grep -E "^\| TARGET-02 \| Phase 1 \|" .planning/REQUIREMENTS.md | grep -qvi "complete" && grep -q "01-SIGNOFF-REQUEST" .planning/REQUIREMENTS.md && test -z "$(git status --porcelain src scripts tests data/raw)" && echo "REQUIREMENTS RECORD CORRECTED"</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/REQUIREMENTS.md` shows `- [ ] **TARGET-02**` and still shows `- [x] **TARGET-01**`.
    - The Traceability row for TARGET-02 no longer reads `Complete`; it names the outstanding human visual sign-off and references `01-SIGNOFF-REQUEST.md`.
    - The Traceability row for TARGET-01 still reads `Complete`.
    - A dated correction note beneath the Traceability table cites `01-VERIFICATION.md` and `01-11-ORDERING-OUTCOME.md` as the basis.
    - The Coverage block still reports 12 v1 requirements with 12/12 mapped to phases and 0 unmapped.
    - `git status --porcelain src scripts tests data/raw` is empty — this task touched no code and no raw data.
  </acceptance_criteria>
  <done>The requirements record matches the verification instead of contradicting it, and the exact remaining condition for TARGET-02 is named.</done>
</task>

<task type="auto">
  <name>Task 2: Produce the human visual sign-off handoff for all 12 regenerated QA figures</name>
  <files>.planning/phases/01-target-extraction-contract/01-SIGNOFF-REQUEST.md</files>
  <read_first>
    - .planning/phases/01-target-extraction-contract/01-11-ORDERING-OUTCOME.md (the regenerated per-track medians, valid counts and fractions, the three adjacent-pair ordering verdicts, the `check_targets.py` output and exit code, and the contract in effect including Amendment A5)
    - .planning/phases/01-target-extraction-contract/01-UAT.md (the three prior findings the reviewer must re-judge: Test 1 residual curvature on all four tracks, Test 2 boundary sawtooth and gap explicitness, Test 3 track-10 crop-edge V-spike; and Test 4's A1/A2 no-tuning judgment which previously passed)
    - .planning/phases/01-target-extraction-contract/01-VERIFICATION.md (Human Verification items 1-3 and truth 4's exact wording: continuous plausible boundary traces with explicitly shaded, not silently dropped, gaps for all four tracks)
    - .planning/phases/01-target-extraction-contract/01-SPEC.md (Acceptance Criteria list and the Prohibitions table's judgment-tier item — the reviewer is also the confirming authority that constants were fixed independently of outcome inspection)
    - .planning/phases/01-target-extraction-contract/01-CONTEXT.md (Amendments A3, A4, and the new A5 — what the reviewer is being asked to accept as the contract)
  </read_first>
  <action>
    Write `.planning/phases/01-target-extraction-contract/01-SIGNOFF-REQUEST.md`. This document REQUESTS sign-off; it must not record, assume, or imply that sign-off was given. No checkbox in it may be pre-ticked.

    Structure it as follows.

    A "Current state" section quoting, verbatim from `01-11-ORDERING-OUTCOME.md`: the four-row summary table (track, power, valid bins, median, mean), the three adjacent-pair ordering verdicts, each track's valid fraction against the 50 percent `MIN_VALID_FRACTION` floor, and the `check_targets.py` exit code. State plainly whether 8 greater than 10 greater than 14 greater than 21 holds and whether the checker passes.

    A "Contract in effect" section naming `DETREND_POLY_ORDER`, `BEAD_MASK_HEIGHT_FRACTION`, `MAX_TRACKING_GAP_COLUMNS`, and the Amendment A5 constant with its value, and stating that each was fixed a priori from residual, physical, or numerical evidence rather than from the ordering outcome — the judgment-tier prohibition the reviewer is being asked to confirm.

    A "Figures to inspect" section listing all 12 paths explicitly, grouped by figure type, each with its own concrete acceptance question:
    - `processed_data/targets/qa/track_{8,10,14,21}_residual_map.png` — is the remaining post-detrend structure acceptable process and substrate variation, rather than the coherent track-wide end-positive / centre-negative bow reported in UAT Test 1, and is there no manufactured height feature hugging the y-strip edge (the specific artifact plan 01-11 diagnosed on track 10)?
    - `processed_data/targets/qa/track_{8,10,14,21}_overlay.png` — do the upper and lower boundary traces follow the physical bead continuously across the 20-100 mm span on every track including track 10, without the sawtooth or EKG-like cross-track jumps reported in UAT Test 2, and are invalid regions shown as explicit grey shading rather than silently bridged or dropped?
    - `processed_data/targets/qa/track_{8,10,14,21}_width.png` — inside the orange-shaded 0.5 mm crop-edge bands at 20 mm and 100 mm, is the edge behaviour physically plausible, and specifically is track 10's terminal V-spike from UAT Test 3 gone?

    A "Decisions requested" section with unticked checkboxes, one per decision, each stating what a yes and a no mean:
    - Residual maps acceptable for all four tracks (closes UAT Test 1 / Human Verification item 2).
    - Boundary overlays acceptable for all four tracks including track 10 (closes UAT Test 2 and verification truth 4).
    - Crop-edge behaviour acceptable for all four tracks (closes UAT Test 3).
    - Constants confirmed fixed independently of outcome inspection (closes the SPEC judgment-tier prohibition and UAT Test 4).
    - Only if the ordering still FLAGs or a track remains below the coverage floor: the explicit choice from `01-08-ORDERING-OUTCOME.md` between (a) recording a documented human override of the roadmap ordering criterion with rationale and proceeding to Phase 2 with the caveat, and (b) commissioning a further diagnosed-defect investigation. Present both; choose neither.

    A "How to record the outcome" section stating that approvals or issues are recorded through `/gsd-verify-work` against this phase, and that TARGET-02's checkbox and traceability row in `REQUIREMENTS.md` flip to complete only after that record exists.

    A closing note stating that if any item is rejected, the correct response is a new gap-closure plan, and that under no circumstances may a locked extraction constant, the `MIN_VALID_FRACTION` floor, or the boundary-clipped-run exclusion be adjusted to make a figure look better.

    Do not edit source files. Do not edit `01-UAT.md` or `01-VERIFICATION.md` — this task creates a request, not a result.
  </action>
  <verify>
    <automated>test -f .planning/phases/01-target-extraction-contract/01-SIGNOFF-REQUEST.md && test $(grep -c "processed_data/targets/qa/track_" .planning/phases/01-target-extraction-contract/01-SIGNOFF-REQUEST.md) -ge 12 && grep -q "\[ \]" .planning/phases/01-target-extraction-contract/01-SIGNOFF-REQUEST.md && test $(grep -c "\[x\]" .planning/phases/01-target-extraction-contract/01-SIGNOFF-REQUEST.md) -eq 0 && test -z "$(git status --porcelain src scripts tests data/raw)" && echo "SIGNOFF REQUEST READY (UNSIGNED)"</automated>
    <human-check>Open the 12 figures listed in `01-SIGNOFF-REQUEST.md` and record approvals or issues via `/gsd-verify-work 1`. This plan cannot close itself: verification truth 4 requires a human confirmation that no automated check can substitute for.</human-check>
  </verify>
  <acceptance_criteria>
    - `01-SIGNOFF-REQUEST.md` exists and names all 12 QA figure paths explicitly (four tracks by three figure types), each with its own concrete acceptance question.
    - The document quotes the regenerated summary table, the three adjacent-pair ordering verdicts, all four valid fractions against the 50 percent floor, and the `check_targets.py` exit code from `01-11-ORDERING-OUTCOME.md`.
    - The document names `DETREND_POLY_ORDER`, `BEAD_MASK_HEIGHT_FRACTION`, `MAX_TRACKING_GAP_COLUMNS`, and the Amendment A5 constant with its value, or records that no A5 constant was introduced.
    - Every decision checkbox is unticked: `grep -c "\[x\]" 01-SIGNOFF-REQUEST.md` returns 0 and `grep -c "\[ \]" 01-SIGNOFF-REQUEST.md` returns at least 4.
    - The three prior UAT findings (residual curvature, boundary sawtooth, track-10 crop-edge spike) each appear as an explicit re-judgement item.
    - The override-versus-further-investigation choice is presented as two options with neither selected, and appears only when the ordering FLAGs or a track is below the floor.
    - `git status --porcelain src scripts tests data/raw` is empty; `01-UAT.md` and `01-VERIFICATION.md` are unmodified by this task.
  </acceptance_criteria>
  <done>A human has everything needed to judge the regenerated QA figures in one place, framed against the current artifacts, with the decision explicitly requested and not pre-answered.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| automated plan output -> human sign-off record | A plan that writes an approval on the reviewer's behalf would forge the one gate TARGET-02 reserves for a human. |
| project record -> downstream phase trust | `REQUIREMENTS.md` is what Phase 2 planning reads to decide whether Phase 1's output is trustworthy; a stale `Complete` is a false safety signal. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-30 | Spoofing | human visual sign-off | high | mitigate | Task 2 produces a request with every checkbox unticked, verified by `grep -c "\[x\]"` returning 0; the plan is marked `autonomous: false` and carries a `<human-check>` so it cannot close without a person. Approvals are recorded through `/gsd-verify-work`, not by this plan. |
| T-01-31 | Repudiation | `REQUIREMENTS.md` completion record | high | mitigate | Task 1 corrects the TARGET-02 checkbox and traceability row to name the outstanding condition, and adds a dated correction note citing `01-VERIFICATION.md` and `01-11-ORDERING-OUTCOME.md`, so the contradiction `01-REVIEW.md` flagged cannot silently reappear. |
| T-01-32 | Tampering | locked extraction constants | medium | mitigate | This plan modifies only `.planning/`; the handoff explicitly forbids adjusting any constant, the `MIN_VALID_FRACTION` floor, or the boundary-clipped-run exclusion in response to a rejected figure, and `git status --porcelain src scripts tests` is asserted empty in both tasks. |
| T-01-33 | Tampering (accidental) | `data/raw/` | high | mitigate | No task reads or writes under `data/raw/`; `git status --porcelain data/raw` is asserted empty. |
| T-01-SC | Tampering | npm/pip/cargo installs | high | mitigate | No packages are installed by this plan; `requirements.txt` is unchanged. No `[ASSUMED]`/`[SUS]` package is introduced, so no legitimacy checkpoint is required. |
</threat_model>

<verification>
- `.planning/REQUIREMENTS.md` shows TARGET-02 as `[ ]` with a traceability status naming the outstanding human visual sign-off, and TARGET-01 still `[x]` / Complete.
- `.planning/phases/01-target-extraction-contract/01-SIGNOFF-REQUEST.md` exists, names all 12 QA figure paths, quotes the regenerated numbers, and has zero ticked checkboxes.
- `git status --porcelain src scripts tests data/raw` is empty for the whole plan.
- All three test suites remain green and `extraction_params()` is unchanged — this plan touches no code.
- The phase's human verification is left open for `/gsd-verify-work` rather than being self-certified.
</verification>

<success_criteria>
- The requirements record matches the verification instead of contradicting it.
- A single, actionable handoff gives a human exactly the figures, questions, and decisions needed to close verification truth 4 and UAT tests 1-4.
- Sign-off is requested, never granted, by this plan.
</success_criteria>

<output>
Create `.planning/phases/01-target-extraction-contract/01-12-SUMMARY.md` when done.
</output>