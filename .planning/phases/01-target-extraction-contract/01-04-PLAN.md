---
phase: 01-target-extraction-contract
plan: 04
type: execute
wave: 4
depends_on: ["01-01", "01-03"]
files_modified:
  - src/nsf_fmrg_data.py
  - src/targets.py
  - tests/test_targets.py
  - tests/test_nsf_fmrg_data.py
  - .planning/phases/01-target-extraction-contract/01-CONTEXT.md
autonomous: true
gap_closure: true
gap_ids: [G-01-1]
requirements: [TARGET-01, TARGET-02]
tags: [profilometry, detrending, polynomial-surface, numpy]
must_haves:
  truths:
    - "robust_plane_detrend() accepts a configurable order parameter; called with order=1 (its new default) it still fully removes a synthetic pure-tilt (affine) surface, matching its original documented behavior exactly."
    - "robust_plane_detrend() called with order=4 removes a synthetic quartic-in-x bow that order=1 is structurally incapable of removing, proven by a direct residual-magnitude comparison on noise-free synthetic data."
    - "src/targets.py calls robust_plane_detrend with a single new DETREND_POLY_ORDER=4 constant applied identically to all 4 tracks (no per-track branching), and extraction_params() reports the new constant for provenance."
    - "01-CONTEXT.md records Amendment A3, documenting the escalation from D-13/D-16's linear-plane baseline to the order-4 surface fit, grounded in the debug session's measured per-track R^2 findings, decided before any new QA figures were inspected."
    - "Regenerating the real 4-track pipeline under the order-4 fit produces residual-map QA figures where the previously reported coherent track-wide red-blue-red bow pattern is dramatically reduced, confirmed by both a quantitative R^2 re-check and human visual review."
  artifacts:
    - path: "src/nsf_fmrg_data.py"
      provides: "robust_plane_detrend with a configurable bivariate-polynomial order (order=1 default, backward compatible)"
      contains: "def robust_plane_detrend"
    - path: "src/targets.py"
      provides: "DETREND_POLY_ORDER constant wired into extract_track_targets and extraction_params"
      contains: "DETREND_POLY_ORDER"
    - path: "tests/test_nsf_fmrg_data.py"
      provides: "Synthetic order=1 legacy-equivalence and order=4 quartic-recovery regressions for robust_plane_detrend"
      contains: "def test_"
    - path: ".planning/phases/01-target-extraction-contract/01-CONTEXT.md"
      provides: "Amendment A3 documenting the detrend order escalation with grounding evidence"
      contains: "Amendment A3"
  key_links:
    - from: "src/targets.py::extract_track_targets"
      to: "src/nsf_fmrg_data.py::robust_plane_detrend"
      via: "order=DETREND_POLY_ORDER keyword argument applied identically for all 4 tracks"
      pattern: "order=DETREND_POLY_ORDER"
    - from: "tests/test_nsf_fmrg_data.py"
      to: "src/nsf_fmrg_data.py::robust_plane_detrend"
      via: "synthetic quartic-bow recovery regression proves the order upgrade's necessity and correctness"
      pattern: "order=4"
    - from: "01-CONTEXT.md Amendment A3"
      to: ".planning/debug/residual-curvature-after-detrend.md"
      via: "grounding citation of the measured per-track R^2 findings that set the shared order a priori"
      pattern: "Amendment A3"
  prohibitions:
    - statement: "Extraction contract constants, including the new detrend polynomial order, must NOT be adjusted after inspecting the resulting residual maps or width ordering — the order is fixed from the debug session's pre-existing measured R^2 evidence, before this plan's Task 2 regenerates any new QA figure."
      status: resolved
      verification: judgment
---

<objective>
Close UAT gap G-01-1: `robust_plane_detrend()` (`src/nsf_fmrg_data.py:205-227`) fits only a first-order affine surface and structurally cannot remove the coherent track-wide bow the reviewer confirmed on tracks 8, 10, 14 (and the alternating long-wavelength waviness on track 21). Per Phase 01's own D-14 escalation trigger, upgrade the detrend to a robust bivariate-polynomial surface fit of configurable order, apply it as one identical shared parameterization (order 4, chosen a priori from the debug session's measured R^2 evidence) across all 4 tracks, and document this as **Amendment A3** to the locked D-01-D-16 contract — following the same amendment-and-ratification documentation style already used for Amendments A1/A2, not a silent rewrite.

Purpose: D-14 named the mandatory residual QA check as the trigger to escalate to a detrend fix if bow was visible; it now has been (UAT Test 1). TARGET-02's visual-QA acceptance and D-14 cannot be satisfied while the detrend model is mathematically incapable of representing the curvature present in the real substrate/instrument geometry.

Output: `src/nsf_fmrg_data.py::robust_plane_detrend` with a configurable `order` parameter (default `order=1`, preserving legacy behavior for any other caller); `src/targets.py`'s `DETREND_POLY_ORDER = 4` constant wired into `extract_track_targets` and `extraction_params()`; a new `tests/test_nsf_fmrg_data.py` regression suite; `01-CONTEXT.md` Amendment A3; regenerated real 4-track `.npz`/QA artifacts under the new fit with a quantitative before/after R^2 comparison.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-target-extraction-contract/01-CONTEXT.md
@.planning/phases/01-target-extraction-contract/01-SPEC.md
@.planning/phases/01-target-extraction-contract/01-01-SUMMARY.md
@.planning/debug/residual-curvature-after-detrend.md
@src/nsf_fmrg_data.py
@src/targets.py

<interfaces>
Existing interfaces consumed/changed by this plan:

- `robust_plane_detrend(Z_mm, x_mm, y_mm, stride_x=40, stride_y=2)` (`src/nsf_fmrg_data.py:205-227`) currently fits `z = coef[0]*x + coef[1]*y + coef[2]` via a 3-pass, 5th/95th-percentile iterative outlier-trimming lstsq over a strided subsample (`stride_x=40, stride_y=2`), and reconstructs `plane = coef[0]*x_mm[None,:] + coef[1]*y_mm[:,None] + coef[2]`; returns `(Z_mm.copy(), None)` when fewer than 100 valid strided samples exist. This is the ONLY call site consumed downstream (`src/targets.py:220`); no dedicated unit tests currently exist for it (only implicit exercise via `src/targets.py`'s synthetic tests, which use curvature-free synthetic data).
- `extract_track_targets(height_dir, track_id)` (`src/targets.py:212-233`) calls `robust_plane_detrend(data['Z_mm'], data['x_actual_mm'], data['y_mm'])` with defaults, identically for all 4 tracks, then raises `ValueError` if `coef is None`.
- `extraction_params()` (`src/targets.py:28-42`) returns the exact 12-key locked constants dict, asserted verbatim by `tests/test_targets.py::test_extraction_params_provenance` and by `scripts/check_targets.py::main()`'s provenance comparison against the persisted `extraction_params.json`.
- Notebooks (`notebooks/01_*.ipynb`, `notebooks/02_*.ipynb`) call `robust_plane_detrend(height['Z_mm'], height['x_actual_mm'], height['y_mm'])` positionally with 3 args only (never `order`, `stride_x`, or `stride_y`) — non-canonical, excluded from testing, but any new keyword-only parameter appended after existing ones preserves their call sites unchanged.
</interfaces>
</context>

## Artifacts this plan produces

- `src/nsf_fmrg_data.py`: `robust_plane_detrend` gains a new keyword parameter `order=1`; internally builds a bivariate-polynomial design matrix (all `(i, j)` exponent pairs with `i + j <= order`) instead of the fixed 3-column `[x, y, 1]` matrix, using `x`/`y` centered at their midpoints for numerical conditioning; the same 3-pass percentile-trim loop and `stride_x=40, stride_y=2` defaults are unchanged.
- `src/targets.py`: new constant `DETREND_POLY_ORDER = 4`; `extraction_params()` returns 13 keys (adds `DETREND_POLY_ORDER`); `extract_track_targets` passes `order=DETREND_POLY_ORDER` explicitly.
- `tests/test_nsf_fmrg_data.py` (new file): `require()` helper, 4 `test_*` functions, `__main__` runner — mirrors `tests/test_targets.py`'s plain-Python convention.
- `tests/test_targets.py`: `test_extraction_params_provenance`'s expected dict gains `"DETREND_POLY_ORDER": 4`.
- `.planning/phases/01-target-extraction-contract/01-CONTEXT.md`: new `### Amendment A3` subsection under `<decisions>`, immediately after D-16.
- Regenerated `processed_data/targets/` artifacts (all 4 tracks) under the new detrend, with a documented before/after R^2 comparison in this plan's SUMMARY.

## Source Coverage Audit

| Source | ID | Gap-plan coverage | Task | Status |
|---|---|---|---|---|
| GOAL | UAT Test 1 / D-14 | Escalate the detrend model order now that the mandatory residual QA check surfaced the anticipated bow, per D-14's own contingency | Task 1, Task 2 | COVERED |
| REQ | TARGET-01 | Amendment A3 documents the superseding decision in the locked contract document itself, grounded in evidence, not silently rewritten | Task 1 | COVERED |
| REQ | TARGET-02 | Real 4-track regeneration + quantitative R^2 re-check + human visual review re-establishes D-14's visual-QA acceptance | Task 2 | COVERED |
| CONTEXT | D-13, D-16 | Explicitly superseded by Amendment A3 (order upgrade); stride_x/stride_y defaults left unchanged since the debug session's root cause is model order, not sampling density | Task 1 | COVERED |
| CONTEXT | D-15 | Detrending remains a single whole-track fit before edge detection, unchanged | Task 1 | COVERED (no change needed) |
| RESEARCH / debug | Quadratic/quartic R^2 findings (residual-curvature-after-detrend.md) | Order 4 chosen a priori from the measured per-track R^2 table, applied uniformly (track 21's requirement, the most demanding case, sets the shared order) | Task 1 | COVERED |
| SPEC | Boundaries — "Upgrading robust_plane_detrend() to a higher-order surface fit — deferred unless the D-14 residual-curvature QA check surfaces an actual problem" | The deferred trigger condition has now fired; this plan is exactly the deferred follow-up SPEC anticipated | Task 1, Task 2 | COVERED |
| SPEC | Prohibitions P1 (constants not tuned to outcome) | Order fixed from pre-existing debug-session evidence before Task 2 regenerates any new figure; SUMMARY records this explicitly | Task 1, Task 2 | COVERED |

No unresolved assumptions. G-01-2 and G-01-3 (boundary tracking, crop-edge smoothing) are explicitly out of scope for this plan — see `01-05-PLAN.md`.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Upgrade robust_plane_detrend to a configurable-order robust polynomial surface fit (Amendment A3)</name>
  <files>src/nsf_fmrg_data.py, src/targets.py, tests/test_targets.py, tests/test_nsf_fmrg_data.py, .planning/phases/01-target-extraction-contract/01-CONTEXT.md</files>
  <read_first>
    - src/nsf_fmrg_data.py:205-227 (current robust_plane_detrend — design matrix, iterative trim loop, degenerate fallback)
    - .planning/debug/residual-curvature-after-detrend.md (root-cause confirmation and the measured per-track linear/quadratic/quartic R^2 table — the grounding evidence for Amendment A3)
    - .planning/phases/01-target-extraction-contract/01-CONTEXT.md (D-13 through D-16, and the surrounding Claude's Discretion block where the new Amendment A3 subsection is appended)
    - .planning/phases/01-target-extraction-contract/01-SPEC.md (Boundaries section — "Upgrading robust_plane_detrend() ... deferred unless the D-14 residual-curvature QA check surfaces an actual problem")
    - src/targets.py:1-42, 212-233 (constants block, extraction_params, extract_track_targets call site)
    - tests/test_targets.py:183-199 (test_extraction_params_provenance's exact expected dict)
  </read_first>
  <behavior>
    - Test 1 (legacy equivalence, order=1 default): a noise-free synthetic Z_mm built from a known pure affine surface `z = a*x + b*y + c` (no curvature) over a grid comparable in size to a real cropped track subsample → `robust_plane_detrend(Z_mm, x_mm, y_mm)` called with no `order` argument (using the new default) returns a detrended surface with residual standard deviation within float tolerance of zero everywhere finite — proving the default still fully removes pure tilt exactly as before.
    - Test 2 (quartic recovery, order=4): a noise-free synthetic Z_mm built from the SAME affine tilt PLUS a known quartic-in-x bow term (no y-curvature needed to prove the point) → `robust_plane_detrend(Z_mm, x_mm, y_mm, order=1)` leaves a large residual standard deviation (the bow survives, reproducing the bug), while `robust_plane_detrend(Z_mm, x_mm, y_mm, order=4)` on the identical input reduces the residual standard deviation by at least two orders of magnitude — a single test proving both that the bug reproduces under order=1 and that order=4 fixes it.
    - Test 3 (degenerate fallback preserved): fewer than 100 finite strided samples (mirror the existing degenerate-input construction) → returns `(Z_mm.copy(), None)` regardless of the `order` argument passed (test both order=1 and order=4).
    - Test 4 (basis size contract): the returned `coef` array has length 3 when `order=1` (legacy 3-term basis: x, y, intercept) and length 15 when `order=4` (the full total-degree-<=4 bivariate polynomial basis) — pins the exact basis size so silent term-generation drift is caught.
  </behavior>
  <action>
    Write `tests/test_nsf_fmrg_data.py` FIRST, mirroring `tests/test_targets.py`'s exact plain-Python convention: a `require(condition, message)` helper raising `AssertionError`, `sys.path` bootstrap identical to `tests/test_targets.py`'s (repo-root parents[1], append `src/` before `import nsf_fmrg_data`), one `test_*` function per behavior item above, and a `__main__` block listing all 4 tests with one `print(f"PASS: {test.__name__}")` per test. Confirm Test 2 fails against the current implementation (RED — order keyword does not exist yet) before implementing.

    Modify `robust_plane_detrend` in `src/nsf_fmrg_data.py` to accept a new keyword parameter `order=1` appended after `stride_y=2` (preserves every existing positional call site, including both notebooks). Replace the fixed 3-column design matrix with a general bivariate-polynomial basis: build the list of all integer exponent pairs `(i, j)` with `i >= 0`, `j >= 0`, and `i + j <= order` (generate via a simple nested loop over total degree `d` from 0 to `order`, and for each `d`, `i` from 0 to `d` with `j = d - i` — there are `(order+1)(order+2)/2` such pairs; for `order=1` this yields exactly the 3 pairs `(0,0), (1,0), (0,1)` in some order, matching the legacy 3-term model; for `order=4` this yields 15 pairs). Compute `x_center = 0.5*(x_mm[0] + x_mm[-1])` and `y_center = 0.5*(y_mm[0] + y_mm[-1])` from the FULL (not strided) coordinate arrays, once, and use `(x - x_center)` / `(y - y_center)` consistently everywhere a raw `x`/`y` value is exponentiated — both when building the strided design matrix for the lstsq fit and when reconstructing the full-resolution `plane` surface for subtraction. Centering avoids numerical conditioning issues from `x**4` reaching magnitudes near 1e8 over the real 20-100mm track range; because the exponent-pair list always includes `(0,0)` (a constant/intercept term), centering does not change the fitted surface's values for any order — only the coefficient values shift to compensate, so this preserves the exact fitted output. Build each design-matrix column as `(Xs - x_center)**i * (Ys - y_center)**j` for the strided meshgrid (fit), and reconstruct the full-resolution `plane` by summing, for every exponent pair and its corresponding fitted coefficient, the broadcast term `(x_mm[None, :] - x_center)**i * (y_mm[:, None] - y_center)**j`. Leave the 3-pass iterative percentile-trim loop (5th/95th percentile of residuals, `valid.sum() < 100` degenerate-fallback threshold) and the `stride_x=40, stride_y=2` defaults completely unchanged — the debug session's root cause is model order, not sampling density or trimming logic (D-16 is not implicated).

    In `src/targets.py`, add `DETREND_POLY_ORDER = 4` to the constants block immediately after `MIN_COLUMNS_PER_BIN`, with a short one-line comment noting it implements Amendment A3 (order chosen a priori from the debug session's measured per-track R^2 evidence, grounded before any new QA figure is generated by Task 2). Add `'DETREND_POLY_ORDER': DETREND_POLY_ORDER` to the dict returned by `extraction_params()`. Change the `extract_track_targets` call site at line 220 to `robust_plane_detrend(data['Z_mm'], data['x_actual_mm'], data['y_mm'], order=DETREND_POLY_ORDER)` — applied identically for all 4 tracks, no per-track branching. Update `tests/test_targets.py::test_extraction_params_provenance`'s expected dict to add `"DETREND_POLY_ORDER": 4`.

    Append a new subsection to `.planning/phases/01-target-extraction-contract/01-CONTEXT.md`'s `<decisions>` block, immediately after D-16 and before `### Claude's Discretion`, titled `### Amendment A3 (post-UAT escalation — plan 01-04)`. Content: state that this amendment supersedes D-13's and D-16's "keep robust_plane_detrend() as-is" instruction now that D-14's mandatory residual QA check surfaced exactly the anticipated bow (UAT Test 1, `.planning/debug/residual-curvature-after-detrend.md`); `robust_plane_detrend()` gains a configurable bivariate-polynomial `order` parameter (default `order=1`, preserving the original 3-term affine fit for any other caller, including the notebooks); `src/targets.py` locks a new `DETREND_POLY_ORDER = 4` constant, applied identically to all 4 tracks; the same 3-pass percentile-trim scheme and the existing `stride_x=40, stride_y=2` sampling (D-16, not implicated by the root-cause finding) are unchanged; cite the measured evidence: quadratic explains 97.7%/96.3% of tracks 8/14's post-linear-detrend residual variance but only 46.9%/29.5% for tracks 10/21, and quartic raises track 10 to 64.0% and track 21 to 44.9% — order 4 was set from track 21 (the most demanding, alternating-lobe case) a priori, not tuned after inspecting the new residual maps Task 2 produces; note that track 21's remaining unexplained residual after a quartic term is expected to reflect genuine local width/process variation rather than removable curvature, and is deliberately not chased further with per-track special-casing, which would violate the one-shared-parameterization constraint (TARGET-02).

    Commit discipline (task-level TDD): first commit the failing test (`test(01-04): add failing order=4 quartic-recovery regression for robust_plane_detrend`), then the implementation and CONTEXT.md amendment (`fix(01-04): upgrade robust_plane_detrend to order-4 surface fit, Amendment A3`).
  </action>
  <verify>
    <automated>.venv/bin/python tests/test_nsf_fmrg_data.py && .venv/bin/python tests/test_targets.py</automated>
  </verify>
  <!-- The constant-assignment literal below is a POSITIVE existence grep (>= 1), and it must
       legitimately appear verbatim in src/targets.py — allowlisted per #429: -->
  <!-- planner-discipline-allow: DETREND_POLY_ORDER = 4 -->
  <acceptance_criteria>
    - `.venv/bin/python tests/test_nsf_fmrg_data.py` exits 0 and prints 4 PASS lines
    - `.venv/bin/python tests/test_targets.py` exits 0 and prints 15 PASS lines (unchanged count; only the provenance dict's expected values changed)
    - `grep -c 'DETREND_POLY_ORDER = 4' src/targets.py` returns >= 1
    - `robust_plane_detrend`'s signature in `src/nsf_fmrg_data.py` accepts `order` as a keyword parameter with default `1`: `grep -c 'def robust_plane_detrend' src/nsf_fmrg_data.py` returns 1 and the signature line contains `order=1`
    - `.planning/phases/01-target-extraction-contract/01-CONTEXT.md` contains the new subsection: `grep -c 'Amendment A3' .planning/phases/01-target-extraction-contract/01-CONTEXT.md` returns >= 1
    - No per-track branching introduced: `.venv/bin/python tests/test_targets.py` includes `test_single_parameterization_has_no_track_conditionals` and it still passes
  </acceptance_criteria>
  <done>robust_plane_detrend supports a configurable polynomial order with a backward-compatible default, src/targets.py locks and applies DETREND_POLY_ORDER=4 identically across all 4 tracks, Amendment A3 is documented in 01-CONTEXT.md with grounding evidence, and all synthetic regressions (new and existing) pass.</done>
</task>

<task type="auto">
  <name>Task 2: Regenerate the real 4-track pipeline under the new detrend and re-verify the residual-bow QA criterion</name>
  <files>processed_data/targets/ (regenerated runtime artifacts, not committed)</files>
  <read_first>
    - .planning/debug/residual-curvature-after-detrend.md (the exact linear/quadratic/quartic R^2 measurement methodology to reproduce against the NEW residual)
    - .planning/phases/01-target-extraction-contract/01-UAT.md Test 1 (the original failing visual criteria to re-check)
    - scripts/run_target_extraction.py (run_pipeline, run_track — unmodified by this plan; confirms the regeneration path)
    - scripts/check_targets.py (unmodified; confirms artifacts remain contract-valid after the detrend change)
  </read_first>
  <action>
    Rerun the full real-data pipeline with the Task 1 code in place: `.venv/bin/python scripts/run_target_extraction.py --project_dir .` (expect ~5-10 minutes; use an extended Bash timeout). This regenerates all 4 tracks' persisted `.npz` artifacts and all 12 QA figures — including the 4 `track_{id}_residual_map.png` files — under the new order-4 detrend. Then run `.venv/bin/python scripts/check_targets.py --project_dir .` to confirm the persisted artifacts remain contract-valid (dtype/shape/grid/mask/crossed-boundary checks); the ordering PASS/FLAG lines may shift from the prior run since `Zd_mm` has changed, but per prohibition P1 do not modify any constant in response regardless of the outcome — record whatever the new ordering result is in the SUMMARY as an honest investigation update.

    Reproduce the debug session's exact quantitative methodology against the NEW residual: for each of the 4 tracks, load the real height map via `load_wyko_asc`, run the new `robust_plane_detrend(..., order=4)`, compute the per-x-column median of the resulting `Zd_mm` (matching `.planning/debug/residual-curvature-after-detrend.md`'s method exactly), then fit linear/quadratic/quartic polynomials to that residual curve and report R^2 for each. This is a one-off diagnostic (matching the debug session's own ad hoc methodology) — it does not need to become a persisted script or repo artifact; run it inline and capture the printed numbers for the SUMMARY. Report the new R^2 table side-by-side against the original pre-fix table (tracks 8/10/14/21: linear 0.036/0.086/0.001/0.004, quadratic 0.977/0.469/0.963/0.295, quartic n/a/0.640/n/a/0.449) to make the before/after improvement explicit and auditable.

    Human-check the regenerated residual-map QA figures for all 4 tracks against the exact language of UAT Test 1's original failure report (coherent track-wide red-blue-red bow on 8/10/14; alternating lobes on 21) and confirm whether that specific pattern is now dramatically reduced or eliminated, and whether any remaining structure reads as localized/random substrate texture rather than smooth systematic curvature.
  </action>
  <verify>
    <automated>.venv/bin/python scripts/check_targets.py --project_dir .</automated>
    <human-check>Open the regenerated `processed_data/targets/qa/track_{8,10,14,21}_residual_map.png` figures and confirm the previously reported coherent track-wide bow/curvature pattern (UAT Test 1) is dramatically reduced or eliminated under the new order-4 detrend, with any remaining structure reading as localized/random texture rather than smooth systematic curvature. Also review whether the width-ordering PASS/FLAG outcome changed from the prior run, and record it honestly without adjusting any constant.</human-check>
  </verify>
  <acceptance_criteria>
    - `.venv/bin/python scripts/check_targets.py --project_dir .` exits 0 and prints ALL CHECKS PASSED
    - All 4 `track_{id}_residual_map.png` files have a newer mtime than the pre-Task-2 run
    - The plan SUMMARY contains the new per-track linear/quadratic/quartic R^2 table alongside the original pre-fix table, and an honest re-statement of the width-ordering PASS/FLAG outcome
    - `git diff --stat -- src/targets.py src/nsf_fmrg_data.py` is empty during this task (no constant or code touched in response to the regenerated QA output — Task 1's commits are the only code changes)
  </acceptance_criteria>
  <done>The real 4-track pipeline has been regenerated under the order-4 detrend; the residual-map QA figures and a quantitative R^2 re-check together demonstrate the previously reported bow is resolved (or the remaining gap is honestly documented), with no constants touched after the fact.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Real `.ASC` height-map data → polynomial surface fit | Same irreplaceable raw-data boundary as the rest of Phase 1; this plan does not add new raw-data I/O paths. |
| Higher-order design matrix → numerical solver | A 15-term quartic basis over a 20-100mm x-range is more conditioning-sensitive than the prior 3-term plane. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-01-15 | Tampering (data integrity) | `robust_plane_detrend`'s polynomial basis construction | medium | mitigate | Center `x`/`y` at their midpoints before exponentiating to avoid ill-conditioned `x**4` magnitudes (~1e8); the existing `valid.sum() < 100` degenerate-fallback threshold is retained unchanged as a backstop. |
| T-01-16 | Repudiation | Amendment A3 traceability | low | mitigate | The escalation rationale and measured R^2 grounding are recorded in `01-CONTEXT.md` and this plan's SUMMARY, mirroring the existing A1/A2 amendment trail, so the decision is auditable rather than a silent rewrite. |
| T-01-SC | Tampering | package installs | high | mitigate | No new packages introduced; the upgrade uses only `numpy` (already an audited dependency). |
</threat_model>

<verification>
- `.venv/bin/python tests/test_nsf_fmrg_data.py` exits 0 (4 new synthetic regressions)
- `.venv/bin/python tests/test_targets.py` exits 0 (15 checks, provenance dict updated)
- `.venv/bin/python scripts/check_targets.py --project_dir .` exits 0 against the regenerated real-data artifacts
- Quantitative R^2 re-check (inline diagnostic, reported in SUMMARY) shows dramatic reduction versus the pre-fix table in `.planning/debug/residual-curvature-after-detrend.md`
- Human visual re-check of the 4 regenerated residual-map QA figures against UAT Test 1's original failure language
</verification>

<success_criteria>
- `robust_plane_detrend` fits a configurable-order bivariate polynomial surface with a backward-compatible default, verified by synthetic legacy-equivalence and quartic-recovery regressions.
- `src/targets.py` locks and applies `DETREND_POLY_ORDER=4` identically across all 4 tracks, with provenance in `extraction_params()`.
- Amendment A3 is documented in `01-CONTEXT.md` with the exact measured grounding evidence, following the same amendment style as A1/A2 — not a silent rewrite.
- The real 4-track pipeline is regenerated and the D-14 residual-bow QA criterion is quantitatively and visually re-checked, with the outcome recorded honestly regardless of result.
</success_criteria>

<output>
Create `.planning/phases/01-target-extraction-contract/01-04-SUMMARY.md` when done. Record: the new/old R^2 comparison table for all 4 tracks, the Amendment A3 text as committed to 01-CONTEXT.md, the post-fix width-ordering PASS/FLAG outcome, and the human visual re-check result against UAT Test 1's original failure language.
</output>
