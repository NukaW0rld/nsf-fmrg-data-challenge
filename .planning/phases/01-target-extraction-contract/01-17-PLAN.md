---
phase: 01-target-extraction-contract
plan: 17
type: execute
wave: 16
depends_on: ["01-16"]
files_modified:
  - .planning/phases/01-target-extraction-contract/01-SIGNOFF-REQUEST.md
autonomous: true
gap_closure: true
requirements: [TARGET-01, TARGET-02]

must_haves:
  truths:
    - "A human opening 01-SIGNOFF-REQUEST.md reads the artifact generation that is actually on disk: every non-empty line the live `scripts/check_targets.py` prints — the results header, all four per-track rows, all three adjacent-pair ordering verdicts, the FLAG-outcomes sentence, and the final pass line — appears verbatim as its own line in the document."
    - "The only 32-hex-character run_id token anywhere in the document is byte-identical to `run_id` in `processed_data/targets/manifest.json`, and the only 64-hex-character digest token is byte-identical to that file's `extraction_params_sha256`. No superseded generation's identifier survives anywhere in the document."
    - "The document states, in words, that `extraction_params_sha256` is UNCHANGED from the prior generation because Amendment A8 introduced no new named constant — so the digest alone cannot distinguish this generation and `run_id` is the field that does. A reader cannot mistake a matching digest for a current document."
    - "The coverage table states each track's valid-bin count and its valid fraction against the 400-bin target grid, both derived from the live checker output in this execution, and each track's count and fraction are co-located on that track's own row (a transposition of two tracks fails)."
    - "The document states the current 10-vs-14 absolute median gap as the value derived from the live checker output, says plainly that the gap WIDENED under Amendment A8 rather than narrowing or closing, and cites `01-16-ORDERING-OUTCOME.md` as the source of the before-to-after comparison."
    - "The '## Contract in effect' section names all seven locked amendment constants (DETREND_POLY_ORDER, BEAD_MASK_HEIGHT_FRACTION, MAX_TRACKING_GAP_COLUMNS, DETREND_MAX_Y_DEGREE, DETREND_MAX_XY_DEGREE, MAX_RUN_MERGE_GAP_PIXELS, MIN_TRACKED_LENGTH_RATIO) AND Amendment A8's two behavioral rules — clip-exclusion applied to raw runs before `merge_adjacent_runs`, and the history-based far-AND-small gate threading `previous_length_mm` — each marked as introducing no new named constant."
    - "The document carries Amendment A8's coverage and fragmentation outcome forward exactly as `01-16-ORDERING-OUTCOME.md` measured it — coverage up on every track, contiguous-run fragmentation improved on three of four tracks and marginally worse on one, jump statistics mixed — and nowhere claims fragmentation is fixed or that Mechanism C's residual tail jitter was addressed."
    - "The already-recorded human acceptance of the 10-vs-14 width-ordering FLAG (option (a), recorded 2026-07-22 via /gsd-verify-work 1 Test 7) survives verbatim with its original date and rationale, and a NEW unticked item asks the reviewer to reaffirm — not re-decide — that acceptance at the widened magnitude. The FLAG is neither reopened nor silently carried forward."
    - "All 12 QA figure paths under processed_data/targets/qa/ remain explicitly listed, exactly five decision items remain unticked, and exactly one ticked item exists — the preserved historical width-ordering override. No new approval is recorded, invented, or implied by this plan."
    - "The extraction pipeline, its locked constants, its tests, its diagnostic scripts, and its published artifacts are unchanged by this plan: `check_targets.py` still exits 0 printing its pass line, and git reports no change under src/, tests/, scripts/, processed_data/, or data/raw/."
    - "[SPEC edge/boundary R1] A per-column peak-baseline separation exactly at MIN_PEAK_BASELINE_SEPARATION_MM is valid (only below invalidates, D-04), and exactly 10 consecutive NaNs is still interpolated (only exceeding 10 invalidates, D-06) — inherited invariant, unchanged by this plan."
    - "[SPEC edge/adjacency R1] SG smoothing is allowed to blend valid points from both sides of a masked gap (standard savgol_filter NaN-skip behavior) — inherited invariant, unchanged by this plan."
    - "[SPEC edge/empty R1] A track with zero valid x-positions after masking raises a hard error rather than silently producing an all-NaN artifact — inherited invariant, unchanged by this plan."
    - "[SPEC edge/precision R1] Percentile calculation uses numpy's default linear-interpolation convention, and a crossed-boundary case (y_upper < y_lower, negative width) marks that x-column invalid rather than reporting a negative or clipped-to-zero width — inherited invariant, unchanged by this plan."
    - "[SPEC edge/boundary R2] Crop-edge behavior at 20mm and 100mm is D-12's SG edge/interp boundary mode, and remains visually inspectable in the four track_*_width.png figures this document points the reviewer at."
    - "[SPEC edge/adjacency R2] Invalid x-positions keep their grid slot with width=NaN and mask=False (D-07) — no ragged or dropped positions at valid/invalid boundaries — inherited invariant, unchanged by this plan."
    - "[SPEC edge/empty R2] Same as R1: an all-invalid track raises a hard error rather than persisting an all-NaN artifact — inherited invariant, unchanged by this plan."
    - "[SPEC edge/precision R2] Persisted target arrays remain float64 dtype — inherited invariant, unchanged by this plan."
  artifacts:
    - ".planning/phases/01-target-extraction-contract/01-SIGNOFF-REQUEST.md"
    - ".planning/phases/01-target-extraction-contract/01-17-SUMMARY.md"
  key_links:
    - "live `scripts/check_targets.py` stdout + `processed_data/targets/manifest.json` -> 01-SIGNOFF-REQUEST.md '## Current state': every number and identifier must be re-derived by running the checker and reading the manifest in THIS execution. Nothing may be transcribed from this plan's text, from a prior ORDERING-OUTCOME document, or from the document's own existing content. The verification gate parses the live checker's own stdout and the manifest's own JSON — it never compares against a literal written in this plan."
    - "processed_data/targets/manifest.json `run_id` -> the document's provenance line: the document must cite exactly one 32-hex run_id and it must be the live one. This is the exact invariant whose violation created this gap."
    - "01-CONTEXT.md Amendment A8 -> 01-SIGNOFF-REQUEST.md '## Contract in effect': the reviewer is being asked to accept a contract, so every rule in force — including A8's two no-new-constant behavioral rules — must appear."
    - "01-16-ORDERING-OUTCOME.md's measured fragmentation/jump/coverage before-after tables -> '## Figures to inspect': the reviewer must be told what changed and what did NOT improve before being asked to judge the overlays."
    - "01-VERIFICATION.md's two Human Verification items -> '## Decisions requested': every decision item must name which verification item it closes, and the widened-gap reaffirmation must exist as its own unticked item."
    - "01-SIGNOFF-REQUEST.md -> REQUIREMENTS.md TARGET-02 row: TARGET-02 flips to complete only after a human records sign-off via /gsd-verify-work 1, never as a side effect of this plan. This plan does not touch REQUIREMENTS.md at all."
  prohibitions:
    - statement: "[SPEC] Extraction contract constants (noise-floor threshold, gap limit, percentile split, and every A3-A8 amendment constant) must NOT be adjusted based on whether the resulting width ordering or QA plots look correct. This plan changes no constant, re-runs no extraction, and re-derives numbers in read-only mode only."
      status: resolved
      verification: judgment
    - statement: "[SPEC] Extraction and persistence code must NOT write to, delete, or modify any file under data/raw/. This plan writes only to .planning/ and never invokes the extraction pipeline in write mode."
      status: resolved
      verification: test
    - statement: "No source, test, script, or published artifact may be modified: src/targets.py, src/nsf_fmrg_data.py, scripts/run_target_extraction.py, scripts/check_targets.py, every diagnostic script, everything under tests/, and everything under processed_data/ are read-only this round."
      status: resolved
      verification: test
    - statement: "The extraction pipeline must NOT be re-run. No .npz file, QA PNG, manifest.json, or extraction_params.json may be regenerated. The checked-in artifacts are the current generation; read-only inspection is required, rewriting is forbidden."
      status: resolved
      verification: test
    - statement: "HONEST-OUTCOME GUARD: the 10-vs-14 width-ordering FLAG must NOT be reopened, re-litigated, threshold-chased, or re-investigated. It is an accepted, documented, four-times-independently-diagnosed known limitation. This plan states the current magnitude honestly and carries the existing acceptance rationale forward — nothing more."
      status: resolved
      verification: judgment
    - statement: "The regenerated document must NOT make the numbers look better. No softening, no favorable rounding, no omission of a worsened figure, no framing that implies Amendment A8 fixed fragmentation or narrowed the ordering gap when it measurably did neither."
      status: resolved
      verification: judgment
    - statement: "This plan must NOT self-certify visual QA. No new checkbox may be ticked, no approval recorded, and TARGET-02 must not be marked complete on the reviewer's behalf. The one pre-existing ticked item is a historical human record and must be preserved, not replicated."
      status: resolved
      verification: test
---

<objective>
Regenerate `.planning/phases/01-target-extraction-contract/01-SIGNOFF-REQUEST.md` so the standing human sign-off document describes the artifact generation that is actually on disk (Amendment A8, produced by plan 01-16), making a fresh `/gsd-verify-work 1` visual sign-off round possible and unambiguous.

`01-SIGNOFF-REQUEST.md` was last written by plan 01-15 against Amendment A7. Gap-closure plan 01-16 has since landed Amendment A8 (clip-exclusion reordered before run merging; a history-based far-AND-small plausibility gate), regenerated all four tracks' `.npz` artifacts, and regenerated all 12 QA PNGs. The document's provenance line, its `## Current state` narrative, its numeric results block, its coverage table, and its `## Contract in effect` table all describe the superseded generation. `01-VERIFICATION.md` records this as the single blocking gap for the phase: a human granting sign-off against this document today would be confirming a narrative that no longer matches the pipeline output — and the 12 image files they would open ARE current, so the mismatch is invisible from the images alone.

This is the same staleness defect class plan 01-15 already fixed once, for the A6→A7 transition. It recurred for A7→A8.

Purpose: TARGET-02's own acceptance criterion is a human confirmation of the QA figures. That confirmation is meaningful only if the document driving it is accurate — and specifically only if it makes two facts impossible to miss: the 10-vs-14 ordering gap WIDENED under Amendment A8 rather than narrowing, and `extraction_params_sha256` did NOT change between A7 and A8 (because A8 introduced no new named constant), so a matching digest is not evidence that a document is current.
Output: An accurate, unsigned sign-off request describing the live generation. Nothing else — no source, no tests, no scripts, no constants, no pipeline run, no requirements flip.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-target-extraction-contract/01-VERIFICATION.md
@.planning/phases/01-target-extraction-contract/01-SPEC.md
@.planning/phases/01-target-extraction-contract/01-CONTEXT.md
@.planning/phases/01-target-extraction-contract/01-16-ORDERING-OUTCOME.md
@.planning/phases/01-target-extraction-contract/01-15-PLAN.md
</context>

<artifacts_produced>
Artifacts this phase produces — every file path this plan creates or modifies:

- **Modified:** `.planning/phases/01-target-extraction-contract/01-SIGNOFF-REQUEST.md` (rewritten in place; section shape preserved, content refreshed to the live Amendment A8 generation)
- **Created:** `.planning/phases/01-target-extraction-contract/01-17-SUMMARY.md` (execution summary)

No new code symbols, constants, CLI flags, tests, source files, scripts, or data artifacts. This plan touches exactly one file outside its own summary, and that file is inside `.planning/`.
</artifacts_produced>

<source_coverage_audit>
Every item from every source artifact, mapped to the plan that covers it. Nothing is silently dropped.

| Source | Item | Covered by | Status |
|---|---|---|---|
| GOAL (ROADMAP Phase 1, Success Criterion 3) | QA plots overlay width/boundary on raw+detrended maps for all 4 tracks incl. track 21's gaps, and are visually confirmed sane | 01-17 Tasks 1-2 make the confirmation round possible against current figures; the confirmation itself is the human's `/gsd-verify-work 1` round after this plan | COVERED (plan enables; human decides) |
| REQ | TARGET-01 (contract specified and documented before extraction code is trusted) | 01-17 Task 2 — `## Contract in effect` restates the full in-force contract incl. Amendment A8's behavioral rules, so the reviewer confirms the contract they are accepting | COVERED |
| REQ | TARGET-02 (extractor implements contract, persisted, visually QA'd against all 4 tracks) | 01-17 Tasks 1-2 — the sign-off vehicle is made accurate; TARGET-02's own checkbox stays `[ ]` until a human records approval | COVERED (blocked on human, by design) |
| VERIFICATION gap 1, missing item 1 | Regenerate 01-SIGNOFF-REQUEST.md against live A8 check_targets output + manifest.json (run_id, published timestamp, sha256 cross-check, summary table, coverage table, plain-language paragraphs incl. widened gap + A8 coverage/fragmentation before-after, Contract-in-effect table) | 01-17 Task 1 (provenance, results, coverage, plain-language) and Task 2 (contract table, figures, decisions) | COVERED |
| VERIFICATION gap 1, missing item 2 | A fresh human visual sign-off round via `/gsd-verify-work 1` | NOT an executor task by design. Task 2's `<human-check>` routes it; the document names `/gsd-verify-work 1` as the only recording route | COVERED (explicitly deferred to human) |
| VERIFICATION gap 1, missing item 3 | Update G-01-6's status in 01-UAT.md once the human round completes | 01-18 Task 2 annotates G-01-6 WITHOUT flipping its verdict; the flip belongs to the human round | COVERED (annotate-only, by design) |
| VERIFICATION Human Verification item 2 | Reaffirm (not reopen) the 10-vs-14 FLAG acceptance at the widened magnitude | 01-17 Task 2 — a new unticked reaffirmation decision item | COVERED |
| VERIFICATION Anti-Pattern (Warning) | `scripts/diagnose_track10_coverage.py` drifted out of sync with production `halfmax_edges` after A8 | 01-18 Task 3 — in-file historical-baseline disclaimer (WR-01 fix option (b)) | COVERED |
| Capability gate (api-coverage, false positive) | `COVERAGE.md` absent; seal-time gate will re-fire the detector and block | 01-18 Task 1 — reasoned no-external-API declaration | COVERED |
| CONTEXT | D-01/D-03 half-max width definition; D-04 separation floor; D-06 gap rule; D-07 fixed 0.2mm grid + mask slot retention; D-12 SG edge mode; D-14 residual visual check | 01-17 must_haves SPEC-edge truths (inherited invariants) and Task 2's figure questions | COVERED (inherited, unchanged) |
| CONTEXT | Amendments A3-A7 locked constants | 01-17 Task 2 — the seven-row constant table is preserved with its fixed-from rationale | COVERED |
| CONTEXT | Amendment A8 (clip-before-merge reorder; history-based joint gate; no new constant; Mechanism C reaffirmed deferred) | 01-17 Task 2 — two new behavioral rows + Mechanism C disclosure in the figures intro | COVERED |
| CONTEXT | HONEST-OUTCOME GUARD | 01-17 `must_haves.prohibitions` + Task 2 preserves the recorded option-(a) acceptance instead of re-deciding it | COVERED |
| CONTEXT | Claude's Discretion (noise-floor value; savgol boundary mode) | Already resolved in earlier plans; no discretion is exercised in this doc-only round | NOT APPLICABLE (already locked) |
| CONTEXT | Deferred Ideas | None recorded in 01-CONTEXT.md | NOT APPLICABLE |
| RESEARCH / debug | Mechanism C (DP/Viterbi joint-tracker gap; residual far-edge tail jitter) | Disclosed to the reviewer in Task 2's figures intro, NOT implemented | DEFERRED (source-authorized; 01-14-PLAN.md and Amendment A8 both scope it out) |
| RESEARCH / debug | Left-edge unassessable crop bands (MIN_COLUMNS_PER_BIN / MAX_GAP_PIXELS native-data-availability limit) | Disclosed as expected, non-defective behavior in Task 2's width-curve question | NOT A GAP (accepted per diagnosis) |
| STATE.md staleness (Info) | "Plan: 2 of 16" / "Ready to execute" | Handled by the plan-phase workflow's own STATE.md update, not by an executor task | COVERED (workflow-owned) |

No item is MISSING. No item is deferred without a source that already authorized the deferral.
</source_coverage_audit>

<assumption_delta_decision>
**Capability signal:** `assumption-delta` fired (advisory, non-blocking) on two signals — `pluralization`/"fallback" and `chosen`/"configurable".

**Decision: `no-change`.**

**Rationale:** Both signals are incidental matches in historical planning prose, not a live identity-model change in this round. "Fallback" comes from the earlier 10-vs-14 option-(a) fallback discussion in `01-13-PLAN.md`; "configurable" comes from Amendment A3's configurable-order detrend, locked in plan 01-04 and unchanged since. This plan introduces no entity, no cardinality change, no new configurability, and no persisted-format change — it rewrites one markdown document. No architectural migration is invented, proposed, or implied.
</assumption_delta_decision>

<assumptions>
Explicit assumptions and deltas, flagged rather than left as unstated premises.

1. **This plan deliberately contains no figures to transcribe.** The defect being fixed is a document quoting numbers that drifted from live artifacts. Baking the current numbers into this plan would recreate exactly that failure mode one level up. Every number in the regenerated document must be derived at execution time from `.venv/bin/python scripts/check_targets.py --project_dir .` and `processed_data/targets/manifest.json`. Every acceptance criterion below therefore asserts **document-to-live-source agreement**, never equality to a literal written here.

2. **Delta from plan 01-15's premise.** 01-15's document says the 10-vs-14 gap "roughly doubled" under Amendments A6/A7 and reports it at the A7 magnitude. Under Amendment A8 the gap widened further. This plan replaces the A7 magnitude with the live one and preserves the direction of travel (widening), never reversing or softening it.

3. **Delta from plan 01-15's premise.** 01-15's document describes track 10's coverage margin above the hard 50% floor as razor-thin. Under Amendment A8 that margin widened materially (`01-16-ORDERING-OUTCOME.md`). The regenerated document must state the live margin — this is a case where the honest current number is *better* than the stale one, and it must be reported at its live value rather than kept pessimistic for continuity.

4. **The provenance digest is a trap, and this plan treats it as one.** `extraction_params_sha256` is identical between the A7 and A8 generations because Amendment A8 introduced no new named constant (a deliberate design choice recorded in 01-CONTEXT.md, not an oversight). A reader who cross-checks only the digest would wrongly conclude the document is current. The regenerated document must say this out loud.

5. **The one existing ticked checkbox is a human record, not this plan's to reproduce or revoke.** `01-SIGNOFF-REQUEST.md` already carries a ticked width-ordering-override item recording a human's option-(a) selection on 2026-07-22 via `/gsd-verify-work 1` Test 7. The HONEST-OUTCOME GUARD forbids reopening it. This plan preserves that record verbatim, updates only the surrounding "current position" numbers, and adds a separate unticked item asking for reaffirmation at the widened magnitude.

6. **Interpretation of "Decisions requested" alignment.** `01-VERIFICATION.md` lists two Human Verification items while the document uses granular checkboxes. This plan continues 01-15's interpretation: the granular items are the concrete questions verification item 2 refers to, and each names which verification item it closes. This is an interpretation, not a stated fact in either document.

7. **SPEC dismissed edges surfaced, not dropped.** `01-SPEC.md`'s Edge Coverage reports 8/10 covered, 0 unresolved, 2 dismissed — both `ordering` edges (R1 and R2), dismissed because the output x-grid order is fixed by construction (D-07) and no data-dependent sort exists in the extraction algorithm. Recorded here rather than silently omitted from `must_haves.truths`. There are no `backstop` edges in this SPEC.

8. **Schema-push detection.** No ORM exists in this repository. This plan introduces no schema, no migration, and no persisted-format change. `extraction_params.json`'s key set, the `.npz` array layout, and `manifest.json`'s field set are read-only inputs.

9. **Live artifacts outrank this plan's text — unconditionally.** If the executor's live run produces anything at odds with a qualitative claim in this plan (e.g. the ordering verdicts differ from what this plan describes), the live artifacts win, the document states the live reality, and the disagreement is recorded in `01-17-SUMMARY.md`'s deviation record. It is never silently reconciled toward this plan's narrative.
</assumptions>

<tasks>

<task type="tracer">
  <name>Task 1: Re-derive the live generation and land it in "## Current state" — one path, live source to document, end to end</name>
  <files>.planning/phases/01-target-extraction-contract/01-SIGNOFF-REQUEST.md</files>
  <read_first>
    - `.planning/phases/01-target-extraction-contract/01-SIGNOFF-REQUEST.md` — the whole file. Note its section order (`# Phase 1 Human Sign-Off Request: Target Extraction QA Figures` → `## Current state` → the fenced live-results block → the coverage table → the plain-language bullets → `## Contract in effect` → `## Figures to inspect` with three subsections → `## Decisions requested` → `## How to record the outcome` → `## Closing note`) and its header block (`**Requested:**`, `**Requested by:**`, and the bolded "This document REQUESTS sign-off. It does not grant it." line). The shape is preserved; the content is refreshed.
    - `.planning/phases/01-target-extraction-contract/01-VERIFICATION.md` — the frontmatter `gaps:` block (the single blocking gap, its `artifacts:` entry, and all three `missing:` items), the `gaps_remaining:` list, the `overrides:` entry, and both Human Verification sections.
    - `.planning/phases/01-target-extraction-contract/01-16-ORDERING-OUTCOME.md` — the whole file, and specifically: the "Extraction contract in effect" line, the verbatim regenerated-results block, the "## Coverage" table with its per-track before-to-after bin deltas, the "## Fragmentation-count and jump-statistic before/after comparison" table, the two "## Crop-edge last-6-grid-point inspection" tables, and the "## Verdict" section stating the 10-vs-14 gap widened and by how much.
    - `.planning/phases/01-target-extraction-contract/01-CONTEXT.md` — the "### Amendment A8" subsection in full, including its explicit statement that neither mechanism introduces a new named constant and that `extraction_params()`'s existing key set and provenance digest tests are correctly left unchanged.
    - `processed_data/targets/manifest.json` — `run_id`, `published_at_utc`, `extraction_params_sha256`, `track_ids`.
    - `.planning/phases/01-target-extraction-contract/01-15-PLAN.md` — Task 1, as the structural precedent for this exact operation on the previous amendment transition.
  </read_first>
  <precondition>`.venv/bin/python` exists and `scripts/check_targets.py --project_dir .` runs read-only against the checked-in `processed_data/targets/` artifacts and exits 0. If the checker cannot run or exits non-zero, HALT — the document must never be regenerated from transcribed numbers.</precondition>
  <reversibility rating="reversible">A markdown rewrite inside `.planning/`, fully recoverable via git. No code, constant, artifact, or requirement record is touched.</reversibility>
  <action>
    Run `.venv/bin/python scripts/check_targets.py --project_dir .` ONCE, capture its full stdout and exit code, and read `processed_data/targets/manifest.json`. Derive every number, identifier, and timestamp written into the document from THAT stdout and THAT file, in this execution. Do not transcribe any figure from this plan, from `01-16-ORDERING-OUTCOME.md`'s quoted blocks, or from the document's existing content — `01-16-ORDERING-OUTCOME.md` is read for its before-to-after *narrative* (what changed relative to the prior amendment), not as the source of current values.

    Do NOT run `scripts/run_target_extraction.py`. Do NOT regenerate any `.npz`, QA PNG, `manifest.json`, or `extraction_params.json`. Do NOT modify `.planning/REQUIREMENTS.md`.

    Update the header block: set `**Requested:**` to the current date and change the requester line to name plan 01-17 as the gap-closure regeneration of the plan 01-15 original. Leave the bolded "This document REQUESTS sign-off. It does not grant it." sentence and its no-checkbox-may-be-inferred-as-approved clause exactly as written.

    Rewrite `## Current state` end to end against the live run:

    Replace the contract-attribution line so it names Amendment A8 sitting on top of A3 through A7, sourced from `01-16-ORDERING-OUTCOME.md`'s own "Extraction contract in effect" line and from `01-CONTEXT.md`'s Amendment A8 text: the same locked constants as the prior generation, plus (new in A8) `halfmax_edges` applying the D-01/D-03 clip-exclusion test to each raw run BEFORE `merge_adjacent_runs` is called, and gating tracked selection of a lone same-column-plausible candidate by a joint far-AND-small check against the runtime-derived `previous_length_mm` — applied identically to all four tracks with no per-track branch.

    State the artifact generation explicitly from `manifest.json`: the `run_id`, the `published_at_utc` timestamp, and the `extraction_params_sha256`. Then add a sentence that makes the digest's behavior explicit: the `extraction_params_sha256` value is UNCHANGED from the prior generation because Amendment A8 introduced no new named constant (deliberate, per 01-CONTEXT.md), so the digest alone cannot distinguish this generation from the previous one — `run_id` is the field that identifies which generation a reader is looking at. Do not let a reader infer currency from a matching digest.

    Replace the fenced results block with the checker's stdout copied verbatim, every line, including the results header row, all four per-track rows, all three adjacent-pair ordering verdict lines, the FLAG-outcomes sentence, and the final pass line. Do not reformat, re-align, round, or reorder any line — a downstream gate asserts every line of the live stdout appears verbatim as its own line in this document. State the exit code separately, as observed.

    Replace the coverage table's four rows using the live valid-bin counts against the 400-position target grid, computing each valid fraction as bins/400 expressed as a percentage to two decimal places. Keep the table's existing column shape (track, power, valid bins, valid fraction, verdict against the floor) so each track's count and fraction stay co-located on that track's own row. Beneath the table, state each track's margin against the hard `MIN_VALID_FRACTION = 0.5` floor at its live value, and state how the tightest track's margin moved relative to the prior generation, citing `01-16-ORDERING-OUTCOME.md`'s measured per-track bin deltas. If the live margin is more comfortable than the prior generation's, say so plainly — do not preserve a pessimistic framing that the live numbers no longer support.

    Rewrite the "Plain-language state" bullets so every claim matches the live run and none softens a bad result:
    - State which adjacent pairs PASS and which FLAG, exactly as the checker prints them, with each track's live median.
    - State the current absolute 10-vs-14 gap as the difference between the two live medians, and say explicitly that this gap WIDENED under Amendment A8 rather than narrowing or closing. Cite `01-16-ORDERING-OUTCOME.md`'s "## Verdict" section as the source of the before-to-after comparison, and state the prior-generation magnitude it is being compared against. Do not describe the gap as narrowing, closing, or improving anywhere in the document.
    - State that the checker passes because the coverage floor is the only hard gate it enforces; it does not gate on the width-ordering FLAG, which is diagnostic output.
    - State track 10's live coverage position honestly relative to where it was under the prior amendment, citing `01-16-ORDERING-OUTCOME.md`'s coverage table.
    - Add a bullet carrying Amendment A8's fragmentation and jump-statistic outcome forward exactly as `01-16-ORDERING-OUTCOME.md` measured it: which tracks' contiguous valid-run counts improved and by how much, which worsened, and that the jump statistics are mixed rather than uniformly improved. Name the track whose fragmentation improvement was largest and the track that got marginally worse. Nowhere imply that fragmentation is fixed.
    - Add a bullet stating that Mechanism C (the greedy nearest-to-`previous_center` selection among simultaneously-plausible candidates) was NOT addressed by Amendment A8, remains the explicitly-deferred DP/Viterbi joint-tracker gap, and its residual effect is still visible as column-to-column instability at both domain far edges — per `01-16-ORDERING-OUTCOME.md`'s "## Mechanism C's residual effect" section.

    Do not tick any checkbox. Do not add a checkbox. Do not edit any other section of the file in this task. Do not edit any file outside `.planning/`.
  </action>
  <verify>
    <automated>T=$(mktemp); D=.planning/phases/01-target-extraction-contract/01-SIGNOFF-REQUEST.md; MJ=processed_data/targets/manifest.json; .venv/bin/python scripts/check_targets.py --project_dir . > "$T" 2>&1 || { echo "CHECKER FAILED"; exit 1; }; while IFS= read -r L; do [ -z "$L" ] && continue; grep -qxF "$L" "$D" || { echo "DOC MISSING LIVE CHECKER LINE: $L"; exit 1; }; done < "$T"; RUN=$(.venv/bin/python -c "import json;print(json.load(open('$MJ'))['run_id'])"); SHA=$(.venv/bin/python -c "import json;print(json.load(open('$MJ'))['extraction_params_sha256'])"); PUB=$(.venv/bin/python -c "import json;print(json.load(open('$MJ'))['published_at_utc'])"); test "$(grep -oE '\b[0-9a-f]{32}\b' "$D" | sort -u)" = "$RUN" || { echo "DOC DOES NOT CITE EXACTLY THE LIVE run_id"; exit 1; }; test "$(grep -oE '\b[0-9a-f]{64}\b' "$D" | sort -u)" = "$SHA" || { echo "DOC DOES NOT CITE EXACTLY THE LIVE sha256"; exit 1; }; grep -qF "$PUB" "$D" || { echo "DOC MISSING LIVE published_at_utc"; exit 1; }; for row in $(awk 'NF==5 && ($1=="8"||$1=="10"||$1=="14"||$1=="21"){printf "%s:%s:%.2f%%\n",$1,$3,$3/4}' "$T"); do tr=${row%%:*}; r=${row#*:}; b=${r%%:*}; f=${r##*:}; grep -qE "^\| *$tr *\|.*[| ]$b[ |].*$f" "$D" || { echo "COVERAGE ROW NOT LIVE OR NOT CO-LOCATED: track $tr"; exit 1; }; done; GAP=$(awk 'NF==5&&$1=="10"{a=$4} NF==5&&$1=="14"{b=$4} END{printf "%.4f",b-a}' "$T"); grep -qF "$GAP" "$D" || { echo "DOC MISSING LIVE 10-vs-14 GAP $GAP"; exit 1; }; grep -qiF 'widened' "$D" || { echo "DOC DOES NOT STATE THE GAP WIDENED"; exit 1; }; grep -iE '(sha256|digest)' "$D" | grep -qi 'unchanged' || { echo "DOC DOES NOT DISCLOSE THE DIGEST IS UNCHANGED ACROSS GENERATIONS"; exit 1; }; grep -qF 'Amendment A8' "$D" || exit 1; test "$(grep -c -F 'Amendment A7 on top of' "$D")" -eq 0 || { echo "STALE CONTRACT-IN-EFFECT ATTRIBUTION SURVIVED"; exit 1; }; test -z "$(git status --porcelain src tests scripts processed_data data/raw)" || { echo "SOMETHING OUTSIDE .planning CHANGED"; exit 1; }; echo "CURRENT STATE IS LIVE"</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/bin/python scripts/check_targets.py --project_dir .` was run during this task and exits 0.
    - Every non-empty line of that live run's stdout appears verbatim as its own full line in `01-SIGNOFF-REQUEST.md` — asserted by the `grep -qxF` loop in `<verify>`, which reads the checker's own stdout. A transcription from this plan or from a prior outcome report cannot satisfy this, and neither can a partially-updated results block.
    - `grep -oE '\b[0-9a-f]{32}\b' 01-SIGNOFF-REQUEST.md | sort -u` produces exactly one value, and it is byte-identical to `.run_id` in `processed_data/targets/manifest.json`. No superseded run_id survives anywhere in the document.
    - `grep -oE '\b[0-9a-f]{64}\b' 01-SIGNOFF-REQUEST.md | sort -u` produces exactly one value, and it is byte-identical to `.extraction_params_sha256` in `processed_data/targets/manifest.json`.
    - The document contains `.published_at_utc` from `manifest.json` verbatim.
    - For each of the four tracks, the coverage table has a row beginning with that track id on which the live valid-bin count and the live-derived valid fraction (bins/400, two decimal places, percent) are co-located — asserted by the awk-driven loop in `<verify>`. A transposition of two tracks' rows fails this check even if every individual value is present somewhere in the file.
    - The document contains the 10-vs-14 absolute gap computed from the live medians (`median_14 - median_10`, four decimal places) — asserted by a value awk-derived from the live checker output, not by comparison against any literal in this plan.
    - The document contains the word `widened` describing the direction of the 10-vs-14 gap, and cites `01-16-ORDERING-OUTCOME.md`.
    - At least one line of the document mentions the provenance digest (`sha256` or `digest`) and the word `unchanged` together — the reader is told the digest does not distinguish this generation from the previous one.
    - The document contains `Amendment A8`, and `grep -c -F 'Amendment A7 on top of' 01-SIGNOFF-REQUEST.md` returns 0 — the superseded contract-in-effect attribution is gone.
    - `git status --porcelain src tests scripts processed_data data/raw` is empty — no source, test, script, artifact, or raw-data file was touched.
  </acceptance_criteria>
  <done>`## Current state` describes the artifact generation actually on disk, every value sourced from a live checker run and a live manifest read in this execution, with the widened ordering gap and the non-distinguishing provenance digest both stated plainly and neither softened.</done>
</task>

<task type="auto">
  <name>Task 2: Refresh the contract table, figure questions, and decision set for Amendment A8 — preserving the recorded override, adding the reaffirmation ask</name>
  <files>.planning/phases/01-target-extraction-contract/01-SIGNOFF-REQUEST.md</files>
  <read_first>
    - `.planning/phases/01-target-extraction-contract/01-SIGNOFF-REQUEST.md` — the state left by Task 1, plus the sections still to refresh: `## Contract in effect` (a seven-row constant / value / fixed-from table plus its trailing paragraph), `## Figures to inspect` with its three subsections and all 12 explicit figure paths, `## Decisions requested` (four unticked items plus one already-ticked width-ordering-override item carrying a dated human record and its option (a)/(b) bodies), `## How to record the outcome`, and `## Closing note`.
    - `.planning/phases/01-target-extraction-contract/01-CONTEXT.md` — the "### Amendment A8" subsection in full: Mechanism A's reordering (clip-exclusion applied to raw runs before `merge_adjacent_runs`, introducing no new constant, only a reordering), Mechanism B's history-based joint far-AND-small gate (reusing `MIN_TRACKED_LENGTH_RATIO` and the runtime-derived `previous_length_mm`, introducing no new named constant), Mechanism C's reaffirmed deferral, and the closing paragraph's restatement of the no-outcome-driven-tuning prohibition including that the 10-vs-14 FLAG is not reopened.
    - `.planning/phases/01-target-extraction-contract/01-16-ORDERING-OUTCOME.md` — the fragmentation/jump-statistic before-after table, both crop-edge last-6-grid-point tables (tracks 10 and 21) with their honest "symptom resolved but residual instability persists" verdicts, the "## Mechanism C's residual effect" section, and the "## Human verification round context" section.
    - `.planning/phases/01-target-extraction-contract/01-UAT.md` — the G-01-6 entry's `reason:` field, which names the exact visual complaints the reviewer previously raised (track 10 fragmentation, tracks 14/21 EKG-like jumps, track 8's excursion regions, track 10's isolated valid sample in the right crop-edge band, track 21's abrupt terminal drop, all four left bands unassessable).
    - `.planning/phases/01-target-extraction-contract/01-VERIFICATION.md` — both Human Verification sections verbatim, so each decision item can name the item it closes, and the second one's explicit framing of the widened-gap question as a REAFFIRMATION rather than a reinvestigation.
    - `.planning/phases/01-target-extraction-contract/01-SPEC.md` — the `## Prohibitions (must-NOT)` table's judgment-tier item, which the human reviewer is the confirming authority for.
    - `processed_data/targets/qa/` — confirm all 12 PNG filenames on disk match the 12 paths the document lists, so no path in the document points at a file that does not exist.
  </read_first>
  <precondition>Task 1 has already rewritten `## Current state` and its numbers are live; this task edits the remaining sections of the same file and must not revert Task 1's values or its provenance line.</precondition>
  <reversibility rating="reversible">Markdown edits within `.planning/`, fully recoverable via git.</reversibility>
  <action>
    Keep all seven existing rows of `## Contract in effect` (`DETREND_POLY_ORDER`, `BEAD_MASK_HEIGHT_FRACTION`, `MAX_TRACKING_GAP_COLUMNS`, `DETREND_MAX_Y_DEGREE`, `DETREND_MAX_XY_DEGREE`, `MAX_RUN_MERGE_GAP_PIXELS`, `MIN_TRACKED_LENGTH_RATIO`) with their values and fixed-from rationale intact — Amendment A8 changed none of them. Add two rows for Amendment A8's behavioral rules, sourced from `01-CONTEXT.md`'s Amendment A8 text:
    - A row for the clip-before-merge reordering: `halfmax_edges` applies the D-01/D-03 boundary-clip-exclusion test to each raw run before `merge_adjacent_runs` is called, so a merged candidate can structurally never itself touch a y-strip boundary. Its value cell must state that this is a reordering of two existing steps and introduces no new constant.
    - A row for the history-based joint plausibility gate: a lone same-column-plausible candidate is rejected only when it is BOTH farther from `previous_center` than the recently-tracked run's own physical width AND smaller than `MIN_TRACKED_LENGTH_RATIO` of that same width, with the distance scale derived from the runtime `previous_length_mm` value. Its value cell must state that it reuses an already-locked ratio against a runtime-derived quantity and introduces no new named constant.

    Update the paragraph beneath the table so it covers Amendments A3 through A8, keeps the claim that every constant was fixed from residual, physical, numerical, or already-locked-contract evidence before the ordering outcome was inspected, and keeps the judgment-tier prohibition from `01-SPEC.md` that the reviewer is asked to confirm. Add one sentence explaining that Amendment A8 deliberately introduced no new named constant, which is why `extraction_params()`'s key set and the provenance digest quoted above are unchanged from the previous generation.

    Rewrite the intro paragraph of `## Figures to inspect` so the reviewer knows, before opening anything, exactly what `01-16-ORDERING-OUTCOME.md` measured: valid-bin coverage increased on every track; contiguous-run fragmentation improved on three of four tracks and got marginally worse on one; jump statistics are mixed; both crop-edge symptoms G-01-6 named are resolved; and Mechanism C's residual far-edge column-to-column instability was NOT addressed and remains visible. Frame the reviewer's job as judging whether the residual jitter is acceptable under the D-14 / success-criterion-3 standard — not as confirming that Amendment A8 fixed it. Source the specific fragmentation numbers from `01-16-ORDERING-OUTCOME.md`'s own before-after table; do not restate them from memory.

    Refresh each of the three figure subsections' questions against the current generation, keeping all 12 explicit `processed_data/targets/qa/` paths exactly as they appear on disk:
    - Residual maps: keep the existing questions about coherent track-wide bow and the y-strip-edge artifact. Note that the detrend path is unchanged by Amendment A8 (A8 touched candidate selection, not the surface fit), so these four surfaces should look like the previously-reviewed ones; a change here would itself be a finding.
    - Boundary overlays: keep the continuity and explicit-grey-shading questions, and replace the prior-generation observations with `01-16-ORDERING-OUTCOME.md`'s measured ones so the reviewer can confirm or dispute them — the per-track contiguous-run counts before and after, which tracks improved, which got marginally worse, and the specific excursion regions on track 8 that G-01-6's `reason:` field named.
    - Width curves: keep the crop-edge question and replace the prior-generation observations with `01-16-ORDERING-OUTCOME.md`'s two crop-edge last-6-grid-point findings — that track 10's isolated-valid-run symptom and track 21's near-zero terminal drop are both resolved, while column-to-column width instability persists at both far edges as Mechanism C's residual effect. Restate that all four left crop-edge bands are an expected, already-locked native-data-availability limitation (`MIN_COLUMNS_PER_BIN` / `MAX_GAP_PIXELS`), not a defect, so the reviewer does not re-report it as one.

    Rewrite `## Decisions requested` as follows, keeping the preamble that states ticking is a human action taken outside this document:
    - Keep the four unticked visual/constants items (residual maps, boundary overlays, crop-edge behavior, constants-fixed-independently-of-outcome). Update their bodies to reference the current generation and keep each one naming which of `01-VERIFICATION.md`'s Human Verification items it closes. Extend the constants item so the reviewer is asked to confirm Amendments A3 through A8, including A8's no-new-constant property.
    - PRESERVE the existing ticked width-ordering-override item exactly as a historical record: its ticked state, its original 2026-07-22 date, its `/gsd-verify-work 1` Test 7 attribution, and the reviewer's recorded rationale text stay as written. Update ONLY the "Current position" numbers inside it to the live values from Task 1, and add one sentence noting the magnitude has moved since that acceptance was recorded, citing `01-16-ORDERING-OUTCOME.md`. Do not un-tick it, do not re-word the recorded rationale, and do not present option (b) as newly open — the HONEST-OUTCOME GUARD forbids reopening this decision.
    - ADD one NEW unticked item: a reaffirmation of the recorded width-ordering acceptance at the widened magnitude, matching `01-VERIFICATION.md`'s Human Verification item 2. Its body must make clear this asks for a brief recorded confirmation that the already-accepted rationale still applies at the new magnitude — NOT a new investigation cycle, and NOT a reopening. State that the phase's HONEST-OUTCOME GUARD authorizes no further open-ended tuning absent a new, independently-diagnosed root cause, and that four such cycles have already been run.

    Update `## How to record the outcome` so it names `/gsd-verify-work 1` explicitly as the only recording route, states that `TARGET-02`'s checkbox and traceability row in `.planning/REQUIREMENTS.md` flip to complete only after that record exists, and states that this plan (01-17) does not and cannot make that change itself. Add that `01-UAT.md`'s G-01-6 gap entry likewise moves off its current failed state only as an outcome of that human round.

    Update `## Closing note` so its list of locked constants and behavioral rules covers Amendments A3 through A8, and keep its rule intact: a rejected item is answered by a new gap-closure plan, never by adjusting a locked constant, the `MIN_VALID_FRACTION` floor, or the boundary-clipped-run exclusion in `halfmax_edges` to make a figure look better.

    Do not tick any checkbox and do not add a ticked one. Do not revert Task 1's numbers or provenance line. Do not edit `01-UAT.md`, `01-VERIFICATION.md`, `.planning/REQUIREMENTS.md`, or any file outside `.planning/`.
  </action>
  <verify>
    <automated>D=.planning/phases/01-target-extraction-contract/01-SIGNOFF-REQUEST.md; for s in DETREND_POLY_ORDER BEAD_MASK_HEIGHT_FRACTION MAX_TRACKING_GAP_COLUMNS DETREND_MAX_Y_DEGREE DETREND_MAX_XY_DEGREE MAX_RUN_MERGE_GAP_PIXELS MIN_TRACKED_LENGTH_RATIO previous_length_mm merge_adjacent_runs previous_center MIN_VALID_FRACTION "Amendment A8" "/gsd-verify-work 1" "01-16-ORDERING-OUTCOME.md" "Mechanism C" SELECTED; do grep -qF "$s" "$D" || { echo "MISSING: $s"; exit 1; }; done; test "$(grep -c -F 'processed_data/targets/qa/track_' "$D")" -ge 12 || { echo "FEWER THAN 12 QA FIGURE PATHS"; exit 1; }; for f in processed_data/targets/qa/*.png; do grep -qF "$f" "$D" || { echo "FIGURE ON DISK NOT LISTED: $f"; exit 1; }; done; test "$(grep -cE '^- \[ \] \*\*' "$D")" -eq 5 || { echo "EXPECTED EXACTLY 5 UNTICKED DECISION ITEMS"; exit 1; }; test "$(grep -cE '^- \[x\] \*\*' "$D")" -eq 1 || { echo "EXPECTED EXACTLY 1 PRESERVED TICKED DECISION"; exit 1; }; grep -E '^- \[x\] \*\*' "$D" | grep -qiF 'width-ordering override' || { echo "THE SINGLE TICKED ITEM IS NOT THE PRESERVED WIDTH-ORDERING OVERRIDE"; exit 1; }; test "$(grep -c -F '[x]' "$D")" -eq 1 || { echo "A SECOND APPROVAL MARK APPEARED"; exit 1; }; grep -qF '2026-07-22' "$D" || { echo "ORIGINAL OVERRIDE DATE NOT PRESERVED"; exit 1; }; test "$(grep -c -F 'Amendment A7 on top of' "$D")" -eq 0 || exit 1; grep -qE '^- \[ \] \*\*TARGET-02\*\*' .planning/REQUIREMENTS.md || { echo "TARGET-02 WAS FLIPPED"; exit 1; }; test -z "$(git status --porcelain src tests scripts processed_data data/raw .planning/REQUIREMENTS.md)" || { echo "SOMETHING OUT OF SCOPE CHANGED"; exit 1; }; echo "CONTRACT, FIGURES AND DECISIONS REFRESHED FOR A8"</automated>
    <human-check>Open all 12 figures listed in `01-SIGNOFF-REQUEST.md` under `processed_data/targets/qa/` and record approvals, rejections, or the widened-gap reaffirmation via `/gsd-verify-work 1`. This plan cannot close the phase: TARGET-02's acceptance criterion and `01-VERIFICATION.md`'s two Human Verification items both require a person, and no automated check substitutes for either. Until that round is recorded, `01-UAT.md`'s G-01-6 entry stays on its current failed state and `REQUIREMENTS.md`'s TARGET-02 checkbox stays unticked.</human-check>
  </verify>
  <acceptance_criteria>
    - `01-SIGNOFF-REQUEST.md` contains all seven locked constant names: `DETREND_POLY_ORDER`, `BEAD_MASK_HEIGHT_FRACTION`, `MAX_TRACKING_GAP_COLUMNS`, `DETREND_MAX_Y_DEGREE`, `DETREND_MAX_XY_DEGREE`, `MAX_RUN_MERGE_GAP_PIXELS`, `MIN_TRACKED_LENGTH_RATIO`.
    - `01-SIGNOFF-REQUEST.md` contains `merge_adjacent_runs`, `previous_center`, and `previous_length_mm` — Amendment A8's two behavioral rules are described in terms of the actual production identifiers, not paraphrase.
    - The `## Contract in effect` table has at least nine data rows, and each of the two new rows names Amendment A8 and states in its value cell that it introduces no new named constant.
    - Every `.png` file present in `processed_data/targets/qa/` is referenced by path in the document — asserted by the `for f in processed_data/targets/qa/*.png` loop in `<verify>`, so a figure on disk cannot be silently omitted from the reviewer's list. `grep -c -F 'processed_data/targets/qa/track_'` returns at least 12.
    - `grep -cE '^- \[ \] \*\*' 01-SIGNOFF-REQUEST.md` returns exactly 5 — the four visual/constants items plus the one new widened-gap reaffirmation item, anchored at line start so an incidental bracket pair elsewhere cannot satisfy the count.
    - `grep -cE '^- \[x\] \*\*' 01-SIGNOFF-REQUEST.md` returns exactly 1, `grep -c -F '[x]'` returns exactly 1, and that single ticked line names the width-ordering override — the pre-existing human record is preserved and no new approval was forged.
    - The document still contains the string `2026-07-22` — the original recorded override date survived the rewrite.
    - The document contains `Mechanism C` and states its residual effect was not addressed by Amendment A8.
    - The document contains the literal `/gsd-verify-work 1` and cites `01-16-ORDERING-OUTCOME.md`.
    - `grep -c -F 'Amendment A7 on top of' 01-SIGNOFF-REQUEST.md` still returns 0.
    - `.planning/REQUIREMENTS.md` still shows `- [ ] **TARGET-02**` — this plan did not flip it, and `git status --porcelain .planning/REQUIREMENTS.md` is empty.
    - `git status --porcelain src tests scripts processed_data data/raw` is empty.
  </acceptance_criteria>
  <done>The reviewer sees the full Amendment A8 contract they are being asked to accept, is told what measurably improved and what measurably did not before opening a figure, finds their own prior width-ordering decision preserved rather than re-asked, and is handed one clear reaffirmation question at the current magnitude — with nothing pre-decided and no approval invented.</done>
</task>

</tasks>

<threat_model>
This round is documentation-only against a local, offline scientific data pipeline. There is no network surface, no authentication, no user input, no ORM, and no deployed runtime. Padding this register with web-application categories would be dishonest; the real ASVS-L1-relevant risks here are data-handling and record-integrity risks — an inaccurate sign-off artifact, a forged approval, accidental tampering with locked constants or published artifacts, and leaking competition-restricted raw data into a committed document. ASVS level 1; block on `high`.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| checked-in `processed_data/targets/` artifacts → regenerated markdown | The document's factual claims must be derived from the live artifacts in this run, not transcribed from a plan prompt or a prior report. A transcription error becomes a false statement a human signs off on — the exact defect this plan exists to fix, for the second time. |
| automated plan output → human sign-off record | A plan that ticks a checkbox, records an approval, or flips a requirement checkbox would forge the one gate TARGET-02 reserves for a human. A prior automated tooling step already did this once to TARGET-02 and had to be manually reverted. |
| doc-only plan → locked extraction contract and published artifacts | The phase's integrity rests on constants never being tuned toward a desired outcome. A documentation round must not become a covert tuning round. |
| competition-restricted `data/raw/` → committed `.planning/` markdown | `DATA_USE_LICENSE.md` restricts this dataset. The document may quote aggregate derived statistics (per-track medians, bin counts) that are already committed elsewhere in the repo, and must not embed raw height-map content, file excerpts, or per-pixel data. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-49 | Repudiation | `01-SIGNOFF-REQUEST.md` factual accuracy | high | mitigate | Task 1 derives every number from a live `check_targets.py` run and a live `manifest.json` read in this execution. `<verify>` asserts every non-empty line of the checker's own stdout appears verbatim in the document, and derives the coverage fractions and the 10-vs-14 gap with awk from that same stdout. No acceptance criterion compares against a literal written in this plan, so a transcription cannot pass. |
| T-01-50 | Spoofing | provenance identity of the described generation | high | mitigate | `<verify>` asserts the document cites exactly one 32-hex run_id and that it is byte-identical to `manifest.json`'s. Because `extraction_params_sha256` is identical across the A7 and A8 generations, the digest alone is a spoofable currency signal — the document is required to say so explicitly, and `<verify>` asserts a line mentioning the digest also says `unchanged`. |
| T-01-51 | Spoofing | human visual sign-off | high | mitigate | No task ticks a checkbox. `<verify>` asserts exactly one `[x]` exists in the whole file and that it is the pre-existing width-ordering override line carrying its original 2026-07-22 date — so neither a new approval nor a duplicated one can pass. Task 2 carries a `<human-check>` routing approval to `/gsd-verify-work 1`. `.planning/REQUIREMENTS.md` is asserted unchanged and TARGET-02 asserted still unticked. |
| T-01-52 | Tampering (information suppression) | worsened or improved numbers reframed | high | mitigate | The gate positively requires the live-derived 10-vs-14 gap value and the word `widened`, and requires each track's live bin count and derived fraction to be co-located on its own coverage row. Omitting or softening the widened gap fails mechanically. The prohibition block forbids favorable rounding and improvement-implying framing; the `<human-check>` is the judgment backstop. |
| T-01-53 | Tampering | locked extraction constants and published artifacts | high | mitigate | Both tasks assert `git status --porcelain src tests scripts processed_data data/raw` is empty. `run_target_extraction.py` is never invoked; `check_targets.py` is read-only against `processed_data/targets/`. `must_haves.prohibitions` names the source, test, script, constant, and pipeline freeze explicitly. |
| T-01-54 | Information disclosure | competition-restricted raw data | low | accept | No task reads, copies, or excerpts anything under `data/raw/`. The document quotes only aggregate per-track statistics already committed in `01-16-ORDERING-OUTCOME.md` and `manifest.json`. `git status --porcelain data/raw` is asserted empty in both tasks. No new data path, network call, or credential handling is introduced. |
| T-01-SC | Tampering | npm/pip/cargo installs | high | mitigate | No packages are installed by this plan and `requirements.txt` is unchanged. No `[ASSUMED]` or `[SUS]` package is introduced, so no legitimacy checkpoint is required. |
</threat_model>

<verification>
- `.planning/phases/01-target-extraction-contract/01-SIGNOFF-REQUEST.md` reproduces every non-empty line of a live `.venv/bin/python scripts/check_targets.py --project_dir .` run verbatim, cites exactly the live `run_id`, `published_at_utc`, and `extraction_params_sha256` from `processed_data/targets/manifest.json`, and cites no superseded identifier.
- The coverage table's per-track valid-bin counts and valid fractions are live-derived and co-located per track row; the 10-vs-14 gap is live-derived; the document states the gap widened and cites `01-16-ORDERING-OUTCOME.md`.
- The document discloses that the provenance digest is unchanged across the two generations because Amendment A8 introduced no new named constant.
- `## Contract in effect` lists all seven locked amendment constants plus Amendment A8's two behavioral rules, described using the production identifiers `merge_adjacent_runs`, `previous_center`, and `previous_length_mm`.
- Every `.png` in `processed_data/targets/qa/` is listed by path; exactly five decision items are unticked; exactly one is ticked and it is the preserved 2026-07-22 width-ordering override.
- `/gsd-verify-work 1` is named as the only recording route, and the document states TARGET-02 and G-01-6 move only as an outcome of that human round.
- `.planning/REQUIREMENTS.md` is unchanged and TARGET-02 remains unticked.
- `git status --porcelain src tests scripts processed_data data/raw` is empty across the whole plan — no source, test, script, constant, artifact, or raw-data file changed.
- The 10-vs-14 FLAG is not reopened: no extraction parameter is touched, no threshold is tuned, and the recorded human acceptance survives with its original date and rationale.
</verification>

<success_criteria>
- A human running `/gsd-verify-work 1` after this plan is reviewing a document that describes the exact artifact generation whose 12 images they are opening, with every number traceable to a command they can re-run themselves.
- The two facts most likely to be missed — that the ordering gap widened rather than narrowed, and that a matching provenance digest does not mean a current document — are both stated explicitly and neither is softened.
- The reviewer's own previously-recorded width-ordering decision is preserved rather than re-asked, and the only new question put to them is a bounded reaffirmation at the current magnitude.
- Nothing outside `.planning/` changed, and the extraction contract, its constants, its tests, its scripts, and its published artifacts are provably untouched.
</success_criteria>

<output>
Create `.planning/phases/01-target-extraction-contract/01-17-SUMMARY.md` when done. Record in its deviation section any case where a live artifact disagreed with a qualitative claim made in this plan — the live artifact wins, the disagreement is stated, and it is never silently reconciled toward this plan's narrative.
</output>
