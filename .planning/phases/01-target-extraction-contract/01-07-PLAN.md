---
phase: 01-target-extraction-contract
plan: 07
type: execute
wave: 7
depends_on: [01-06]
files_modified:
  - src/targets.py
  - src/nsf_fmrg_data.py
  - tests/test_targets.py
  - tests/test_nsf_fmrg_data.py
  - .planning/phases/01-target-extraction-contract/01-CONTEXT.md
autonomous: true
gap_closure: true
requirements: [TARGET-01, TARGET-02]
must_haves:
  truths:
    - "One uniform, track-independent correction — the leading candidate being bead-region masking before the detrend surface fit — is implemented in the shared extraction path and applied identically to all 4 tracks (Success Criterion 4)."
    - "extraction_params() returns a dict containing key 'MAX_TRACKING_GAP_COLUMNS' and the new fix parameter key, so the complete current extraction contract is machine-readable."
    - "Extraction provenance is change-sensitive: mutating MAX_TRACKING_GAP_COLUMNS or the new fix parameter changes the SHA-256 digest of the parameter dictionary."
    - "The continuity/stale-history tracking rule from plan 01-05 and the Gap-2 methodological fix are recorded as canonical Amendment A4 in 01-CONTEXT.md, in the same style as Amendment A3."
    - "A named synthetic regression proves the corrected detrend surface does not absorb an elevated bead into the fitted background, and that the masking rule uses one shared parameterization for tracks of differing height."
  artifacts:
    - "src/targets.py"
    - "src/nsf_fmrg_data.py"
    - "tests/test_targets.py"
    - "tests/test_nsf_fmrg_data.py"
    - ".planning/phases/01-target-extraction-contract/01-CONTEXT.md"
  key_links:
    - "extraction_params() -> extraction_params.json / manifest SHA-256 digest: the completed dict is what plan 01-08 regenerates into persisted provenance."
    - "new fix parameter -> robust_plane_detrend surface fit: bead pixels are excluded from the FIT while the fitted surface is still subtracted from all pixels."
  prohibitions:
    - "No per-track branches or per-track-tuned constants — one shared parameterization across all 4 tracks (SPEC Prohibitions; Success Criterion 4)."
    - "The fix mechanism and its parameter value must NOT be selected or adjusted to make the width ordering pass; they must follow the plan-06 pre-registered, outcome-independent criterion."
    - "No writes/deletes/modifications under data/raw/."
---

<objective>
Close both Phase 1 gaps at the contract level. (Gap 2) Apply exactly ONE uniform, track-independent correction chosen by the plan-06 pre-registered criterion — the endorsed leading remedy is masking the bead region before fitting the detrend surface so a high-order polynomial cannot absorb the bead's own cross-section into the "background," which is the diagnosed cause of the width collapse. (Gap 1) Complete the extraction contract's provenance: add the behavior-changing MAX_TRACKING_GAP_COLUMNS (introduced in 01-05) and the new fix parameter to extraction_params(), make the provenance digest change-sensitive, and record the continuity/stale-history rule plus this Gap-2 fix as canonical Amendment A4.

Purpose: TARGET-01 requires the complete method be reproducibly documented and provenance-locked; TARGET-02 requires the extractor apply one shared rule across all 4 tracks. This plan makes the contract complete and the fix uniform. Plan 01-08 then regenerates artifacts and reports the ordering as an honest outcome.
Output: A corrected, uniform detrend/extraction path; a complete, change-sensitive extraction_params(); and Amendment A4.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-target-extraction-contract/01-VERIFICATION.md
@.planning/phases/01-target-extraction-contract/01-CONTEXT.md
@.planning/phases/01-target-extraction-contract/01-06-DIAGNOSIS.md
@.planning/phases/01-target-extraction-contract/01-05-SUMMARY.md
@src/targets.py
@src/nsf_fmrg_data.py
@tests/test_targets.py
@tests/test_nsf_fmrg_data.py
</context>

<artifacts_produced>
New symbols and paths introduced by this plan (for downstream consumers):
- New constant in src/targets.py: `BEAD_MASK_HEIGHT_FRACTION` (float; baseline-relative height fraction above which pixels are excluded from the detrend surface fit) — if the plan-06 diagnosis instead implicates continuity/order, substitute the parameter that the chosen uniform fix requires and name it in the summary.
- New helper in src/targets.py: `bead_exclusion_mask(...)` returning a boolean (y, x) fit-mask (True = include in surface fit).
- New keyword argument on `robust_plane_detrend(...)`: `fit_mask=None` (full-resolution boolean array; when provided, pixels marked False are excluded from the least-squares fit but the fitted surface is still subtracted from every pixel).
- extraction_params() new keys: `MAX_TRACKING_GAP_COLUMNS` and `BEAD_MASK_HEIGHT_FRACTION` (or the chosen fix parameter).
- New tests in tests/test_targets.py: `test_provenance_includes_tracking_gap_and_fix_param`, `test_provenance_digest_is_change_sensitive`, `test_bead_mask_rule_is_track_independent`.
- New test in tests/test_nsf_fmrg_data.py: `test_robust_plane_detrend_fit_mask_excludes_bead_from_fit`.
- Updated test: `test_extraction_params_provenance` (new complete key set).
- Canonical amendment: Amendment A4 in 01-CONTEXT.md.
</artifacts_produced>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement the ONE uniform, track-independent detrend/extraction fix</name>
  <files>src/nsf_fmrg_data.py, src/targets.py, tests/test_nsf_fmrg_data.py, tests/test_targets.py</files>
  <read_first>
    - .planning/phases/01-target-extraction-contract/01-06-DIAGNOSIS.md (the implicated mechanism and the pre-registered fix-selection criterion — this task MUST implement the fix that criterion selects)
    - src/targets.py (DETREND_POLY_ORDER application at extract_track_targets; the constants block; halfmax_edges baseline/peak percentile logic to mirror for the exclusion rule)
    - src/nsf_fmrg_data.py (robust_plane_detrend: strided sampling, valid mask, iterative lstsq trimming — where fit_mask must be applied)
    - tests/test_nsf_fmrg_data.py (existing detrend regression style)
    - .planning/phases/01-target-extraction-contract/01-VERIFICATION.md (Gap 2)
  </read_first>
  <behavior>
    - A synthetic height map = smooth low-order background + an elevated rectangular bead corridor: the current unmasked order-4 fit follows the bead (post-detrend bead height is strongly suppressed), whereas the masked fit recovers the background and preserves the bead's true height/width.
    - robust_plane_detrend with a fit_mask excludes the False pixels from the least-squares fit but still subtracts the fitted surface from all pixels (output shape and coverage unchanged).
    - The exclusion rule uses one shared parameterization: applied to two synthetic tracks whose beads differ in absolute height, the SAME BEAD_MASK_HEIGHT_FRACTION excludes each bead corridor (rule is relative, not an absolute height threshold).
  </behavior>
  <action>
    Implement the ONE uniform correction chosen by the 01-06 pre-registered criterion. The endorsed leading remedy (implement this unless the diagnosis explicitly selects the continuity or order fallback):

    Add a fit_mask keyword to robust_plane_detrend in src/nsf_fmrg_data.py (default None, preserving every existing caller including the affine order=1 path and the notebooks). When fit_mask is provided, stride it the same way as Z_mm and AND it into the existing `valid` array before the iterative lstsq, so flagged bead pixels never contribute to the fitted coefficients; keep subtracting the fitted surface from the full-resolution Z_mm exactly as today. Preserve the existing <100-sample degenerate fallback and order validation.

    Add BEAD_MASK_HEIGHT_FRACTION to the src/targets.py constants block (SCREAMING_SNAKE_CASE, no units suffix needed for a fraction; add a one-line comment noting it is a baseline-relative exclusion fraction fixed from the residual/physical criterion, not from the ordering outcome). Add bead_exclusion_mask(Z_mm, ...) that computes, with one shared rule for all tracks, a boolean (y, x) fit-mask marking substrate pixels True and elevated bead-corridor pixels False, using a baseline-relative height threshold (e.g. per-column or robust-global low/high percentiles mirroring BASELINE_PCT/PEAK_PCT, excluding pixels above baseline + BEAD_MASK_HEIGHT_FRACTION * (peak - baseline)). Grounding for the exact percentile/fraction choice is Claude's discretion but MUST be justified by the residual/physical criterion and MUST NOT be chosen by inspecting the resulting width ordering. Wire extract_track_targets to compute this mask from the raw data['Z_mm'] and pass it as fit_mask into the DETREND_POLY_ORDER=4 robust_plane_detrend call, applied identically to all 4 tracks.

    If (and only if) 01-06-DIAGNOSIS.md instead implicates continuity tracking or detrend order, implement that uniform fix in place of the bead mask (uniformly across all tracks), introduce the parameter it requires, and name it in the SUMMARY. Do not implement more than one fix.

    Add the RED-then-GREEN regressions described in <behavior>: test_robust_plane_detrend_fit_mask_excludes_bead_from_fit in tests/test_nsf_fmrg_data.py and test_bead_mask_rule_is_track_independent in tests/test_targets.py (both appended to the module __main__ runner lists). No per-track conditionals anywhere in the fix.
  </action>
  <verify>
    <automated>.venv/bin/python tests/test_nsf_fmrg_data.py && .venv/bin/python tests/test_targets.py && grep -q "fit_mask" src/nsf_fmrg_data.py && grep -Eq "BEAD_MASK_HEIGHT_FRACTION|def bead_exclusion_mask" src/targets.py && test -z "$(grep -nE 'track_id *== *(8|10|14|21)|if .*track.*in *\(' src/targets.py)"</automated>
  </verify>
  <acceptance_criteria>
    - robust_plane_detrend accepts a fit_mask keyword that excludes flagged pixels from the fit while still subtracting the fitted surface from all pixels; default None preserves existing behavior (existing detrend tests still pass).
    - The synthetic bead regression proves the masked fit preserves the bead's height where the unmasked order-4 fit suppresses it.
    - The masking rule is track-independent: one BEAD_MASK_HEIGHT_FRACTION excludes bead corridors of differing absolute height (proven by test_bead_mask_rule_is_track_independent).
    - No per-track branch or per-track-tuned constant exists in src/targets.py (grep finds no `track_id ==` numeric branch); `.venv/bin/python tests/test_targets.py` still reports all PASS.
  </acceptance_criteria>
  <done>One uniform, track-independent correction chosen by the pre-registered criterion is implemented and covered by named synthetic regressions; no per-track tuning was introduced.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Complete extraction_params() and make provenance change-sensitive</name>
  <files>src/targets.py, tests/test_targets.py</files>
  <read_first>
    - src/targets.py (extraction_params() at lines 32-47; the constants MAX_TRACKING_GAP_COLUMNS at line 16 and the new fix constant from Task 1)
    - tests/test_targets.py (test_extraction_params_provenance at lines 323-340; the require() helper and __main__ runner list)
    - .planning/phases/01-target-extraction-contract/01-VERIFICATION.md (the failing "Provenance sensitivity" spot-check: changing MAX_TRACKING_GAP_COLUMNS 10->999 left the digest unchanged)
    - scripts/check_targets.py (line 119 asserts persisted_params == extraction_params(); the SHA-256 digest computation at lines 130-133)
  </read_first>
  <behavior>
    - extraction_params() includes 'MAX_TRACKING_GAP_COLUMNS' and the new fix parameter key.
    - For each newly added key, mutating the underlying module constant and recomputing hashlib.sha256(json.dumps(extraction_params(), sort_keys=True)) yields a digest different from the unmutated baseline.
    - test_extraction_params_provenance asserts the exact new complete key set (previous 13 keys plus the 2 new keys) and their values.
  </behavior>
  <action>
    Add 'MAX_TRACKING_GAP_COLUMNS' and the new fix parameter (BEAD_MASK_HEIGHT_FRACTION, or the parameter the chosen fix required) as entries in the dict returned by extraction_params() in src/targets.py, keeping the existing keys and their order style. Update tests/test_targets.py test_extraction_params_provenance to the new complete expected dict (fix the stale "12-value"/"13-value" wording in its assertion message to the new count).

    Add test_provenance_includes_tracking_gap_and_fix_param asserting both new keys are present in extraction_params(). Add test_provenance_digest_is_change_sensitive that, for MAX_TRACKING_GAP_COLUMNS and for the new fix parameter, monkeypatches the corresponding module attribute on the targets module to a different value, recomputes the SHA-256 over json.dumps(extraction_params(), sort_keys=True), asserts it differs from the baseline digest, and restores the original value in a finally block. Append both new tests to the __main__ runner list. Do not change check_targets.py; its existing persisted==code and manifest-digest assertions will hold once plan 01-08 regenerates the JSON and manifest.
  </action>
  <verify>
    <automated>.venv/bin/python tests/test_targets.py && .venv/bin/python -c "import sys; sys.path.insert(0,'src'); import targets; p=targets.extraction_params(); assert 'MAX_TRACKING_GAP_COLUMNS' in p, p; import json,hashlib; base=hashlib.sha256(json.dumps(p,sort_keys=True).encode()).hexdigest(); targets.MAX_TRACKING_GAP_COLUMNS=999; assert hashlib.sha256(json.dumps(targets.extraction_params(),sort_keys=True).encode()).hexdigest()!=base; print('CHANGE-SENSITIVE OK')"</automated>
  </verify>
  <acceptance_criteria>
    - extraction_params() returns a dict containing key 'MAX_TRACKING_GAP_COLUMNS' and the new fix parameter key.
    - test_provenance_digest_is_change_sensitive exits 0: mutating MAX_TRACKING_GAP_COLUMNS (and the new fix param) changes the SHA-256 digest.
    - test_extraction_params_provenance passes against the new complete key set with corrected wording.
    - `.venv/bin/python tests/test_targets.py` reports all PASS (including the new provenance tests).
  </acceptance_criteria>
  <done>The complete current extraction contract is machine-readable and its provenance digest is demonstrably change-sensitive.</done>
</task>

<task type="auto">
  <name>Task 3: Record canonical Amendment A4 (continuity/stale-history rule + Gap-2 fix)</name>
  <files>.planning/phases/01-target-extraction-contract/01-CONTEXT.md</files>
  <read_first>
    - .planning/phases/01-target-extraction-contract/01-CONTEXT.md (Amendment A3 block at lines 40-44 — match its style and placement inside the decisions section)
    - .planning/phases/01-target-extraction-contract/01-05-SUMMARY.md (the continuity tracking, MAX_TRACKING_GAP_COLUMNS=10 expiry, and stale-history reset that need canonicalizing)
    - .planning/phases/01-target-extraction-contract/01-06-DIAGNOSIS.md (the pre-registered criterion and implicated mechanism the fix addresses)
    - src/targets.py (final constant values to cite: MAX_TRACKING_GAP_COLUMNS, the new fix parameter)
  </read_first>
  <action>
    Append Amendment A4 to the decisions section of 01-CONTEXT.md, immediately after Amendment A3, matching A3's heading style and prose discipline. A4 MUST document two things as one canonical contract addition. First, the plan-01-05 continuity/stale-history tracking rule: halfmax_edges selects the non-clipped candidate nearest the previous successful boundary center while history is fresh, falls back to largest-run selection with no history, and expires previous_center after MAX_TRACKING_GAP_COLUMNS consecutive invalid columns — applied identically to all 4 tracks. Second, the Gap-2 methodological fix from this plan: state the chosen uniform correction (the bead-region masking of the detrend surface fit, or the fallback mechanism actually implemented), name its new parameter and that it is fixed from the plan-06 residual/physical pre-registered criterion rather than the ordering outcome, and reaffirm the no-outcome-driven-per-track-tuning prohibition. Note that A4 supersedes nothing in A3 but completes the record of behavior-changing parameters (MAX_TRACKING_GAP_COLUMNS and the new fix parameter) now carried in extraction_params() provenance.
  </action>
  <verify>
    <automated>grep -q "Amendment A4" .planning/phases/01-target-extraction-contract/01-CONTEXT.md && grep -q "MAX_TRACKING_GAP_COLUMNS" .planning/phases/01-target-extraction-contract/01-CONTEXT.md && grep -Eqi "bead|continuity|stale" .planning/phases/01-target-extraction-contract/01-CONTEXT.md</automated>
  </verify>
  <acceptance_criteria>
    - 01-CONTEXT.md contains an Amendment A4 heading in the decisions section, styled like Amendment A3.
    - A4 documents the continuity/stale-history rule (naming MAX_TRACKING_GAP_COLUMNS) and the Gap-2 uniform fix (naming its parameter), and reaffirms the no-outcome-driven-per-track-tuning prohibition.
  </acceptance_criteria>
  <done>The continuity/stale-history rule and the Gap-2 methodological fix are canonicalized as Amendment A4, so the current contract is fully documented.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| filesystem read of data/raw/ -> in-memory arrays | Raw height maps are read by tests/synthetic fixtures only; production regeneration is deferred to plan 01-08. |
| source edits -> extraction contract semantics | Changing detrend/extraction and provenance code must keep one shared parameterization and must not silently alter the persisted contract. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-07-01 | Tampering | data/raw/ | high | mitigate | This plan edits only source/tests/CONTEXT and runs synthetic-fixture tests; no code path writes under data/raw/. |
| T-01-07-02 | Repudiation | provenance completeness | high | mitigate | Add MAX_TRACKING_GAP_COLUMNS + fix param to extraction_params() and prove the digest is change-sensitive, so silent parameter drift is detectable. |
| T-01-07-03 | Tampering | no-tuning integrity | medium | mitigate | The fix parameter is fixed from the plan-06 pre-registered residual/physical criterion; Amendment A4 records the prohibition against outcome-driven per-track tuning. |
</threat_model>

<verification>
- `.venv/bin/python tests/test_targets.py` and `.venv/bin/python tests/test_nsf_fmrg_data.py` both report all PASS (including the new fix, provenance, and change-sensitivity regressions), normally and under `python -O`.
- extraction_params() contains MAX_TRACKING_GAP_COLUMNS and the new fix parameter; mutating either changes the SHA-256 digest.
- src/targets.py contains no per-track numeric branch (one shared parameterization).
- 01-CONTEXT.md contains Amendment A4.
</verification>

<success_criteria>
- One uniform, track-independent correction is implemented per the plan-06 pre-registered criterion, with no per-track tuning.
- The complete current extraction contract (including MAX_TRACKING_GAP_COLUMNS and the new fix parameter) is in extraction_params() and its provenance digest is change-sensitive.
- Amendment A4 canonicalizes the continuity/stale-history rule and the Gap-2 fix.
</success_criteria>

<output>
Create `.planning/phases/01-target-extraction-contract/01-07-SUMMARY.md` when done.
</output>
