---
phase: 01-target-extraction-contract
plan: 18
type: execute
wave: 17
depends_on: ["01-17"]
files_modified:
  - .planning/phases/01-target-extraction-contract/COVERAGE.md
  - .planning/phases/01-target-extraction-contract/01-UAT.md
  - scripts/diagnose_track10_coverage.py
  - .planning/phases/01-target-extraction-contract/01-18-SUMMARY.md
autonomous: true
gap_closure: true
requirements: [TARGET-01, TARGET-02]

must_haves:
  truths:
    - "Running the phase's api-coverage seal gate (`gsd-tools check api-coverage-verify-pre 01`) reports `block: false` — the phase can be sealed once a human grants visual sign-off, instead of being blocked a second time by a detector signal that is a prior plan's own sentence saying this phase integrates no external API."
    - "`COVERAGE.md` is a reasoned declaration, not a fabricated coverage matrix: it carries one line of the exact declared form with a mandatory one-line reason naming what this phase touches instead, and it contains no invented API-surface table."
    - "`01-UAT.md`'s G-01-6 entry carries a dated annotation recording what Amendment A8 changed and that plan 01-17 regenerated the sign-off document — while its `status:` field is provably unchanged, so no verdict is flipped ahead of the human round."
    - "The phase gap ledger's shape is provably unchanged by this plan: the same six gap entries, the same five resolved, the same one failed. The annotation adds information without adding, removing, or re-deciding a gap."
    - "`.planning/REQUIREMENTS.md`'s TARGET-02 checkbox is still unticked after this plan — the automated re-tick that previously had to be manually reverted does not recur."
    - "`scripts/diagnose_track10_coverage.py` carries an in-file disclaimer stating that its hand-duplicated `classify_column` mirrors the pre-Amendment-A8 `halfmax_edges` behavior, has not been updated for Amendment A8, and that its committed `processed_data/diagnostics/track10_coverage_diagnosis.csv` is a historical baseline that must never be read as current production behavior — so the next reader of that CSV is not misled."
    - "The disclaimer is comment-only: the diff for `scripts/diagnose_track10_coverage.py` contains no added or removed non-comment, non-blank line, so the script's diagnostic behavior is provably identical before and after and no diagnostic output is regenerated."
    - "No extraction parameter, no locked constant, no test, no target artifact, and no QA figure is touched: `check_targets.py` still exits 0 and git reports no change under `src/`, `tests/`, `processed_data/`, or `data/raw/`."
    - "[SPEC edge/boundary R1] A per-column peak-baseline separation exactly at MIN_PEAK_BASELINE_SEPARATION_MM is valid (only below invalidates, D-04), and exactly 10 consecutive NaNs is still interpolated (only exceeding 10 invalidates, D-06) — inherited invariant, unchanged by this plan."
    - "[SPEC edge/adjacency R1] SG smoothing is allowed to blend valid points from both sides of a masked gap (standard savgol_filter NaN-skip behavior) — inherited invariant, unchanged by this plan."
    - "[SPEC edge/empty R1] A track with zero valid x-positions after masking raises a hard error rather than silently producing an all-NaN artifact — inherited invariant, unchanged by this plan."
    - "[SPEC edge/precision R1] Percentile calculation uses numpy's default linear-interpolation convention, and a crossed-boundary case (y_upper < y_lower, negative width) marks that x-column invalid rather than reporting a negative or clipped-to-zero width — inherited invariant, unchanged by this plan."
    - "[SPEC edge/boundary R2] Crop-edge behavior at 20mm and 100mm is D-12's SG edge/interp boundary mode, and remains visually inspectable in the four track_*_width.png figures the sign-off document points the reviewer at."
    - "[SPEC edge/adjacency R2] Invalid x-positions keep their grid slot with width=NaN and mask=False (D-07) — no ragged or dropped positions at valid/invalid boundaries — inherited invariant, unchanged by this plan."
    - "[SPEC edge/empty R2] Same as R1: an all-invalid track raises a hard error rather than persisting an all-NaN artifact — inherited invariant, unchanged by this plan."
    - "[SPEC edge/precision R2] Persisted target arrays remain float64 dtype — inherited invariant, unchanged by this plan."
  artifacts:
    - ".planning/phases/01-target-extraction-contract/COVERAGE.md"
    - ".planning/phases/01-target-extraction-contract/01-UAT.md"
    - "scripts/diagnose_track10_coverage.py"
    - ".planning/phases/01-target-extraction-contract/01-18-SUMMARY.md"
  key_links:
    - "api-coverage detector signal -> COVERAGE.md reasoned declaration -> `gsd-tools check api-coverage-verify-pre 01` reporting `block: false`: the declaration must actually satisfy the gate, not merely exist. The task's verify runs the real gate."
    - "01-16-ORDERING-OUTCOME.md's measured A8 outcome + 01-17's regenerated sign-off document -> 01-UAT.md's G-01-6 annotation: the ledger records what happened without recording a verdict that has not been reached."
    - "01-UAT.md G-01-6 `status:` field -> the human `/gsd-verify-work 1` round: only that round may change this field. This plan asserts the field is unchanged as a pass condition, inverting the usual direction of a gate."
    - "src/targets.py's Amendment A8 halfmax_edges -> scripts/diagnose_track10_coverage.py's classify_column reimplementation: the drift is not repaired (that would be the third re-sync of hand-duplicated logic in one phase); it is disclosed in-file so the committed diagnostic CSV cannot be mistaken for a live characterization."
  prohibitions:
    - statement: "[SPEC] Extraction contract constants (noise-floor threshold, gap limit, percentile split, and every A3-A8 amendment constant) must NOT be adjusted based on whether the resulting width ordering or QA plots look correct. This plan changes no constant and runs no extraction."
      status: resolved
      verification: judgment
    - statement: "[SPEC] Extraction and persistence code must NOT write to, delete, or modify any file under data/raw/. This plan never opens anything under data/raw/."
      status: resolved
      verification: test
    - statement: "G-01-6's `status:` field in 01-UAT.md must NOT be changed by any task in this plan, and no gap entry may be added, removed, or re-decided. Only a human `/gsd-verify-work 1` round may move that verdict."
      status: resolved
      verification: test
    - statement: "`.planning/REQUIREMENTS.md`'s TARGET-02 checkbox must NOT be ticked and its traceability row must NOT be flipped to complete. An automated tooling step already did this once and had to be manually reverted."
      status: resolved
      verification: test
    - statement: "`scripts/diagnose_track10_coverage.py`'s executable behavior must NOT change. The edit is comment-only, and `processed_data/diagnostics/track10_coverage_diagnosis.csv` must NOT be regenerated."
      status: resolved
      verification: test
    - statement: "COVERAGE.md must NOT contain a fabricated API-surface matrix. The detector signal is a known false positive — the honest response is a reasoned declaration with a mandatory reason, never an invented enumeration of APIs this phase does not use."
      status: resolved
      verification: test
    - statement: "HONEST-OUTCOME GUARD: no task may reopen the 10-vs-14 width-ordering FLAG, tune a threshold, or start a new investigation cycle."
      status: resolved
      verification: judgment
---

<objective>
Close the three non-narrative items blocking a clean phase seal, without touching a single verdict that belongs to a human.

Three items sit between this phase and a clean seal once plan 01-17 has regenerated the sign-off document: (1) `COVERAGE.md` is absent, and the api-coverage seal gate currently reports `block: true` on a detector signal that is literally a prior plan's own sentence stating this phase integrates no external API — a false positive that will re-fire at seal time and block the phase even after visual sign-off is granted; (2) `01-UAT.md`'s G-01-6 entry records nothing about Amendment A8 having landed, so the phase's own gap ledger reads as though nothing happened since the gap was filed; (3) `scripts/diagnose_track10_coverage.py`'s hand-duplicated `classify_column` drifted out of sync with production `halfmax_edges` for the second time in one day (`01-REVIEW.md` WR-01), so its committed diagnostic CSV reports rejection counts that match neither the current production `valid_mask` sums nor the script's own current logic.

Purpose: none of these three changes any conclusion. Each removes an obstacle or a source of future misreading, and each is deliberately bounded so it cannot become a covert verdict change. The G-01-6 task's pass condition is that G-01-6's `status:` field is UNCHANGED — the gate runs backwards on purpose.
Output: a reasoned no-external-API declaration that actually satisfies the seal gate; an annotated but un-flipped G-01-6 ledger entry; and an in-file historical-baseline disclaimer on the drifted diagnostic.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-target-extraction-contract/01-VERIFICATION.md
@.planning/phases/01-target-extraction-contract/01-REVIEW.md
@.planning/phases/01-target-extraction-contract/01-SPEC.md
@.planning/phases/01-target-extraction-contract/01-16-ORDERING-OUTCOME.md
</context>

<artifacts_produced>
Artifacts this phase produces — every file path this plan creates or modifies:

- **Created:** `.planning/phases/01-target-extraction-contract/COVERAGE.md` (reasoned no-external-API declaration)
- **Modified:** `.planning/phases/01-target-extraction-contract/01-UAT.md` (one added annotation key under the G-01-6 gap entry; no existing key altered)
- **Modified:** `scripts/diagnose_track10_coverage.py` (comment-only historical-baseline disclaimer above `classify_column`; no executable line changed)
- **Created:** `.planning/phases/01-target-extraction-contract/01-18-SUMMARY.md` (execution summary)

No new code symbols, constants, functions, CLI flags, tests, or data artifacts. `processed_data/diagnostics/track10_coverage_diagnosis.csv` is explicitly NOT regenerated.
</artifacts_produced>

<assumption_delta_decision>
**Capability signal:** `assumption-delta` fired (advisory, non-blocking) on `pluralization`/"fallback" and `chosen`/"configurable".

**Decision: `no-change`.**

**Rationale:** Both are incidental matches in historical planning prose — "fallback" from the earlier 10-vs-14 option-(a) discussion, "configurable" from Amendment A3's configurable-order detrend (locked in plan 01-04, unchanged since). This plan writes one declaration file, adds one annotation key, and adds one comment block. It introduces no entity, no cardinality change, no new configurability, and no persisted-format change. No architectural migration is invented or implied.
</assumption_delta_decision>

<assumptions>
Explicit assumptions and deltas, flagged rather than left as unstated premises.

1. **The api-coverage detection is a confirmed false positive, and the honest response is a declaration, not a matrix.** The deterministic detector matched `integrates` + `api` on a prior plan's own sentence: "This phase integrates no external API…". Per the capability contract, a fabricated coverage matrix would be worse than the absent file. The declaration form with a mandatory reason is the sanctioned response.

2. **The seal gate is satisfied by running it, not by assuming file existence — and the failure mode to plan for is FORM, not PATH.** Task 1 writes `COVERAGE.md` into the phase directory and then runs the real gate. Phase-directory resolution is already known-good: the gate was invoked during planning for phase `01` and returned a phase-scoped result with detected signals, so relocating the file is not a remedy for a failing gate. The failure that will actually occur is a malformed declaration, and the sharpest edge is a length cap: `gsd-core/bin/lib/api-coverage.cjs` sets `REASON_MAX_LEN = 200` and pushes `declaration reason exceeds 200 chars` into `errors[]`, yielding `valid: false` and `block: true` for a declaration that otherwise looks perfectly well-formed to a human reader. Task 1's `<verify>` therefore enforces the 200-character ceiling itself, before invoking the gate, so this failure surfaces with a readable message. On any `block: true` with `coverage_present: true`, the executor reads `errors[]` and fixes the declaration's form.

3. **A pre-existing untracked file lives in the working tree, and the freeze gates are written to tolerate it.** `processed_data/diagnostics/width_regression_sweep.csv` is untracked and predates this planning session (diagnostic output from `scripts/diagnose_width_regression.py`, first observed by plan 01-15 and deliberately left undisposed — it is not this plan's to delete or commit, and no task here does either). A bare `git status --porcelain processed_data` reports it and would fail every gate in this plan before the gate could evaluate its real subject. All freeze assertions therefore use `git status --porcelain --untracked-files=no <paths>`, which expresses the invariant these gates actually encode: *no committed production artifact was modified*. That remains strict — a real edit to a tracked file under `src/`, `tests/`, `scripts/`, `processed_data/`, or `data/raw/` still fails. This matters most in Task 3, whose entire safety argument is that `track10_coverage_diagnosis.csv` was not regenerated: that CSV **is** tracked, so `--untracked-files=no` does not weaken the check that protects it. The flag must not be replaced with a `grep`-filtered pipeline, which could mask exactly such a modification.

4. **WR-01 disposition: disclose, do not re-sync.** `01-REVIEW.md` offers two fixes for the drifted diagnostic: (a) re-sync `classify_column` (ideally by importing `targets.halfmax_edges` instead of hand-duplicating it) and regenerate the CSV, or (b) add an explicit historical-baseline disclaimer matching the pattern `scripts/diagnose_width_regression.py` already uses. This plan takes (b), deliberately. Rationale: option (a) is a behavioral change to a script plus a regeneration of a committed artifact under `processed_data/`, which every prohibition in this phase freezes and which would be the third re-sync of the same hand-duplicated logic within this phase; the drift is diagnostic-only and does not affect the shipped pipeline. The real fix (importing the production function rather than duplicating it) is a design change that belongs to a future phase, not to a doc-hygiene round five days from the submission deadline. Recorded here as **accepted, disclosed debt with a durable in-repo marker** — not silently dropped.

5. **`01-REVIEW.md` IN-01 (`diagnose_width_regression.py` calls `halfmax_edges` without `previous_length_mm`) is accepted, undisclosed debt this round.** Unlike WR-01, that script calls the real production function, so it cannot produce *wrong* output — only a sweep of a variant with the Mechanism B gate switched off. It is Info-severity, was not raised by `01-VERIFICATION.md` as an unresolved anti-pattern, and is out of this plan's scope. Flagged here so it is not mistaken for an oversight.

6. **The G-01-6 gate runs backwards.** For every other task in this phase, a `<verify>` asserts something changed. Here the pass condition includes that G-01-6's `status:` field did NOT change and that the gap ledger's resolved/failed counts are identical to before. This is deliberate: the single largest risk in this task is an executor helpfully flipping a verdict that belongs to a human.

7. **SPEC dismissed edges surfaced, not dropped.** `01-SPEC.md`'s Edge Coverage reports 8/10 covered, 0 unresolved, 2 dismissed — both `ordering` edges (R1 and R2), dismissed because the output x-grid order is fixed by construction (D-07) and no data-dependent sort exists in the extraction algorithm. There are no `backstop` edges in this SPEC.

8. **Schema-push detection.** No ORM exists in this repository. This plan introduces no schema, no migration, and no persisted-format change.
</assumptions>

<tasks>

<task type="tracer">
  <name>Task 1: Unblock the phase seal end to end — reasoned no-external-API declaration, verified against the real gate</name>
  <files>.planning/phases/01-target-extraction-contract/COVERAGE.md</files>
  <read_first>
    - `.planning/phases/01-target-extraction-contract/01-SPEC.md` — the `## Boundaries` section (what this phase is in scope for) and the `## Constraints` section, so the declaration's reason names what the phase actually touches rather than a generic disclaimer.
    - `.planning/phases/01-target-extraction-contract/01-15-PLAN.md` — its `<assumptions>` item 6, which is the exact sentence the deterministic detector matched on. Read it so the declaration's reason is written independently rather than echoing the matched text.
    - `src/nsf_fmrg_data.py` and `src/targets.py` — confirm by inspection that neither imports an HTTP client, SDK, or network module, so the declaration is a verified statement rather than an assumed one.
    - `requirements.txt` — confirm no networked client library is declared as a dependency.
  </read_first>
  <precondition>The api-coverage gate is runnable in this environment via the `gsd-tools.cjs` shim: `node ${CLAUDE_CONFIG_DIR:-$HOME/.claude}/gsd-core/bin/gsd-tools.cjs check api-coverage-verify-pre 01` produces JSON containing a `block` field. Resolve the shim by FILE PATH, not by `command -v gsd-tools` — on this machine a `gsd-tools` binary on `PATH` is the gsd-sdk bridge, which routes `check` to a stale bundled `gsd-tools.cjs` that fails with `Unknown command: check`. If no `gsd-tools.cjs` can be found on disk, HALT and record it — the task's whole point is satisfying that gate, and file existence alone does not prove it.</precondition>
  <reversibility rating="reversible">Creating one new markdown file inside `.planning/`, fully recoverable via git.</reversibility>
  <action>
    Before writing anything, run `grep -rniE "requests|httpx|urllib|aiohttp|boto3|openai|socket" src/ scripts/ requirements.txt` and confirm the extraction pipeline opens no network client. The declaration must be a verified statement, not an assumption.

    Create `.planning/phases/01-target-extraction-contract/COVERAGE.md`. Its body must contain exactly one declaration line, beginning with the literal text `No external API integration: ` and continuing with a mandatory one-line reason naming what this phase touches instead. Use this reason, which is measured at 195 characters and clears the gate's limits:

    `this phase reads local Wyko .ASC height maps and writes only local artifacts under processed_data/targets/, with no network call, SDK, or external service anywhere in the extraction pipeline.`

    The reason has a HARD LENGTH WINDOW: it must be at least 40 and **at most 200 characters** after the `No external API integration: ` prefix. The 200-character cap is enforced by the gate itself (`REASON_MAX_LEN` in `gsd-core/bin/lib/api-coverage.cjs`), which pushes `declaration reason exceeds 200 chars` into its `errors[]` array and returns `block: true`. An over-long but otherwise well-formed reason therefore fails the gate silently as far as the file's appearance goes — do not expand the reason with a format enumeration or extra qualifiers. The line must end with a period. The reason is mandatory: a bare declaration with no reason does not parse at all.

    Above that line, add a short heading and one or two sentences of provenance explaining WHY this declaration exists rather than a coverage matrix: the deterministic api-coverage detector matched the verb `integrates` against the noun `api` on a prior plan's own sentence stating that this phase integrates no external API, which is a self-referential false positive. State that per the capability contract a coverage matrix must not be fabricated for a surface that does not exist, and that this reasoned declaration is the sanctioned alternative. Cite `01-REVIEW.md` and `01-VERIFICATION.md` only if they are actually relevant; do not pad.

    Do NOT write a markdown table, an enumerated API surface, an endpoint list, or any INTEGRATE/OPT-OUT matrix. There is no external API to enumerate and inventing one would be worse than the missing file.

    Then run the real gate through the `gsd-tools.cjs` shim, resolved by file path in this order — `gsd-core/bin/gsd-tools.cjs`, then `.claude/gsd-core/bin/gsd-tools.cjs`, then `${CLAUDE_CONFIG_DIR:-$HOME/.claude}/gsd-core/bin/gsd-tools.cjs` — invoked as `node <resolved-path> check api-coverage-verify-pre 01`. It must report `block` as false. Do NOT resolve the tool with `command -v gsd-tools`: a `gsd-tools` binary on `PATH` here is the gsd-sdk bridge, whose `check` path falls back to a stale bundled shim and errors with `Unknown command: check`, which would surface as a spurious gate failure.

    If it still reports `block` as true with the file in place, read the gate's own `errors[]` array in its JSON output and fix the declaration's FORM — the failure will be a malformed or over-long declaration, not a misplaced file. The phase-directory resolution is already known-good: the gate was run during planning and returned a phase-scoped result with detected signals for phase `01`, so `COVERAGE.md` belongs at the path named above and relocating it is not a remedy. The realistic failure modes, in order of likelihood: the reason exceeds 200 characters; the reason is missing entirely (a bare declaration does not parse); or a coverage table was added alongside the declaration, which the gate rejects as contradictory. Fix the form, re-run until `block` is false, and record any correction as a deviation in `01-18-SUMMARY.md`. Do not declare this task done on file existence alone.

    Touch no other file.
  </action>
  <verify>
    <automated>C=.planning/phases/01-target-extraction-contract/COVERAGE.md; test -f "$C" || { echo "COVERAGE.md MISSING"; exit 1; }; L=$(grep -m1 -E '^No external API integration: .+' "$C") || { echo "DECLARATION LINE MISSING OR MALFORMED"; exit 1; }; R=${L#No external API integration: }; test "${#R}" -ge 40 || { echo "REASON TOO SHORT TO BE A REASON"; exit 1; }; test "${#R}" -le 200 || { echo "REASON IS ${#R} CHARS, GATE CAP IS 200"; exit 1; }; case "$R" in *.) ;; *) echo "REASON MUST END WITH A PERIOD"; exit 1;; esac; test "$(grep -c '^|' "$C")" -eq 0 || { echo "FABRICATED COVERAGE MATRIX PRESENT"; exit 1; }; grep -qF 'processed_data/targets' "$C" || { echo "REASON DOES NOT NAME WHAT THE PHASE TOUCHES INSTEAD"; exit 1; }; GT=""; for c in "gsd-core/bin/gsd-tools.cjs" ".claude/gsd-core/bin/gsd-tools.cjs" "${CLAUDE_CONFIG_DIR:-$HOME/.claude}/gsd-core/bin/gsd-tools.cjs"; do [ -f "$c" ] && { GT="node $c"; break; }; done; [ -n "$GT" ] || { echo "gsd-tools.cjs NOT FOUND ON DISK"; exit 1; }; OUT=$($GT check api-coverage-verify-pre 01 2>&1) || true; echo "$OUT"; echo "$OUT" | grep -q '"block": *false' || { echo "SEAL GATE STILL BLOCKS"; exit 1; }; test -z "$(git status --porcelain --untracked-files=no src tests scripts processed_data data/raw)" || exit 1; echo "SEAL GATE UNBLOCKED"</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/01-target-extraction-contract/COVERAGE.md` exists and contains a line matching `^No external API integration: .+` whose reason portion is between 40 and 200 characters inclusive and ends with a period. Both bounds are enforced in `<verify>`: the floor makes the mandatory-reason requirement real, and the ceiling matches the gate's own `REASON_MAX_LEN`, so an over-long reason fails here with a readable message instead of surfacing as an opaque `SEAL GATE STILL BLOCKS`.
    - The declaration's reason names `processed_data/targets` — it states what the phase touches instead, rather than being a bare disclaimer.
    - `grep -c '^|' COVERAGE.md` returns 0 — no fabricated coverage matrix or API-surface table was invented for a surface that does not exist.
    - `node <resolved gsd-tools.cjs> check api-coverage-verify-pre 01` reports `block` as false — the real seal-time gate is satisfied, asserted by running it, not by assuming the file's presence is sufficient. The shim is resolved by file path; `command -v gsd-tools` must not be used, since the `gsd-tools` on `PATH` is the gsd-sdk bridge and errors with `Unknown command: check`.
    - `grep -rniE "requests|httpx|urllib|aiohttp|boto3|openai|socket" src/ scripts/ requirements.txt` returns no network-client usage, so the declaration is verified rather than assumed.
    - `git status --porcelain --untracked-files=no src tests scripts processed_data data/raw` is empty.
  </acceptance_criteria>
  <done>The api-coverage seal gate reports `block: false` for phase 01 because a reasoned, verified declaration exists — not because a coverage matrix was invented for an API surface this phase does not have.</done>
</task>

<task type="auto">
  <name>Task 2: Annotate G-01-6 with what Amendment A8 changed — without touching its verdict</name>
  <files>.planning/phases/01-target-extraction-contract/01-UAT.md</files>
  <read_first>
    - `.planning/phases/01-target-extraction-contract/01-UAT.md` — the whole `## Gaps` section. Note the six `- gap_id:` entries, the key order used inside each entry (`gap_id`, `truth`, `status`, `reason`, `severity`, `test`, `root_cause`, `artifacts`, `missing`, `debug_session`), the two-space indentation of the scalar keys, and that G-01-6 is the last entry in the file.
    - `.planning/phases/01-target-extraction-contract/01-16-SUMMARY.md` — the `## Accomplishments` section and the coverage entry `D3` with `human_judgment: true`, which is the authoritative statement that visual sign-off on the regenerated figures was deliberately deferred to a human round.
    - `.planning/phases/01-target-extraction-contract/01-16-ORDERING-OUTCOME.md` — the "## Human verification round context" section, which states in one place what closed, what measurably improved, what got marginally worse, and what remains open.
    - `.planning/phases/01-target-extraction-contract/01-VERIFICATION.md` — the `re_verification.gaps_closed` entry describing G-01-6's mechanism-level closure and the `gaps_remaining` entry stating why the gap is nonetheless still open.
    - `.planning/REQUIREMENTS.md` — the TARGET-02 checkbox and the three dated correction notes beneath the Traceability table, especially the 2026-07-22 plan-01-16 note recording that an automated step re-ticked TARGET-02 and had to be reverted.
  </read_first>
  <precondition>`01-UAT.md`'s G-01-6 entry still records its current unresolved verdict and no human sign-off round has been recorded since this plan was written. If a sign-off record has appeared in the interim, HALT rather than annotating over it.</precondition>
  <reversibility rating="reversible">One added scalar key in a planning document, fully recoverable via git. No existing key, verdict, or count is altered.</reversibility>
  <action>
    Edit `.planning/phases/01-target-extraction-contract/01-UAT.md` with scoped replacements only — do not rewrite the file and do not use a whole-file write.

    Add exactly one new key, `post_fix_note:`, to the G-01-6 entry, at the same two-space indentation as its sibling scalar keys and positioned immediately after `debug_session:` (the entry's last key, and the file's last line). Its value is a single-line quoted scalar recording, in this order:
    - that plan 01-16 landed Amendment A8, closing Mechanism A (clip-exclusion reordered before run merging) and Mechanism B (the history-based far-AND-small plausibility gate), citing `01-16-ORDERING-OUTCOME.md`;
    - that valid-bin coverage rose on every track and contiguous-run fragmentation improved on three of four tracks while getting marginally worse on one, and that both crop-edge symptoms this gap's own `reason` field named are resolved — sourced from `01-16-ORDERING-OUTCOME.md`, not restated from memory;
    - that Mechanism C's residual far-edge instability was deliberately NOT addressed and remains the already-deferred DP/Viterbi joint-tracker gap;
    - that plan 01-17 regenerated `01-SIGNOFF-REQUEST.md` against the live Amendment A8 run so a fresh visual round can be held against the current figures;
    - and, explicitly, that this note is NOT a sign-off and this gap keeps its current unresolved verdict until a human records a `/gsd-verify-work 1` round.

    Do NOT change G-01-6's `status:` field. Do NOT change any other gap's `status:` field. Do NOT add, remove, renumber, or re-order any gap entry. Do NOT edit `truth:`, `reason:`, `severity:`, `test:`, `root_cause:`, `artifacts:`, `missing:`, or `debug_session:` on any entry. Do NOT edit the file's frontmatter.

    Do NOT touch `.planning/REQUIREMENTS.md` in this task or any other task in this plan. TARGET-02's checkbox stays unticked and its traceability row keeps its awaiting-sign-off wording; a prior automated step already re-ticked it once and had to be manually reverted, and this plan must not repeat that.

    Keep the value on a single line so the file's line-oriented gap-ledger structure is preserved and the verification greps below remain meaningful.
  </action>
  <verify>
    <automated>U=.planning/phases/01-target-extraction-contract/01-UAT.md; test "$(grep -c '^- gap_id:' "$U")" -eq 6 || { echo "GAP COUNT CHANGED"; exit 1; }; test "$(grep -c '^  status: resolved' "$U")" -eq 5 || { echo "RESOLVED COUNT CHANGED"; exit 1; }; test "$(grep -c '^  status: failed' "$U")" -eq 1 || { echo "FAILED COUNT CHANGED"; exit 1; }; awk '/^- gap_id: G-01-6/{f=1} f && /^  status:/{print $2; exit}' "$U" | grep -qx failed || { echo "G-01-6 VERDICT WAS FLIPPED"; exit 1; }; N=$(grep -c '^  post_fix_note:' "$U"); test "$N" -eq 1 || { echo "EXPECTED EXACTLY ONE post_fix_note KEY, GOT $N"; exit 1; }; for s in "Amendment A8" "01-16-ORDERING-OUTCOME.md" "01-SIGNOFF-REQUEST.md" "/gsd-verify-work 1" "Mechanism C"; do grep '^  post_fix_note:' "$U" | grep -qF "$s" || { echo "ANNOTATION MISSING: $s"; exit 1; }; done; grep -qE '^- \[ \] \*\*TARGET-02\*\*' .planning/REQUIREMENTS.md || { echo "TARGET-02 WAS FLIPPED"; exit 1; }; grep -E '^\| TARGET-02 \| Phase 1 \|' .planning/REQUIREMENTS.md | grep -qiF 'awaiting human visual sign-off' || { echo "TARGET-02 TRACEABILITY ROW WAS FLIPPED"; exit 1; }; test -z "$(git status --porcelain --untracked-files=no src tests scripts processed_data data/raw .planning/REQUIREMENTS.md)" || { echo "SOMETHING OUT OF SCOPE CHANGED"; exit 1; }; echo "G-01-6 ANNOTATED, VERDICT INTACT"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^- gap_id:' 01-UAT.md` returns 6, `grep -c '^  status: resolved'` returns 5, and `grep -c '^  status: failed'` returns 1 — the gap ledger's shape is provably identical to before this task.
    - The first `status:` line following the `G-01-6` gap header still reads its original unresolved verdict — asserted by the awk scan in `<verify>`, which reads the entry positionally rather than trusting a whole-file count.
    - `grep -c '^  post_fix_note:' 01-UAT.md` returns exactly 1 — one annotation key was added, at sibling indentation, and no second one crept in.
    - That single `post_fix_note:` line contains `Amendment A8`, `01-16-ORDERING-OUTCOME.md`, `01-SIGNOFF-REQUEST.md`, `/gsd-verify-work 1`, and `Mechanism C` — the annotation records what changed, what did not, where the current numbers live, and how the gap actually closes.
    - `.planning/REQUIREMENTS.md` still shows `- [ ] **TARGET-02**` and its traceability row still reads "awaiting human visual sign-off"; `git status --porcelain --untracked-files=no .planning/REQUIREMENTS.md` is empty — this plan did not touch it at all.
    - `git status --porcelain --untracked-files=no src tests scripts processed_data data/raw` is empty.
  </acceptance_criteria>
  <done>G-01-6's ledger entry tells a reader what Amendment A8 changed, what it deliberately did not, and where the live numbers are — while its verdict, and every other gap's verdict, is provably exactly as a human last left it.</done>
</task>

<task type="auto">
  <name>Task 3: Disclose the WR-01 diagnostic drift in-file as accepted, dated debt — comment-only, behavior frozen</name>
  <files>scripts/diagnose_track10_coverage.py</files>
  <read_first>
    - `scripts/diagnose_track10_coverage.py` — the module imports at the top, and `classify_column` in full (roughly lines 110-175), including its existing two-line mirror comment stating that it mirrors `targets.halfmax_edges`' rejection branches and must be kept in step with that function, and the inner comment on the `MIN_TRACKED_LENGTH_RATIO` gate.
    - `scripts/diagnose_width_regression.py` — the comment block immediately above `BEAD_MASK_OPTIONS` (roughly lines 37-42). This is the established in-repo disclaimer pattern `01-REVIEW.md` explicitly points at: it names which rows exercise the production path, which are retained only as a historical baseline, and states they must never be mistaken for what `src/targets.py` actually ships. Match this voice and shape.
    - `.planning/phases/01-target-extraction-contract/01-REVIEW.md` — the WR-01 section in full: the two concrete divergences (merge-then-clip ordering versus production's clip-then-merge, and the entirely absent history-based gate), the measured mismatch between the committed CSV's counts and the current production `valid_mask` sums, and both offered fixes.
    - `src/targets.py` — `halfmax_edges` as it currently stands, to confirm the disclaimer describes the real divergence accurately rather than restating the review's prose.
    - `./.claude/CLAUDE.md` — the Comments convention: sparse, single-line-style comments explaining non-obvious intent; no docstrings; print-based CLI feedback. The disclaimer must match this house style.
  </read_first>
  <precondition>`scripts/diagnose_track10_coverage.py` still contains a hand-written `classify_column` that reimplements the candidate-selection logic rather than importing `targets.halfmax_edges`. If it has since been re-synced or refactored to call the production function, HALT — the disclaimer would then be false.</precondition>
  <reversibility rating="reversible">Adding a comment block to a diagnostic script, fully recoverable via git. No executable line changes, so the script's behavior is bit-identical.</reversibility>
  <action>
    Add a comment block immediately above the `def classify_column(prof, y_mm, previous_center):` line in `scripts/diagnose_track10_coverage.py`, following the house comment style (plain `#` lines, no docstring, no type hints added, sparse and explanatory). Leave the existing two-line mirror comment inside the function body exactly as it is.

    The comment must state, accurately and in the reviewer's own terms:
    - that this function is a hand-written reimplementation of `targets.halfmax_edges`' candidate-selection and rejection branches, last synced to the pre-Amendment-A8 behavior;
    - that it has NOT been updated for Amendment A8, naming both divergences concretely: it merges runs and then filters boundary-touching ones, whereas production now filters non-boundary-touching raw runs before merging; and it has no equivalent of production's history-based far-AND-small gate keyed on `previous_length_mm`;
    - that as a consequence `processed_data/diagnostics/track10_coverage_diagnosis.csv` is a historical baseline whose per-track rejection counts do not match the current production `valid_mask` sums, and must never be read as a live characterization of what `src/targets.py` ships;
    - that the authoritative source of production behavior is `targets.halfmax_edges` in `src/targets.py`;
    - and that this is accepted, disclosed debt recorded as WR-01 in `01-REVIEW.md`, with the durable fix being to call `targets.halfmax_edges` directly rather than duplicating it — deferred out of this phase.

    Change nothing else. Do NOT alter any executable statement, import, constant, argument, or return value. Do NOT re-sync the logic. Do NOT regenerate `processed_data/diagnostics/track10_coverage_diagnosis.csv` or run the diagnostic. Do NOT touch `src/targets.py`, `tests/`, or anything under `processed_data/`.

    Run this task's `<verify>` BEFORE committing — its diff-based assertion that no non-comment line changed reads the working tree against `HEAD`.
  </action>
  <verify>
    <automated>S=scripts/diagnose_track10_coverage.py; .venv/bin/python -m py_compile "$S" || { echo "SCRIPT NO LONGER COMPILES"; exit 1; }; for s in "Amendment A8" "previous_length_mm" "track10_coverage_diagnosis.csv" "halfmax_edges" "WR-01"; do grep -qF "$s" "$S" || { echo "DISCLAIMER MISSING: $s"; exit 1; }; done; grep -qiF 'historical' "$S" || { echo "DISCLAIMER DOES NOT MARK THE CSV AS A HISTORICAL BASELINE"; exit 1; }; BAD=$(git diff HEAD -- "$S" | grep -E '^[+-]' | grep -vE '^(\+\+\+|---)' | grep -vE '^[+-][[:space:]]*#' | grep -vE '^[+-][[:space:]]*$'); test -z "$BAD" || { echo "NON-COMMENT LINE CHANGED:"; echo "$BAD"; exit 1; }; test -z "$(git status --porcelain --untracked-files=no src tests processed_data data/raw)" || { echo "SOMETHING OUT OF SCOPE CHANGED"; exit 1; }; .venv/bin/python scripts/check_targets.py --project_dir . >/dev/null 2>&1 || { echo "PIPELINE CHECK REGRESSED"; exit 1; }; echo "WR-01 DISCLOSED, BEHAVIOR FROZEN"</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/bin/python -m py_compile scripts/diagnose_track10_coverage.py` succeeds — the file is still valid Python.
    - The file contains `Amendment A8`, `previous_length_mm`, `halfmax_edges`, `track10_coverage_diagnosis.csv`, `WR-01`, and the word `historical` — the disclaimer names the amendment it lags, both divergences by identifier, the artifact it invalidates, the authoritative source, and its review-finding id.
    - `git diff HEAD -- scripts/diagnose_track10_coverage.py`, filtered to added/removed lines that are neither comment lines nor blank, is EMPTY — asserted in `<verify>`, so the script's executable behavior is provably identical before and after and no logic was silently re-synced.
    - `git status --porcelain --untracked-files=no processed_data` is empty — `track10_coverage_diagnosis.csv` was not regenerated and no target artifact or QA figure was touched.
    - `git status --porcelain --untracked-files=no src tests data/raw` is empty — no production source, test, or raw-data file was touched.
    - `.venv/bin/python scripts/check_targets.py --project_dir .` still exits 0.
  </acceptance_criteria>
  <done>Anyone opening the drifted diagnostic or its committed CSV is told, in the file itself, exactly which production behavior it lags and that its numbers are a historical baseline — while the script's behavior and every committed artifact are provably unchanged.</done>
</task>

</tasks>

<threat_model>
This round writes one declaration file, adds one annotation key to a planning document, and adds a comment block to a diagnostic script, in a local offline scientific pipeline with no network surface, no authentication, no user input, and no ORM. Inventing web-application threats here would be padding. The genuine ASVS-L1-relevant risks are record-integrity risks — a forged verdict, a fabricated coverage claim, or an accidental behavioral change smuggled in as a comment — plus the data-handling constraint that competition-restricted raw data stays out of committed artifacts. ASVS level 1; block on `high`.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| automated plan output → human gap verdict | `01-UAT.md`'s G-01-6 `status:` field and `REQUIREMENTS.md`'s TARGET-02 checkbox are human-owned. An automated step already re-ticked TARGET-02 once and had to be manually reverted; this plan must be structurally incapable of repeating that. |
| capability detector → sealing decision | A fabricated coverage matrix would satisfy the gate while asserting something false about the system, converting a false positive into a false record. |
| comment-only edit → executable diagnostic behavior | A "comment" edit that silently changes a statement would alter a diagnostic's output while claiming behavior was frozen. |
| competition-restricted `data/raw/` → committed artifacts | `DATA_USE_LICENSE.md` restricts this dataset. No task in this plan opens anything under `data/raw/`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-55 | Spoofing | human gap verdict and requirement completion | high | mitigate | Task 2's `<verify>` asserts the gap count, resolved count, and failed count are all unchanged, and positionally re-reads G-01-6's own `status:` line via awk to confirm the verdict is untouched. It additionally asserts `REQUIREMENTS.md` still shows TARGET-02 unticked with its awaiting-sign-off traceability row, and that `git status --porcelain --untracked-files=no .planning/REQUIREMENTS.md` is empty — the file is not in this plan's `files_modified` at all. |
| T-01-56 | Tampering (false record) | `COVERAGE.md` content | high | mitigate | The declaration's shape is gated: the exact declaration prefix, a reason of at least 40 characters ending in a period, a required mention of what the phase touches instead, and `grep -c '^\|'` returning 0 so no fabricated matrix can pass. The declaration's factual basis is verified by grepping `src/`, `scripts/`, and `requirements.txt` for network clients before it is written. |
| T-01-57 | Tampering | diagnostic script behavior smuggled in as a comment | high | mitigate | Task 3's `<verify>` filters `git diff HEAD` for added/removed lines that are neither comment nor blank and requires the result to be empty, then re-runs `py_compile` and `check_targets.py`. A logic change disguised as a comment edit fails mechanically. |
| T-01-58 | Tampering | locked extraction constants and published artifacts | high | mitigate | Every task asserts `git status --porcelain --untracked-files=no src tests processed_data data/raw` is empty (Tasks 1-2 additionally freeze `scripts`). The extraction pipeline is never invoked in write mode; `track10_coverage_diagnosis.csv` is explicitly not regenerated. |
| T-01-59 | Information disclosure | competition-restricted raw data | low | accept | No task reads, copies, or excerpts anything under `data/raw/`. The only quantities referenced are aggregate per-track statistics already committed in `01-16-ORDERING-OUTCOME.md`. No new data path, network call, or credential handling is introduced. |
| T-01-SC | Tampering | npm/pip/cargo installs | high | mitigate | No packages are installed by this plan and `requirements.txt` is unchanged. No `[ASSUMED]` or `[SUS]` package is introduced, so no legitimacy checkpoint is required. |
</threat_model>

<verification>
- `gsd-tools check api-coverage-verify-pre 01` reports `block` as false, and `.planning/phases/01-target-extraction-contract/COVERAGE.md` carries a single declaration line with a mandatory, verified reason and no fabricated matrix.
- `01-UAT.md` has exactly six gap entries, five resolved and one failed, exactly as before this plan; G-01-6's own `status:` line is positionally confirmed unchanged; exactly one `post_fix_note:` key was added and it names Amendment A8, `01-16-ORDERING-OUTCOME.md`, `01-SIGNOFF-REQUEST.md`, `/gsd-verify-work 1`, and Mechanism C.
- `.planning/REQUIREMENTS.md` is untouched: TARGET-02 remains unticked and its traceability row still reads "awaiting human visual sign-off".
- `scripts/diagnose_track10_coverage.py` compiles, carries the WR-01 historical-baseline disclaimer naming both A8 divergences and the invalidated CSV, and its diff contains no added or removed non-comment, non-blank line.
- `processed_data/diagnostics/track10_coverage_diagnosis.csv` is unchanged; `git status --porcelain --untracked-files=no src tests processed_data data/raw` is empty for the whole plan.
- `.venv/bin/python scripts/check_targets.py --project_dir .` still exits 0.
- No extraction parameter was touched, no threshold was tuned, and the 10-vs-14 FLAG was not reopened.
</verification>

<success_criteria>
- The phase can be sealed on the strength of a human's visual sign-off alone, with no capability gate blocking on a self-referential false positive.
- The phase's own gap ledger reads honestly: it records that Amendment A8 landed and what it did and did not achieve, while still showing G-01-6 as unresolved — because it is, until a human says otherwise.
- A future reader of `track10_coverage_diagnosis.csv` is warned in the source file itself that its numbers are a historical baseline, rather than discovering the drift a third time.
- Nothing that a human owns was decided by an automated step, and no committed artifact or executable line changed.
</success_criteria>

<output>
Create `.planning/phases/01-target-extraction-contract/01-18-SUMMARY.md` when done. Record in its deviation section: any correction to `COVERAGE.md`'s path required to satisfy the real gate, and any case where a live source disagreed with a claim made in this plan — the live source wins and the disagreement is stated, never silently reconciled.
</output>
