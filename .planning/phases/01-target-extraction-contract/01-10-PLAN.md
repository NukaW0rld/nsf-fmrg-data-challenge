---
phase: 01-target-extraction-contract
plan: 10
type: execute
wave: 9
depends_on: [01-08]
files_modified:
  - scripts/run_target_extraction.py
  - scripts/check_targets.py
  - tests/test_run_target_extraction.py
autonomous: true
gap_closure: true
requirements: [TARGET-01, TARGET-02]
must_haves:
  truths:
    - "resolve_output_path rejects a path that IS a symlink, and rejects any symlinked ancestor between the candidate and the canonical repository root, before it resolves or returns anything (CR-03)."
    - "publish_staging_dir re-checks backup_dir and targets_dir with is_symlink() immediately before every shutil.rmtree and every rename, and refuses to operate on a symlink."
    - "A pre-existing symlink at processed_data/targets.previous pointing at an unrelated in-repository directory causes run_pipeline to fail, and the victim directory survives intact — proven by tests/test_run_target_extraction.py::test_symlink_at_backup_path_is_rejected."
    - "A pre-existing symlink at processed_data itself pointing at an unrelated in-repository directory causes run_pipeline to fail with the victim intact — proven by tests/test_run_target_extraction.py::test_symlink_at_processed_data_is_rejected."
    - "scripts/check_targets.py defines MIN_VALID_FRACTION = 0.5 and enforces it through require(...), so a track below the floor raises and main() never reaches ALL CHECKS PASSED (CR-02, decision U-02)."
    - "The coverage floor is a single project-wide value with no per-track allowance table and no documented per-track exemption; the previously informational sub-50% print no longer exists as a separate non-blocking branch."
    - "check_targets.py exits non-zero against the artifacts published on 2026-07-20 because track 10's valid fraction is 5.2% — the intended fail-closed behavior that sequences the track-10 fix ahead of any Phase 1 sign-off."
  artifacts:
    - "scripts/run_target_extraction.py"
    - "scripts/check_targets.py"
    - "tests/test_run_target_extraction.py"
  key_links:
    - "resolve_output_path -> publish_staging_dir -> shutil.rmtree/rename: every destructive publish operation must pass a symlink check at both the validation point and immediately before the operation, because the two are separated by a TOCTOU window this project has explicitly accepted."
    - "check_targets.py::check_track -> main()'s ALL CHECKS PASSED: the coverage floor must raise through require(), not print, or the phase's only automated contract gate reports success on a 95%-invalid track."
  prohibitions:
    - statement: "Output publication must NOT permit destructive operations outside the intended output tree — no rmtree or rename may follow a symlink out of processed_data/."
      status: resolved
      verification: test
    - statement: "Extraction and persistence code must NOT write to, delete, or modify any file under data/raw/."
      status: resolved
      verification: test
    - statement: "Extraction contract constants must NOT be adjusted based on whether the resulting width ordering or QA plots look correct; this plan changes no extraction constant."
      status: resolved
      verification: judgment
    - statement: "Do NOT weaken an existing gate to make a check pass — MIN_VALID_FRACTION is not to be lowered, exempted, or made per-track when check_targets.py starts failing on track 10."
      status: resolved
      verification: judgment
---

<objective>
Close the two reproduced critical findings that make this phase's automated safety net untrustworthy. CR-03: the publish path follows a pre-existing symlink at `processed_data/targets.previous` and `shutil.rmtree`s whatever it points at — reproduced live deleting `src/` during a normal, successful `run_pipeline()` while the `data/raw/` audit correctly reported PASS. CR-02: `check_targets.py`'s only hard coverage gate is `valid_count > 0`, so it printed "ALL CHECKS PASSED" over a track that was 5.2% valid.

Purpose: TARGET-01's contract-checker and TARGET-02's persistence path are the two mechanisms every downstream phase relies on to trust `processed_data/targets/`. One can destroy repository state on a successful run; the other reports success on a near-total extraction failure. Both must fail closed before track 10 is diagnosed.
Output: A publish path that refuses symlinks at every destructive step, and a coverage floor that raises. Per decision U-02, `check_targets.py` will exit non-zero against today's artifacts — that is the intended, accepted outcome and it correctly sequences plan 01-11 ahead of any Phase 1 sign-off.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-target-extraction-contract/01-REVIEW.md
@.planning/phases/01-target-extraction-contract/01-VERIFICATION.md
@.planning/phases/01-target-extraction-contract/01-SECURITY.md
@scripts/run_target_extraction.py
@scripts/check_targets.py
@tests/test_run_target_extraction.py
</context>

<artifacts_produced>
Artifacts this phase produces — new symbols and paths introduced by this plan (exclude these from source-drift verification; they do not exist yet):

- New module constant in `scripts/check_targets.py`: `MIN_VALID_FRACTION = 0.5`.
- New helper in `scripts/run_target_extraction.py`: `reject_symlink_path(path, project_root)` — raises `ValueError` when the given path or any ancestor strictly below the canonical repository root is a symlink.
- New tests in `tests/test_run_target_extraction.py`: `test_symlink_at_backup_path_is_rejected`, `test_symlink_at_processed_data_is_rejected`, `test_publish_refuses_symlinked_backup_immediately_before_rmtree`.
- No new files, no new CLI flags.
</artifacts_produced>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: CR-03 — reject symlinks at every output path the publisher touches</name>
  <files>scripts/run_target_extraction.py, tests/test_run_target_extraction.py</files>
  <read_first>
    - scripts/run_target_extraction.py (lines 62-70: `resolve_output_path`, which calls `Path(path).resolve(strict=False)` and then checks containment but never rejects a symlink; lines 294-306: `publish_staging_dir`, which computes `backup_dir` from `targets_dir.with_name(...)` and unconditionally `shutil.rmtree`s it, then renames `targets_dir` onto it and `staging_dir` onto `targets_dir`; lines 309-412: `run_pipeline`, for every other `resolve_output_path` call site that must keep working)
    - .planning/phases/01-target-extraction-contract/01-REVIEW.md (CR-03: the live reproduction transcript and the suggested `is_symlink()` fix shape)
    - .planning/phases/01-target-extraction-contract/01-VERIFICATION.md (truth 7 and its `missing:` item — the exact regression shape required: plant a symlink at `targets.previous` and separately at `processed_data`, pointing at a harmless in-repo victim directory, and assert the victim survives)
    - tests/test_run_target_extraction.py (the whole file: the `require()` helper at line 15, `make_repository` at 20-33, `require_no_published_or_staged_output` at 34-47, `clean_stub` at 48-65, the existing `test_symlink_output_into_raw_is_rejected` at 81-98 whose assertions must keep passing, and the `__main__` runner list at 254-266)
    - .planning/phases/01-target-extraction-contract/01-SECURITY.md (threats T-01-10 and T-01-11: the already-accepted residual TOCTOU window this fix narrows but does not remove)
  </read_first>
  <behavior>
    - Given a bounded temp repository built by `make_repository`, with a real directory `VICTIM/` containing a sentinel file and a symlink at `processed_data/targets.previous` pointing at `VICTIM/`, calling `run_pipeline` with a `clean_stub` track runner raises `ValueError`, `VICTIM/` and its sentinel file still exist, and no `processed_data/targets/` tree is published.
    - The same holds when the symlink is planted at `processed_data` itself rather than at `targets.previous`.
    - `resolve_output_path` raises `ValueError` naming the offending path when the candidate is a symlink, and when any ancestor between the candidate and the canonical repository root is a symlink.
    - `resolve_output_path` still returns the resolved path unchanged for ordinary non-symlink destinations, so every existing call site in `run_pipeline` and in `scripts/check_targets.py` and `scripts/diagnose_width_regression.py` keeps working.
    - The pre-existing `test_symlink_output_into_raw_is_rejected` still passes (a symlink pointing into `data/raw` is still rejected, now at the symlink check rather than the raw-containment check).
  </behavior>
  <action>
    Add `reject_symlink_path(path, project_root)` to `scripts/run_target_extraction.py`, placed immediately above `resolve_output_path`. It takes the lexical (unresolved) candidate: if the candidate is relative, join it onto the resolved `project_root`. Raise `ValueError(f"Output path must not be a symlink: {offender}.")` when `Path.is_symlink()` is true for the candidate itself or for any of `candidate.parents` that is strictly below the resolved `project_root`; stop the ancestor walk at `project_root` so a symlink outside the repository in the user's own path prefix is not the pipeline's business. Use `is_symlink()` (lstat-based, no dereference) and never `exists()`, so a dangling symlink is also rejected.

    Call `reject_symlink_path(path, project_root)` as the first statement of `resolve_output_path`, before the existing `Path(path).resolve(strict=False)`. Keep the two existing checks (canonical-root containment and protected-raw exclusion) and the return value exactly as they are, so every current call site's contract is unchanged.

    In `publish_staging_dir`, re-check immediately before each destructive operation, because path validation and the operation are separated by the TOCTOU window this project has explicitly accepted: before the first `shutil.rmtree(backup_dir)` raise `ValueError` when `backup_dir.is_symlink()`; before `targets_dir.rename(backup_dir)` raise when either `targets_dir.is_symlink()` or `backup_dir.is_symlink()`; before `staging_dir.rename(targets_dir)` raise when `targets_dir.is_symlink()` or `staging_dir.is_symlink()`; and before the trailing `shutil.rmtree(backup_dir)` raise when `backup_dir.is_symlink()`. Use `is_symlink()` rather than `exists()` for these guards so the check does not follow the link. Add `staging_dir` and `targets_dir` symlink guards without changing the atomic staged-publish semantics established in plan 01-03.

    Note that `publish_staging_dir` is invoked from `run_pipeline`'s `finally` block after the `data/raw/` audit; a `ValueError` raised there must propagate as it does today, and the existing failure-path staging cleanup must remain intact. Do not restructure the `finally` block.

    Add the three regressions named in `<artifacts_produced>` to `tests/test_run_target_extraction.py`, reusing `make_repository`, `clean_stub`, and `require_no_published_or_staged_output`. Create the victim as a real directory containing a sentinel file, plant the symlink with `Path.symlink_to(..., target_is_directory=True)`, assert the raise, then assert the victim directory and its sentinel file still exist. Append all three to the `__main__` runner list.
  </action>
  <verify>
    <automated>.venv/bin/python tests/test_run_target_extraction.py && .venv/bin/python -O tests/test_run_target_extraction.py && .venv/bin/python -c "import sys; sys.path[:0]=['src','scripts']; import run_target_extraction as r; print('reject_symlink_path' in dir(r))" | grep -qx True && echo "SYMLINK GUARD PRESENT"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "is_symlink" scripts/run_target_extraction.py` shows guards in both `reject_symlink_path` and `publish_staging_dir` (at minimum one guard preceding each `shutil.rmtree` and each `rename`).
    - `.venv/bin/python tests/test_run_target_extraction.py` reports `PASS: test_symlink_at_backup_path_is_rejected`, `PASS: test_symlink_at_processed_data_is_rejected`, and `PASS: test_publish_refuses_symlinked_backup_immediately_before_rmtree`, and the victim directory survives `run_pipeline` in each.
    - The pre-existing `PASS: test_symlink_output_into_raw_is_rejected` and all other originally-registered tests in the file still pass, under both `.venv/bin/python` and `.venv/bin/python -O`.
    - `tests/test_run_target_extraction.py` grows from 7 to 10 registered tests.
    - `.venv/bin/python scripts/run_target_extraction.py --project_dir .` is NOT run by this task (it is plan 01-11's atomic regeneration); `git status --porcelain processed_data` shows no artifact churn from this task.
    - `git status --porcelain data/raw` is empty.
  </acceptance_criteria>
  <done>No destructive publish operation can be redirected by a pre-existing symlink, and the exploit reproduced in 01-REVIEW.md CR-03 is covered by a regression that asserts the victim directory survives.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: CR-02 — promote the coverage floor to a hard, project-wide require()</name>
  <files>scripts/check_targets.py</files>
  <read_first>
    - scripts/check_targets.py (lines 21-29: the module constants block and the `require()` helper; lines 92-96: `valid_count`/`valid_fraction` and the bare informational sub-50% print that must be reconciled; lines 107-144: `main()`, which reaches `print("ALL CHECKS PASSED")` unconditionally today)
    - .planning/phases/01-target-extraction-contract/01-REVIEW.md (CR-02: the exact `require(...)` shape suggested, and the explicit note that a per-track allowance would be the alternative)
    - .planning/phases/01-target-extraction-contract/01-VERIFICATION.md (truth 6 and Human Verification item #3 — the floor value is the human policy decision, now locked at 0.5)
    - .planning/phases/01-target-extraction-contract/01-08-ORDERING-OUTCOME.md (track 10's 21/400 = 5.2% valid fraction, the artifact this gate must now reject)
  </read_first>
  <behavior>
    - `check_targets.py` run against the artifacts published on 2026-07-20 raises `ValueError` on track 10 and exits non-zero; `ALL CHECKS PASSED` is not printed.
    - The raised message names the track, its actual valid fraction as a percentage, and the `MIN_VALID_FRACTION` floor it violated.
    - Tracks 8, 14, and 21 (359, 301, and 324 of 400 valid) pass the floor unchanged.
    - The floor is a single module constant consulted by every track; there is no per-track lookup table and no exemption branch.
  </behavior>
  <action>
    Add `MIN_VALID_FRACTION = 0.5` to the module constants block in `scripts/check_targets.py`, alongside `EXPECTED_KEYS`, `FLOAT_KEYS`, `EXPECTED_SHAPE`, and `Y_STRIP_EXTENT_MM`, following the existing SCREAMING_SNAKE_CASE convention. Add a one-line comment recording that this floor is the project's locked minimum usable coverage per decision U-02, with no per-track allowance.

    In `check_track`, keep the existing `require(valid_count > 0, ...)` all-invalid guard, then replace the non-blocking sub-50% branch with a hard gate: `require(valid_fraction >= MIN_VALID_FRACTION, ...)` whose message names the track id, the actual fraction formatted as a percentage, the `MIN_VALID_FRACTION` floor formatted as a percentage, and states that extraction likely failed for that track. Remove the now-redundant informational print entirely rather than leaving both — a single, blocking statement of the condition, not a print followed by a raise.

    Do not add a CLI flag, environment variable, or any other mechanism to bypass or soften the floor. Do not introduce a per-track allowance dictionary. `require()` already raises `ValueError` unconditionally (it is deliberately not a bare `assert`, so it survives `python -O` per T-01-14) — reuse it as-is.

    Expect and accept that this makes `.venv/bin/python scripts/check_targets.py --project_dir .` exit non-zero against the currently published artifacts, because track 10 is 5.2% valid. That is the intended fail-closed behavior; it correctly blocks Phase 1 sign-off until plan 01-11 resolves track 10's coverage collapse. Do not regenerate artifacts, do not touch `src/targets.py`, and do not adjust any extraction constant to make this command pass.
  </action>
  <verify>
    <automated>.venv/bin/python -m py_compile scripts/check_targets.py && grep -q "MIN_VALID_FRACTION = 0.5" scripts/check_targets.py && grep -q "valid_fraction >= MIN_VALID_FRACTION" scripts/check_targets.py && .venv/bin/python scripts/check_targets.py --project_dir . > /tmp/check_targets_out.txt 2>&1; test $? -ne 0 && ! grep -q "ALL CHECKS PASSED" /tmp/check_targets_out.txt && grep -qi "track 10" /tmp/check_targets_out.txt && echo "COVERAGE FLOOR FAILS CLOSED"</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/check_targets.py` contains `MIN_VALID_FRACTION = 0.5` at module scope.
    - `scripts/check_targets.py` enforces the floor via `require(valid_fraction >= MIN_VALID_FRACTION, ...)`; the coverage condition appears exactly once, as a blocking statement, with no surviving parallel non-blocking print of the same condition.
    - `.venv/bin/python scripts/check_targets.py --project_dir .` exits non-zero while track 10's valid fraction is below 50%, its output names track 10 and the 50% floor, and it does not print `ALL CHECKS PASSED`.
    - The same command under `.venv/bin/python -O` also exits non-zero (the gate is a `require()`, not a bare `assert`).
    - `git diff --stat -- src/targets.py src/nsf_fmrg_data.py` is empty for this task's commit — no extraction constant was touched to influence the result.
    - `.venv/bin/python tests/test_targets.py` and `.venv/bin/python tests/test_run_target_extraction.py` still report every test PASS.
  </acceptance_criteria>
  <done>The automated contract checker fails closed on catastrophic per-track coverage loss, so a 5.2%-valid track can no longer be reported as a passing artifact.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| filesystem path validation -> destructive filesystem operation | `resolve_output_path` validates, `publish_staging_dir` deletes and renames; the gap between them is a TOCTOU window in which a symlink can be introduced. |
| pre-existing filesystem state -> publish step | `processed_data/targets.previous` and `processed_data` may already exist as symlinks left by a prior partial failure, another process, or a hostile plant. |
| persisted artifact -> downstream trust | `check_targets.py`'s exit status is the only automated signal that `processed_data/targets/` is usable as ground truth for Phases 2-5. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-20 | Tampering / Denial of service | `publish_staging_dir` -> `shutil.rmtree(backup_dir)` | high | mitigate | Task 1 rejects symlinks in `resolve_output_path` (candidate and every in-repo ancestor) and re-checks `is_symlink()` immediately before each `rmtree`/`rename`; regressions `test_symlink_at_backup_path_is_rejected` and `test_symlink_at_processed_data_is_rejected` assert an in-repo victim directory survives `run_pipeline`. This is CR-03, reproduced live deleting `src/`. |
| T-01-21 | Repudiation | `scripts/check_targets.py` coverage gate | high | mitigate | Task 2 promotes the coverage check to `require(valid_fraction >= MIN_VALID_FRACTION)` so a 5.2%-valid track raises instead of printing a FLAG under an unconditional `ALL CHECKS PASSED`. This is CR-02. |
| T-01-22 | Tampering | validation-to-operation TOCTOU window | medium | mitigate | Narrowed, not eliminated: symlink checks are repeated immediately before each destructive call rather than only at validation time. The residual single-user local window remains explicitly accepted per T-01-11 and `01-03-SUMMARY.md`. |
| T-01-23 | Tampering (accidental) | `data/raw/` | high | mitigate | This plan edits only `scripts/` and `tests/`; the existing `snapshot_raw`/`raw_snapshot_diff` audit is untouched and still runs in `run_pipeline`'s `finally`; `git status --porcelain data/raw` is empty after both tasks. |
| T-01-SC | Tampering | npm/pip/cargo installs | high | mitigate | No packages are installed by this plan; `requirements.txt` is unchanged. No `[ASSUMED]`/`[SUS]` package is introduced, so no legitimacy checkpoint is required. |
</threat_model>

<verification>
- `.venv/bin/python -m py_compile src/*.py scripts/*.py tests/*.py` exits 0.
- `.venv/bin/python tests/test_run_target_extraction.py` reports 10 tests PASS (7 pre-existing plus 3 new symlink regressions), under both normal and `-O` Python; `tests/test_targets.py` and `tests/test_nsf_fmrg_data.py` are unaffected and still green.
- `.venv/bin/python scripts/check_targets.py --project_dir .` exits non-zero, names track 10 and the 50% floor, and does not print `ALL CHECKS PASSED` — the accepted, intended consequence of decision U-02 until plan 01-11 lands.
- `git diff --stat -- src/targets.py src/nsf_fmrg_data.py` is empty for this plan.
- `grep -n "track_id ==" src/targets.py src/nsf_fmrg_data.py scripts/run_target_extraction.py` returns no matches; `tests/test_targets.py::test_single_parameterization_has_no_track_conditionals` stays green.
- `git status --porcelain data/raw` is empty.
</verification>

<success_criteria>
- The publish path refuses symlinks at validation and again immediately before every `rmtree` and `rename`, with regressions proving an in-repo victim directory survives (CR-03 closed, verification truth 7 satisfiable).
- `check_targets.py` fails closed at a single project-wide `MIN_VALID_FRACTION = 0.5` floor with no per-track exemption (CR-02 closed, verification truth 6 satisfiable).
- No extraction constant was changed, no gate was weakened, and `data/raw/` is untouched.
</success_criteria>

<output>
Create `.planning/phases/01-target-extraction-contract/01-10-SUMMARY.md` when done.
</output>
