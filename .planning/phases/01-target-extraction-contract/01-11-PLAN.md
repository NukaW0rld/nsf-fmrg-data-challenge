---
phase: 01-target-extraction-contract
plan: 11
type: execute
wave: 10
depends_on: [01-09, 01-10]
files_modified:
  - scripts/diagnose_track10_coverage.py
  - scripts/check_targets.py
  - src/nsf_fmrg_data.py
  - src/targets.py
  - tests/test_nsf_fmrg_data.py
  - tests/test_targets.py
  - processed_data/diagnostics/track10_coverage_diagnosis.csv
  - .planning/phases/01-target-extraction-contract/01-11-DIAGNOSIS.md
  - .planning/phases/01-target-extraction-contract/01-11-CRITERION.md
  - .planning/phases/01-target-extraction-contract/01-11-ORDERING-OUTCOME.md
  - .planning/phases/01-target-extraction-contract/01-CONTEXT.md
autonomous: true
gap_closure: true
requirements: [TARGET-01, TARGET-02]
must_haves:
  truths:
    - "The diagnostic's evidence CSV is written to processed_data/diagnostics/ — a SIBLING of processed_data/targets/, never a child — so Task 4's publish_staging_dir rename-and-rmtree cycle cannot destroy it, and it still exists on disk after the four-track regeneration completes."
    - "Track 10's coverage collapse is characterized by committed, re-runnable diagnostic code that independently re-derives the raw substrate span, the production-path residual profile, the fitted-surface edge behavior, the per-bin rejection-reason histogram, and the design-matrix conditioning for all 4 tracks under one uniform code path."
    - "The diagnosis states plainly whether track 10's bead is a detrend artifact (recoverable by a uniform change) or physically truncated by the profilometer y-window (not recoverable), and does not force the recoverable conclusion."
    - "The fix-selection criterion is residual/physical/numerical, outcome-independent, names the evidence that would REJECT each candidate, and is committed to git before any change exists in src/targets.py or src/nsf_fmrg_data.py."
    - "Exactly ONE uniform, track-independent change is applied; it is selected by the pre-registered criterion and not by whether the width ordering or any track's valid fraction improves."
    - "Any new contract constant is present in extraction_params(), covered by the change-sensitive SHA-256 provenance digest, and canonicalized as dated Amendment A5 in 01-CONTEXT.md in the same style as A3/A4."
    - "All 4 tracks are regenerated atomically in a single run_target_extraction.py invocation, producing 4 NPZs, 12 QA PNGs, extraction_params.json, and manifest.json from one run id."
    - "The resulting 8 > 10 > 14 > 21 width ordering and every track's valid fraction are reported verbatim and honestly, whatever they are, with no extraction constant changed in response."
    - "Threshold semantics stay exact: a column whose peak-baseline separation equals MIN_PEAK_BASELINE_SEPARATION_MM is valid (only strictly below invalidates), and a gap of exactly MAX_GAP_PIXELS consecutive NaNs is still interpolated (only exceeding it invalidates) — D-04/D-06."
    - "Savitzky-Golay smoothing may blend valid points from both sides of a masked gap (standard NaN-skip window behavior) rather than being restricted to one side."
    - "A track with zero valid x-positions after masking raises a hard ValueError rather than silently producing an all-NaN artifact."
    - "Percentile calculation uses numpy's default linear-interpolation convention, and a crossed boundary (upper not strictly above lower, i.e. a negative width) marks that x-column invalid rather than reporting a negative or zero-clipped width."
    - "Crop-edge (20mm/100mm) behavior uses the shrinking-window SG boundary mode and stays visually inspectable in the per-track width QA figure rather than being silently trimmed — D-12."
    - "Invalid x-positions keep their grid slot with width = NaN and a False valid-mask entry — never a ragged or dropped position array — D-07."
    - "An all-invalid track raises a hard error at the runner level too, so no all-NaN artifact is ever published."
    - "Persisted target arrays (x_grid_mm, w_mm, y_upper_mm, y_lower_mm) are float64 and valid_mask is bool."
    - "Review item IN-01 is closed: check_targets.Y_STRIP_EXTENT_MM and nsf_fmrg_data.EXTRACTED_THERMAL_FRAMES each carry an inline derivation on their module-level definition, with no value, signature, or control-flow change and no comment added inside any src/nsf_fmrg_data.py function body."
  artifacts:
    - "scripts/diagnose_track10_coverage.py"
    - "processed_data/diagnostics/track10_coverage_diagnosis.csv"
    - ".planning/phases/01-target-extraction-contract/01-11-DIAGNOSIS.md"
    - ".planning/phases/01-target-extraction-contract/01-11-CRITERION.md"
    - ".planning/phases/01-target-extraction-contract/01-11-ORDERING-OUTCOME.md"
    - ".planning/phases/01-target-extraction-contract/01-CONTEXT.md"
    - "src/nsf_fmrg_data.py"
    - "src/targets.py"
    - "processed_data/targets/track_8_targets.npz"
    - "processed_data/targets/track_10_targets.npz"
    - "processed_data/targets/track_14_targets.npz"
    - "processed_data/targets/track_21_targets.npz"
    - "processed_data/targets/extraction_params.json"
    - "processed_data/targets/manifest.json"
    - "processed_data/targets/qa/ (12 regenerated PNGs)"
  key_links:
    - "01-11-CRITERION.md -> the applied fix: the criterion must exist in git history before the fix commit, or the no-outcome-driven-tuning prohibition loses its structural enforcement."
    - "targets.bead_exclusion_mask -> robust_plane_detrend(order=DETREND_POLY_ORDER, fit_mask=...) -> extract_targets_from_arrays: the fitted background surface's behavior at the y-strip edge determines whether halfmax_edges sees real bead runs or a manufactured edge ridge."
    - "new contract constant -> extraction_params() -> extraction_params.json -> manifest.json SHA-256 digest: a mechanism outside the provenance digest is an undocumented contract change."
    - "run_target_extraction.py atomic publish -> check_targets.py MIN_VALID_FRACTION gate: the regenerated artifacts must clear the 50% floor plan 01-10 installed, or Phase 1 stays blocked by design."
    - "diagnostic output directory -> run_target_extraction.publish_staging_dir: the publish renames processed_data/targets/ aside and rmtree's the backup, so any diagnostic evidence written under targets/ is destroyed by Task 4's own regeneration run — the CSV must live in a sibling directory."
  prohibitions:
    - statement: "Extraction contract constants (noise-floor threshold, gap limit, percentile split, detrend order, bead-mask fraction, and any constant introduced here) must NOT be adjusted based on whether the resulting width ordering, valid fractions, or QA plots look correct."
      status: resolved
      verification: judgment
    - statement: "Extraction and persistence code must NOT write to, delete, or modify any file under data/raw/."
      status: resolved
      verification: test
    - statement: "No per-track branches or per-track-tuned constants may exist in src/targets.py, src/nsf_fmrg_data.py, or scripts/run_target_extraction.py — one shared parameterization across all 4 tracks."
      status: resolved
      verification: test
    - statement: "Do NOT weaken an existing gate to make a check pass — the boundary-clipped-run exclusion in halfmax_edges is correct per D-01/D-03 and MIN_VALID_FRACTION is not to be lowered or exempted. Fixing track 10 means removing the CAUSE of the spurious clipped runs, never relaxing the exclusion."
      status: resolved
      verification: judgment
    - statement: "Output publication must NOT permit destructive operations outside the intended output tree."
      status: resolved
      verification: test
---

<objective>
Resolve Phase 1's remaining root blocker: track 10's valid fraction collapsed to 21/400 (5.2%) under the Amendment A4 extraction, because 267/400 of its bins fail with `clipped_run_only` — the only above-half-max run in each bin touches the y-strip edge and is correctly discarded by the D-01/D-03 boundary-clip exclusion. That single defect causes the 10-vs-14 ordering FLAG, the visually empty `track_10_overlay.png`, and (since plan 01-10) a non-zero exit from `check_targets.py`.

Per decision U-01 this is ONE combined plan that diagnoses, pre-registers, fixes, regenerates, and reports. The task ordering is the enforcement mechanism the 01-06 / 01-07 / 01-08 plan split previously provided structurally: Task 1 characterizes without observing any fix outcome, Task 2 commits an outcome-independent selection criterion to git before Task 3 touches extraction source at all, Task 3 applies exactly one uniform change, and Task 4 reports whatever comes out.

Purpose: TARGET-02 requires the extractor produce a usable, visually sane target for all 4 tracks under one shared parameterization. A 95%-invalid track is not usable ground truth, and with only 4 track conditions and mandatory leave-one-track-out validation, losing one condition is not survivable for Phases 3-4.
Output: A committed diagnosis, a committed pre-registered criterion, at most one uniform contract change recorded as Amendment A5, atomically regenerated artifacts for all 4 tracks, and an honest ordering-outcome report.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-target-extraction-contract/01-CONTEXT.md
@.planning/phases/01-target-extraction-contract/01-VERIFICATION.md
@.planning/phases/01-target-extraction-contract/01-REVIEW.md
@.planning/phases/01-target-extraction-contract/01-06-DIAGNOSIS.md
@.planning/phases/01-target-extraction-contract/01-08-ORDERING-OUTCOME.md
@.planning/phases/01-target-extraction-contract/01-PATTERNS.md
@src/targets.py
@src/nsf_fmrg_data.py
@scripts/run_target_extraction.py
@tests/test_targets.py
@tests/test_nsf_fmrg_data.py
</context>

<artifacts_produced>
Artifacts this phase produces — new symbols and paths introduced by this plan (exclude these from source-drift verification; they do not exist yet):

- New script `scripts/diagnose_track10_coverage.py`, with entry point `run_coverage_diagnostic(project_dir: Path) -> Path` and helpers `raw_row_median_profile`, `production_residual_profile`, `fitted_surface_edge_report`, `rejection_reason_histogram`, `design_matrix_condition`.
- New diagnostic output `processed_data/diagnostics/track10_coverage_diagnosis.csv` — deliberately a SIBLING of `processed_data/targets/`, not a child of it, because `publish_staging_dir` destroys the published tree on every extraction run (see Task 1). Plan 01-09 relocates `scripts/diagnose_width_regression.py`'s `width_regression_sweep.csv` into the same sibling directory for the same reason, so `processed_data/diagnostics/` may already exist when this plan runs.
- New planning documents `01-11-DIAGNOSIS.md`, `01-11-CRITERION.md`, `01-11-ORDERING-OUTCOME.md` under `.planning/phases/01-target-extraction-contract/`.
- ONE new locked constant in `src/targets.py`, whichever mechanism Task 2's criterion selects — either `DETREND_NORMALIZE_BASIS` (bool) or `DETREND_MAX_Y_DEGREE` (int). Only one is introduced. It becomes the 16th key of `extraction_params()`.
- ONE new keyword argument on `nsf_fmrg_data.robust_plane_detrend`, placed after `fit_mask`, defaulting to today's behavior so every other caller (including the notebooks and the `order=1` affine path) is unchanged: either `normalize_basis=False` or `max_y_degree=None`.
- New tests in `tests/test_nsf_fmrg_data.py`: `test_detrend_does_not_diverge_at_strip_edge`, `test_detrend_edge_fix_preserves_default_behavior`.
- New test in `tests/test_targets.py`: `test_edge_divergence_fix_is_track_independent`.
- Updated (not new) tests: `test_extraction_params_provenance` (16 keys), `test_provenance_digest_is_change_sensitive` (covers the new constant), `test_polynomial_basis_sizes_are_stable` (extended, not replaced).
- New canonical amendment: Amendment A5 in `01-CONTEXT.md`.
- No new symbols from review item IN-01 — it adds inline derivation comments to the existing `scripts/check_targets.py` `Y_STRIP_EXTENT_MM` and `src/nsf_fmrg_data.py` `EXTRACTED_THERMAL_FRAMES` definitions and changes nothing else.
</artifacts_produced>

<assumption_delta_decision>
Fires only if Task 2's criterion selects the per-axis degree mechanism. In that case the singular assumption "the detrend surface has one polynomial `order`" gains a second case (an independent cross-track degree) — a singular-to-plural transition at the identity seam.

- Primary noun after the change: the per-axis polynomial degree pair (along-track, cross-track), not a single scalar `order`.
- Decision: **promote** — `order` remains the along-track degree of record and `DETREND_MAX_Y_DEGREE` caps the cross-track degree, with the keyword defaulting to "same as order" so existing affine and quartic callers are unchanged by construction. Do not add a parallel independent y-order alongside a still-required symmetric `order`; that would allow a future caller to specify an inconsistent pair.
- If the basis-conditioning mechanism is selected instead, no assumption delta fires — `DETREND_NORMALIZE_BASIS` is a numerical conditioning switch, not an identity change.
- Companion invariant already present: `tests/test_nsf_fmrg_data.py::test_polynomial_basis_sizes_are_stable` pins the total-degree basis sizes (3 terms at order 1, 15 at order 4). Extend it rather than replace it, so a future reintroduction of an unconstrained symmetric basis goes red.
</assumption_delta_decision>

<tasks>

<task type="auto">
  <name>Task 1: Characterize track 10's coverage collapse with committed diagnostic code — observing no fix outcome</name>
  <files>scripts/diagnose_track10_coverage.py, processed_data/diagnostics/track10_coverage_diagnosis.csv, .planning/phases/01-target-extraction-contract/01-11-DIAGNOSIS.md</files>
  <read_first>
    - src/targets.py (whole file: constants block lines 7-32; `bead_exclusion_mask` 220-243; `bin_profile` 80-86; `fill_small_gaps` 89-110; `halfmax_edges` 124-180, especially the boundary-clipped-run exclusion at 141-145 and the three `return None` rejection points; `extract_targets_from_arrays` 246-283; `extract_track_targets` 286-314)
    - src/nsf_fmrg_data.py (`load_wyko_asc` 160-202 — returns a dict whose `Z_mm` is shaped (y, x); `robust_plane_detrend` 205-251 — returns a TUPLE `(detrended, coef)`, builds a monomial basis from coordinates that are centered but NOT scaled, and feeds it to `np.linalg.lstsq(..., rcond=None)`)
    - scripts/diagnose_width_regression.py (the structural analog to copy: sys.path bootstrap 8-15, `run_diagnostic`'s `resolve_repository_root` / `resolve_raw_dir` / `snapshot_raw` / `raw_snapshot_diff` bracketing at 152-181, CSV writing, argparse main)
    - .planning/phases/01-target-extraction-contract/01-REVIEW.md (CR-01: the per-bin rejection histogram `{'no_columns': 0, 'gap_fail': 112, 'no_baseline_sep': 0, 'clipped_run_only': 267, 'ok': 21}` this tool must independently reproduce)
    - .planning/phases/01-target-extraction-contract/01-06-DIAGNOSIS.md (the pre-registration and honest-reporting discipline this task mirrors)
    - .planning/phases/01-target-extraction-contract/01-PATTERNS.md (script conventions: sys.path bootstrap verbatim, type hints on the public entry point only, print-only CLI feedback, SCREAMING_SNAKE_CASE constants, no docstrings)
  </read_first>
  <action>
    Create `scripts/diagnose_track10_coverage.py` mirroring `scripts/diagnose_width_regression.py`'s structure exactly: the `# Allow running from repo root without installing as a package.` sys.path block, imports from `targets`, `nsf_fmrg_data`, and `run_target_extraction`, an argparse `--project_dir` main, `print()` feedback only, type hints on the public entry point only, no docstrings. Bracket the run with `snapshot_raw` / `raw_snapshot_diff` and print the `data/raw/ integrity PASS` line exactly as `run_diagnostic` does.

    Compute five measurement groups for EACH track in `TRACK_IDS`, under one uniform code path with no per-track special casing:

    1. `raw_row_median_profile(Z_mm)` — `np.nanmedian(Z_mm, axis=1)` on the RAW, undetrended map from `load_wyko_asc(height_dir, track_id, crop_to_common=True)`. Report min, max, span (max minus min), the y-index of the argmax, whether that argmax lies within 5 percent of either y-strip edge, and the overall finite fraction of `Z_mm`. This is the discriminator between "the bead is physically at the strip edge in the raw data" and "the bead is interior in the raw data".

    2. `production_residual_profile(...)` — run the exact shipped path: `fit_mask = bead_exclusion_mask(Z_mm)`, then `Zd, coef = robust_plane_detrend(Z_mm, x_actual_mm, y_mm, order=DETREND_POLY_ORDER, fit_mask=fit_mask)` (remember the tuple return). Report the aggregate residual row profile `np.nanmedian(Zd, axis=1)`: peak value, peak y-index, the extent of the contiguous above-half-max run containing that peak, and boolean flags for whether that run touches y index 0 or the final y index.

    3. `fitted_surface_edge_report(Z_mm, Zd)` — the fitted background surface is `Z_mm - Zd`. Report its row-median profile value at y index 0, at the mid index, and at the final index, plus the profile's total span. This is the direct test of whether the fitted surface diverges at the strip edge.

    4. `rejection_reason_histogram(Zd, x_actual_mm, y_mm)` — walk `target_grid()` exactly as `extract_targets_from_arrays` does, including the `MAX_TRACKING_GAP_COLUMNS` stale-history reset and the `previous_center` update, but attribute each of the 400 bins to exactly one of `no_columns` (`bin_profile` returned None), `gap_fail` (`fill_small_gaps` returned ok False), `no_baseline_sep` (rejection on `MIN_VALID_Y_POINTS` or `MIN_PEAK_BASELINE_SEPARATION_MM`), `clipped_run_only` (every above-threshold run touched a y boundary so the candidate list was empty), `crossed` (the computed upper boundary was not strictly above the lower), and `ok`. Distinguishing these requires reimplementing `halfmax_edges`' rejection branches locally inside the diagnostic; do that, and add a comment noting it mirrors `src/targets.py` and must be kept in step with it. Do NOT modify `src/targets.py` to expose reasons — Task 1 must not touch extraction source.

    5. `design_matrix_condition(x_actual_mm, y_mm, order)` — build the same strided, centered monomial design matrix `robust_plane_detrend` builds at `DETREND_POLY_ORDER`, and report `np.linalg.cond(A)` and the ratio of the largest to smallest column L2 norm, then report both numbers again after dividing each centered coordinate by its half-span. This quantifies whether the unscaled basis is ill-conditioned enough for `lstsq(..., rcond=None)` to truncate the cross-track structure terms.

    Write all measurements to `processed_data/diagnostics/track10_coverage_diagnosis.csv` through `resolve_output_path` (one row per track, one column per measurement) and print the table.

    That output path is load-bearing: `processed_data/diagnostics/` is a SIBLING of `processed_data/targets/`, not a child of it. Do NOT write under `processed_data/targets/`. `run_target_extraction.publish_staging_dir` (`scripts/run_target_extraction.py:294-304`) renames the whole `targets/` tree aside to `targets.previous`, renames the staging tree into its place, and then `rmtree`s the backup — so a CSV written under `targets/` would be silently destroyed by Task 4's own regeneration run, before anyone could review it. The sibling path also falls outside the `/processed_data/targets/` entry in `.gitignore`, so the CSV is committable evidence rather than an ignored file. Create the directory with `mkdir(parents=True, exist_ok=True)` — plan 01-09 may have created it already for `width_regression_sweep.csv` — and validate both the directory and the CSV path through `resolve_output_path` exactly once each, following the corrected `run_diagnostic` shape that plan 01-09 leaves behind.

    HARD CONSTRAINT — no fix-outcome observation. This script must NOT compute, print, or persist a median width, a mean width, an ordering verdict, or a valid fraction for ANY candidate fix variant. It may report the CURRENT production per-bin `ok` count, because that is the diagnosed defect itself rather than a fix outcome, and it may report conditioning numbers for a normalized basis, because that is a property of the basis rather than of any extraction result. It must not evaluate what any candidate change would do to width or ordering. Decision U-01 requires the selection criterion to be pre-registered before any ordering result is observed; this is where that discipline is enforced.

    Run the script, then write `.planning/phases/01-target-extraction-contract/01-11-DIAGNOSIS.md` reporting the measured table verbatim followed by a root-cause reading that answers one question explicitly: is track 10's bead interior in the RAW data — making the edge-located post-detrend ridge a fitting artifact, therefore recoverable by a uniform change to how the background surface is fitted — or is it genuinely at or beyond the y-window in the raw data, therefore not recoverable by any detrend change? State whichever the measurements support. If they support the non-recoverable reading, say so plainly and state that Task 3 will apply no fix and Task 4 will escalate; do not force the recoverable conclusion. Record whether these committed measurements confirm or contradict the pre-planning probe that suggested the artifact reading.
  </action>
  <verify>
    <automated>.venv/bin/python -m py_compile scripts/diagnose_track10_coverage.py && .venv/bin/python scripts/diagnose_track10_coverage.py --project_dir . && test -f processed_data/diagnostics/track10_coverage_diagnosis.csv && .venv/bin/python -c "from pathlib import Path; c=Path('processed_data/diagnostics/track10_coverage_diagnosis.csv').resolve(); t=Path('processed_data/targets').resolve(); assert not c.is_relative_to(t), c; print('DIAGNOSTICS OUTSIDE PUBLISHED TREE')" && test -f .planning/phases/01-target-extraction-contract/01-11-DIAGNOSIS.md && test -z "$(git status --porcelain data/raw)" && test -z "$(git diff --stat -- src/targets.py src/nsf_fmrg_data.py)" && echo "DIAGNOSTIC OK, EXTRACTION SOURCE UNTOUCHED"</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/diagnose_track10_coverage.py` exists, compiles, and runs to completion printing `data/raw/ integrity PASS`.
    - `processed_data/diagnostics/track10_coverage_diagnosis.csv` contains one row for each of tracks 8, 10, 14, 21 with columns covering all five measurement groups.
    - The CSV's resolved path is NOT inside `processed_data/targets/`, proven by the `is_relative_to` assertion in the verify command printing `DIAGNOSTICS OUTSIDE PUBLISHED TREE` and exiting 0 — the evidence artifact is outside the tree `publish_staging_dir` renames and deletes.
    - The reproduced per-bin histogram for track 10 accounts for all 400 bins and its `ok` count equals the 21 recorded in `01-08-ORDERING-OUTCOME.md`; tracks 8, 14, 21 report `ok` counts of 359, 301, 324 respectively, or the diagnosis explains any discrepancy.
    - `grep -Ein "median_width|ordering|verdict" scripts/diagnose_track10_coverage.py` finds no width-median or ordering-verdict computation — the diagnostic observes no fix outcome.
    - `01-11-DIAGNOSIS.md` states in one explicit sentence whether track 10's bead is interior or edge-located in the RAW undetrended data, cites the measured raw argmax y-index and span for all four tracks, and names which of the two readings the evidence supports.
    - `git diff --stat -- src/targets.py src/nsf_fmrg_data.py` is empty at this task's commit — no extraction source was touched during diagnosis.
    - `git status --porcelain data/raw` is empty.
  </acceptance_criteria>
  <done>The coverage collapse is characterized by committed, re-runnable code rather than an unreviewable one-off probe, and the recoverable-versus-not question is answered on measured evidence.</done>
</task>

<task type="auto">
  <name>Task 2: Pre-register the outcome-independent fix-selection criterion and commit it before any source change</name>
  <files>.planning/phases/01-target-extraction-contract/01-11-CRITERION.md</files>
  <read_first>
    - .planning/phases/01-target-extraction-contract/01-11-DIAGNOSIS.md (Task 1's output — the measurements this criterion is written against)
    - .planning/phases/01-target-extraction-contract/01-06-DIAGNOSIS.md (Section 3: the exact form and discipline of the previous pre-registered criterion, including its "not acceptable merely because it makes the ordering pass" clause and its ordered fallback conditions — mirror this structure)
    - .planning/phases/01-target-extraction-contract/01-CONTEXT.md (Amendments A3 and A4: the precedent that a parameter is fixed a priori from measured evidence, never from the resulting ordering)
    - src/nsf_fmrg_data.py (`robust_plane_detrend` 205-251: the two concrete intervention points a candidate can use — the `exponents` list construction at 220-224, and the centered-but-unscaled coordinate construction at 225-232 feeding `np.linalg.lstsq(..., rcond=None)` at 237)
  </read_first>
  <action>
    Write `.planning/phases/01-target-extraction-contract/01-11-CRITERION.md` and commit it to git BEFORE Task 3 begins. At the moment that commit is created, `git status --porcelain src/targets.py src/nsf_fmrg_data.py` and `git diff HEAD --stat -- src/targets.py src/nsf_fmrg_data.py` must both be empty — no extraction source change may exist yet, staged or unstaged. This ordering is the structural enforcement of the no-outcome-driven-tuning prohibition that the 01-06 / 01-07 plan boundary previously provided.

    The document must contain, in this order:

    1. The criterion itself, stated as a falsifiable residual, physical, and numerical test in the style of 01-06-DIAGNOSIS.md Section 3. It must be a property of the fitted background surface and the raw data, never a property of the resulting width, valid fraction, or ordering. A criterion of the required shape: the fitted background surface must reproduce the raw substrate's own low-frequency cross-track structure across the full y extent without manufacturing a height feature at the y-strip edge that the raw data does not contain — measured by comparing the fitted surface's row-median profile against the raw row-median profile at both strip edges, on all four tracks, under one shared constant. Name a concrete numeric tolerance.

    2. An explicit rejection clause: a candidate is NOT acceptable merely because it restores 8 greater than 10 greater than 14 greater than 21, merely because it raises track 10's valid fraction, or merely because a QA plot looks better. Outcome-shopping among candidate variants is prohibited. Restate that the boundary-clipped-run exclusion in `halfmax_edges` is correct per D-01/D-03 and that relaxing it is not an admissible candidate, because that removes the symptom rather than the cause.

    3. The candidate mechanisms under consideration, both uniform and track-independent, each with the falsifiable measurement that decides for or against it:
       - Basis conditioning. `robust_plane_detrend` centers x and y but does not scale them, so at `DETREND_POLY_ORDER = 4` the design-matrix columns span roughly x-to-the-fourth on the order of 2.5e6 against y-to-the-fourth on the order of 1, and `np.linalg.lstsq(..., rcond=None)` truncates against that spread. Deciding measurement: Task 1's `design_matrix_condition` numbers, plus whether normalizing each centered coordinate by its half-span removes the manufactured edge feature on all four tracks. Constant of record `DETREND_NORMALIZE_BASIS`; keyword `normalize_basis` on `robust_plane_detrend`, defaulting to today's behavior.
       - Per-axis degree cap. The mapped strip is roughly 80 mm along-track against 1.91 mm cross-track, so a total-degree-4 basis carries a degree-4 cross-track polynomial over a strip 42 times shorter — the classic setting for Runge-type divergence at the strip edge. Deciding measurement: whether capping the cross-track exponent removes the manufactured edge feature on all four tracks while preserving the along-track bow removal Amendment A3 established. Constant of record `DETREND_MAX_Y_DEGREE`; keyword `max_y_degree` on `robust_plane_detrend`, defaulting to today's behavior.

    4. The evidence that would REJECT each candidate, stated concretely — for example, the candidate leaves the fitted surface's edge value departing from the raw substrate's edge value by more than the named tolerance, or the candidate re-introduces on tracks 8 and 14 the global bow that Amendment A3 was adopted to remove.

    5. The non-recoverable branch. State the evidence that would establish track 10's bead is genuinely truncated by the profilometer y-window — principally, a raw undetrended row-median argmax at or adjacent to a y-strip edge with no interior maximum. If Task 1's diagnosis reports that evidence, Task 3 applies NO fix, introduces no constant, writes no Amendment A5, and Task 4 reports the unchanged outcome and escalates to the human sign-off handoff in plan 01-12.

    6. The tie-break rule, fixed now: if both candidates satisfy the criterion, prefer the one that changes the fitted model less — the conditioning fix, since it alters no basis term and is a pure numerical correction — and record the reason. Under no circumstances is the tie broken by comparing resulting widths, valid fractions, or ordering.

    Implement nothing in this task. Do not edit `src/`. Do not run the extractor.
  </action>
  <verify>
    <automated>test -f .planning/phases/01-target-extraction-contract/01-11-CRITERION.md && test -z "$(git status --porcelain src/targets.py src/nsf_fmrg_data.py)" && test -z "$(git diff HEAD --stat -- src/targets.py src/nsf_fmrg_data.py)" && grep -qi "reject" .planning/phases/01-target-extraction-contract/01-11-CRITERION.md && grep -q "DETREND_NORMALIZE_BASIS" .planning/phases/01-target-extraction-contract/01-11-CRITERION.md && grep -q "DETREND_MAX_Y_DEGREE" .planning/phases/01-target-extraction-contract/01-11-CRITERION.md && echo "CRITERION PRE-REGISTERED, EXTRACTION SOURCE CLEAN"</automated>
  </verify>
  <acceptance_criteria>
    - `01-11-CRITERION.md` exists and is committed; `git log --oneline -1 -- .planning/phases/01-target-extraction-contract/01-11-CRITERION.md` returns a commit.
    - At that commit, `git status --porcelain src/targets.py src/nsf_fmrg_data.py` and `git diff HEAD --stat -- src/targets.py src/nsf_fmrg_data.py` are both empty — the criterion is committed strictly before any extraction source change exists.
    - The criterion is stated as a property of the fitted surface versus the raw substrate, with a named numeric tolerance, and contains an explicit clause rejecting selection on the basis of width ordering, valid fraction, or QA appearance.
    - Both candidate mechanisms are named with their constant of record (`DETREND_NORMALIZE_BASIS`, `DETREND_MAX_Y_DEGREE`), their `robust_plane_detrend` keyword, and their deciding measurement.
    - A concrete rejection condition is stated for each candidate, and the non-recoverable branch and the tie-break rule are both written down before any fix is applied.
    - `.venv/bin/python tests/test_targets.py` and `.venv/bin/python tests/test_nsf_fmrg_data.py` still report every test PASS (nothing was implemented).
  </acceptance_criteria>
  <done>An outcome-independent selection criterion exists in git history before any extraction source change, so the fix cannot have been chosen by its effect on the ordering.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Apply exactly ONE uniform track-independent fix, register its constant, and record Amendment A5</name>
  <files>src/nsf_fmrg_data.py, src/targets.py, scripts/check_targets.py, tests/test_nsf_fmrg_data.py, tests/test_targets.py, .planning/phases/01-target-extraction-contract/01-CONTEXT.md</files>
  <read_first>
    - .planning/phases/01-target-extraction-contract/01-11-CRITERION.md (Task 2's committed criterion — this task implements exactly what it selects and nothing else)
    - .planning/phases/01-target-extraction-contract/01-11-DIAGNOSIS.md (Task 1's measurements)
    - src/nsf_fmrg_data.py (`robust_plane_detrend` 205-251: the `fit_mask` striding at 212-214, the `<100`-sample degenerate fallback at 215-216, the order validation at 218-219, the `exponents` construction at 220-224, the centered coordinates at 225-232, the three-pass percentile-trimmed `lstsq` loop at 234-244, and the full-resolution surface evaluation at 245-251 which must use the SAME transform as the fit)
    - src/targets.py (constants block 7-32 with `DETREND_POLY_ORDER` and `BEAD_MASK_HEIGHT_FRACTION` and their Amendment comments; `extraction_params()` 39-56 returning 15 keys today; `extract_track_targets` 286-314 where the detrend is called)
    - tests/test_nsf_fmrg_data.py (`synthetic_grid` helper, `test_order_four_removes_quartic_bow`, `test_degenerate_fallback_is_preserved_for_all_orders`, `test_polynomial_basis_sizes_are_stable`, `test_robust_plane_detrend_fit_mask_excludes_bead_from_fit`, and the `__main__` runner list)
    - tests/test_targets.py (`test_extraction_params_provenance`, `test_provenance_includes_tracking_gap_and_fix_param`, `test_provenance_digest_is_change_sensitive`, `test_bead_mask_rule_is_track_independent`, `test_single_parameterization_has_no_track_conditionals`, and the `__main__` runner list)
    - .planning/phases/01-target-extraction-contract/01-CONTEXT.md (Amendment A3 at lines 40-44 and Amendment A4 at 46-54 — match their heading style, placement inside the decisions section, and prose discipline)
    - .planning/phases/01-target-extraction-contract/01-REVIEW.md (IN-01, the two undocumented constants this task also annotates)
    - scripts/check_targets.py (line 24: the module-level `Y_STRIP_EXTENT_MM = 1.907`, sitting in the SCREAMING_SNAKE_CASE constants block alongside `EXPECTED_KEYS` / `EXPECTED_SHAPE`)
    - src/nsf_fmrg_data.py (line 13: the module-level `EXTRACTED_THERMAL_FRAMES` definition with its inline derivation from `COMMON_X_*` and `THERMAL_MM_PER_FRAME`; and line 68 inside `find_thermal_array`, where the same value 400 appears as a bare literal in the scoring heuristic)
    - ./.claude/CLAUDE.md (the module convention: `src/nsf_fmrg_data.py` function bodies carry NO inline comments, units live in variable names, and derivation commentary belongs on the module-level constant definitions)
  </read_first>
  <behavior>
    - Synthetic map shaped like the real data (long along-track extent, short cross-track strip, ~480 cross-track samples): a smooth low-order substrate warp with a span comparable to track 10's measured raw span, plus an interior elevated bead well clear of both strip edges. Under today's `order=4` masked detrend the fitted surface manufactures a height feature at the strip edge that the synthetic substrate does not contain; under the selected fix it does not, within the tolerance named in `01-11-CRITERION.md`.
    - The fix is off by default: calling `robust_plane_detrend` without the new keyword reproduces today's output bit-for-bit, so the `order=1` affine path, the notebooks, and `scripts/diagnose_width_regression.py`'s historical rows are unchanged.
    - One shared constant handles two synthetic tracks whose substrate warps differ by roughly 40x in span — the same value fixes both, proving the rule is relative rather than tuned to one track's magnitude.
    - `extraction_params()` returns 16 keys including the new constant, and mutating that constant changes the SHA-256 digest of `json.dumps(extraction_params(), sort_keys=True)`.
  </behavior>
  <action>
    Implement exactly ONE uniform, track-independent change — whichever `01-11-CRITERION.md` selects. Do not implement both. Do not implement a mechanism the criterion did not name.

    If the criterion selects basis conditioning: add a `normalize_basis=False` keyword to `robust_plane_detrend`, placed after `fit_mask`. When true, divide each centered coordinate by its half-span before forming the monomial basis, and apply the identical scaling when evaluating the fitted surface at full resolution so the subtracted surface is unchanged in meaning. Guard against a zero half-span. Add `DETREND_NORMALIZE_BASIS = True` to the `src/targets.py` constants block with a comment recording that it was fixed from the Task 2 criterion and not from the resulting ordering, and pass it from `extract_track_targets` into the existing `robust_plane_detrend` call alongside `order=DETREND_POLY_ORDER` and `fit_mask=fit_mask`.

    If the criterion selects the per-axis degree cap: add a `max_y_degree=None` keyword to `robust_plane_detrend`, placed after `fit_mask`. When not None, filter the `exponents` list to entries whose cross-track exponent does not exceed it, keeping total-degree construction otherwise identical. Validate it as a non-negative integer using the same style as the existing `order` validation. Add `DETREND_MAX_Y_DEGREE` to the `src/targets.py` constants block with the value the criterion's deciding measurement supports and the same style of comment, and pass it from `extract_track_targets`.

    In either case: preserve the `<100`-valid-sample degenerate fallback returning `(Z_mm.copy(), None)`, preserve the three-pass percentile trimming, preserve `stride_x=40, stride_y=2`, preserve the existing `order` validation, and keep the default equal to today's behavior so no other caller changes. Follow module conventions — no docstrings, no type hints in library code, SCREAMING_SNAKE_CASE constants with an inline derivation comment.

    Add the new constant to `extraction_params()` in `src/targets.py`, bringing it to 16 keys. Update `test_extraction_params_provenance` to the new complete key set and correct its assertion wording to the actual count. Extend `test_provenance_digest_is_change_sensitive` to also mutate the new constant and assert the digest changes, restoring the original value in a `finally` block. Extend `test_polynomial_basis_sizes_are_stable` rather than replacing it.

    Add the RED-then-GREEN regressions from `<behavior>`: `test_detrend_does_not_diverge_at_strip_edge` and `test_detrend_edge_fix_preserves_default_behavior` in `tests/test_nsf_fmrg_data.py`, and `test_edge_divergence_fix_is_track_independent` in `tests/test_targets.py`. Append each to its module's `__main__` runner list.

    Introduce no per-track conditional anywhere. `tests/test_targets.py::test_single_parameterization_has_no_track_conditionals` must stay green.

    Then append Amendment A5 to the decisions section of `01-CONTEXT.md`, immediately after Amendment A4, matching A3/A4's heading style and prose discipline. A5 must: date itself; name the diagnosed defect (track 10's boundary-clipped-run collapse and its measured cause); name the ONE mechanism adopted and its constant, with the value and the measured evidence that fixed it a priori; state that the constant was fixed from the Task 2 pre-registered residual/physical/numerical criterion and not from the resulting width ordering or valid fractions; state that it applies identically to all four tracks with no per-track mask geometry or per-track constant; state that it supersedes nothing in A3 or A4; and reaffirm that neither this constant nor any other may be adjusted in response to whether the ordering passes.

    Separately, apply review item IN-01 from `01-REVIEW.md` — two comment-only derivation annotations, no behavior change, no constant value changed. This is folded here because this is the task that already edits the locked-constants blocks; it is not part of the ONE uniform fix and is not covered by Amendment A5.

    First, in `scripts/check_targets.py`, annotate the module-level `Y_STRIP_EXTENT_MM = 1.907` with a one-line inline derivation stating that it sits deliberately just below the height maps' measured cross-track extent (`y_size * pixel_size_mm`) to leave interpolation margin, and giving that measured extent. Read the measured value out of the four Wyko headers yourself before writing it rather than copying a number from the review, so the comment records a verified fact. A reader must be able to tell from the line alone that 1.907 is a deliberate margin, not a stale rounding.

    Second, in `src/nsf_fmrg_data.py`, annotate the module-level `EXTRACTED_THERMAL_FRAMES` definition to record that `find_thermal_array`'s array-scoring heuristic biases toward candidate arrays whose shape already carries this same frame count, so a future change to the scan-geometry constants that shifts this value is understood to shift that heuristic's intent too. Put the comment ONLY on the module-level constant definition — do NOT add a comment inside `find_thermal_array`'s body, and do NOT restructure the scoring expression. `src/nsf_fmrg_data.py` function bodies carry no inline comments by project convention, and module-level constants are exactly where that convention already permits inline derivation.

    Change no value, no signature, and no control flow in either file for IN-01. It is annotation only.

    NON-RECOVERABLE BRANCH: if `01-11-DIAGNOSIS.md` concluded track 10's bead is genuinely truncated by the y-window, apply NO fix. Introduce no constant, change no extraction behavior, write no Amendment A5. The IN-01 comment-only annotations above still apply in this branch — they are documentation of existing constants, independent of whether a fix is adopted. Record the non-recoverable outcome in the plan summary and proceed directly to Task 4, which will regenerate against the unchanged contract and escalate. Do not substitute a different fix to avoid an empty diff.
  </action>
  <verify>
    <automated>.venv/bin/python tests/test_nsf_fmrg_data.py && .venv/bin/python tests/test_targets.py && .venv/bin/python -O tests/test_nsf_fmrg_data.py && .venv/bin/python -O tests/test_targets.py && .venv/bin/python -c "import sys,json,hashlib; sys.path.insert(0,'src'); import targets; p=targets.extraction_params(); print('keys',len(p)); base=hashlib.sha256(json.dumps(p,sort_keys=True).encode()).hexdigest(); assert len(p)==16, p" && test -z "$(grep -n 'track_id ==' src/targets.py src/nsf_fmrg_data.py scripts/run_target_extraction.py)" && .venv/bin/python -c "import sys; sys.path[:0]=['src','scripts']; import check_targets as c, nsf_fmrg_data as n; assert c.Y_STRIP_EXTENT_MM == 1.907, c.Y_STRIP_EXTENT_MM; assert n.EXTRACTED_THERMAL_FRAMES == 400, n.EXTRACTED_THERMAL_FRAMES; print('IN-01 ANNOTATION BEHAVIOR-NEUTRAL')" && grep -q "Amendment A5" .planning/phases/01-target-extraction-contract/01-CONTEXT.md && echo "UNIFORM FIX + A5 OK"</automated>
  </verify>
  <acceptance_criteria>
    - Exactly one of `DETREND_NORMALIZE_BASIS` or `DETREND_MAX_Y_DEGREE` exists in `src/targets.py`; `grep -c "DETREND_NORMALIZE_BASIS\|DETREND_MAX_Y_DEGREE" src/targets.py` shows only the selected one, and it matches the mechanism named in `01-11-CRITERION.md`.
    - `robust_plane_detrend` gained exactly one new keyword, positioned after `fit_mask`, whose default reproduces today's behavior — proven by `test_detrend_edge_fix_preserves_default_behavior` and by the four pre-existing detrend tests still passing.
    - `extraction_params()` returns 16 keys including the new constant; the assertion command above exits 0.
    - `test_provenance_digest_is_change_sensitive` covers the new constant: mutating it changes the SHA-256 digest.
    - `test_detrend_does_not_diverge_at_strip_edge` and `test_edge_divergence_fix_is_track_independent` both pass and both fail if the new keyword is reverted to its default.
    - `grep -n "track_id ==" src/targets.py src/nsf_fmrg_data.py scripts/run_target_extraction.py` returns no matches, and `test_single_parameterization_has_no_track_conditionals` is green.
    - `01-CONTEXT.md` contains a dated Amendment A5 in the decisions section, immediately after A4, naming the mechanism, its constant and value, the a-priori evidence, and the reaffirmed no-outcome-driven-tuning prohibition.
    - All three test suites report every test PASS under both `.venv/bin/python` and `.venv/bin/python -O`.
    - `git status --porcelain data/raw` is empty.
    - IN-01 is applied: the line defining `Y_STRIP_EXTENT_MM` in `scripts/check_targets.py` carries an inline derivation naming the measured cross-track extent, and the line defining `EXTRACTED_THERMAL_FRAMES` in `src/nsf_fmrg_data.py` carries an inline note tying it to `find_thermal_array`'s shape bias.
    - IN-01 changed no behavior: `Y_STRIP_EXTENT_MM` is still `1.907`, `EXTRACTED_THERMAL_FRAMES` still evaluates to 400, `find_thermal_array`'s scoring expression is unchanged, and no comment was added inside any `src/nsf_fmrg_data.py` function body.
    - If the non-recoverable branch was taken instead, the only changes to `src/targets.py` and `src/nsf_fmrg_data.py` in this task's commit are the IN-01 comment lines — no constant was added, no signature changed, no Amendment A5 exists — and the summary records that conclusion explicitly.
  </acceptance_criteria>
  <done>One uniform, track-independent correction selected by the pre-registered criterion is implemented, provenance-locked, and canonicalized as Amendment A5 — or the non-recoverable conclusion is recorded with no source change at all.</done>
</task>

<task type="auto">
  <name>Task 4: Regenerate all 4 tracks atomically and report the ordering outcome honestly</name>
  <files>.planning/phases/01-target-extraction-contract/01-11-ORDERING-OUTCOME.md</files>
  <read_first>
    - scripts/run_target_extraction.py (`run_pipeline` 309-412: the staged-then-atomically-published flow, the `snapshot_raw` bracketing, and `publish_staging_dir` — one invocation regenerates all four tracks, 12 QA PNGs, `extraction_params.json`, and `manifest.json` under one run id)
    - scripts/check_targets.py (the `MIN_VALID_FRACTION` gate installed by plan 01-10, and `main()`'s structural, provenance, and manifest assertions)
    - .planning/phases/01-target-extraction-contract/01-08-ORDERING-OUTCOME.md (the exact report structure and HONEST-OUTCOME discipline to mirror: verbatim output, verdict, material context, escalation, and a "what this plan did NOT do" section)
    - .planning/phases/01-target-extraction-contract/01-11-CRITERION.md and 01-11-DIAGNOSIS.md (what was predicted and why)
    - src/targets.py (`print_results` 59-77 — the summary table and per-pair ordering verdicts printed by both the runner and the checker)
  </read_first>
  <action>
    Run `.venv/bin/python scripts/run_target_extraction.py --project_dir .` exactly once. This is a single atomic staged publish: all four tracks, 12 QA PNGs, `extraction_params.json`, and `manifest.json` come from one run id, so no mixed generation can be published. Expect several minutes of runtime.

    Then run `.venv/bin/python scripts/check_targets.py --project_dir .` and capture both its full output and its exit code verbatim. Do not suppress a non-zero exit and do not retry with modified inputs.

    Write `.planning/phases/01-target-extraction-contract/01-11-ORDERING-OUTCOME.md` mirroring `01-08-ORDERING-OUTCOME.md`'s structure: the extraction contract in effect (naming Amendment A5 and its constant, or recording that no fix was applied); the verbatim regenerated summary table and the three adjacent-pair ordering verdicts; the verbatim `check_targets.py` output and its exit code; each track's valid-bin count and fraction against the 50 percent `MIN_VALID_FRACTION` floor; a plain verdict on whether 8 greater than 10 greater than 14 greater than 21 holds; and a "what this task did NOT do" section.

    HONEST-OUTCOME GUARD — non-negotiable. If the ordering still FLAGs, or any track remains below the coverage floor, or `check_targets.py` exits non-zero: do NOT change `DETREND_POLY_ORDER`, `BEAD_MASK_HEIGHT_FRACTION`, `MAX_TRACKING_GAP_COLUMNS`, `MIN_VALID_FRACTION`, the new Amendment A5 constant, or any other locked value. Do NOT relax the boundary-clipped-run exclusion in `halfmax_edges`. Do NOT add a per-track branch. Do NOT re-run the extractor with a different constant to see whether the outcome improves — that is exactly the outcome-shopping the Task 2 criterion prohibits. Report the result and escalate to the human decision in plan 01-12.

    Do not re-run the extractor more than once for the purpose of comparing outcomes. A second run is permissible only to recover from an infrastructure failure (for example an interrupted write), and any such re-run must be disclosed in the report.

    Confirm and record that `data/raw/ integrity PASS` was printed by the run, that 12 PNGs exist under `processed_data/targets/qa/`, and that `git diff --stat -- src/targets.py src/nsf_fmrg_data.py scripts/check_targets.py` is empty for this task's commit.

    Also confirm that `processed_data/diagnostics/track10_coverage_diagnosis.csv` still exists after the publish completes. `run_pipeline` publishes through `publish_staging_dir`, which renames `processed_data/targets/` aside and then `rmtree`s the backup; the diagnostic CSV survives only because Task 1 wrote it to a sibling directory. If a future change re-buries diagnostics under `targets/`, this check is what catches it — report the missing file rather than re-running the diagnostic to recreate it.
  </action>
  <verify>
    <automated>.venv/bin/python scripts/run_target_extraction.py --project_dir . && test $(ls processed_data/targets/qa/*.png | wc -l) -eq 12 && test -f processed_data/targets/manifest.json && test -f processed_data/targets/extraction_params.json && for t in 8 10 14 21; do test -f processed_data/targets/track_${t}_targets.npz || exit 1; done && test -f processed_data/diagnostics/track10_coverage_diagnosis.csv && .venv/bin/python scripts/check_targets.py --project_dir . > /tmp/check_targets_final.txt 2>&1; echo "check_targets exit=$?"; cat /tmp/check_targets_final.txt; test -f processed_data/diagnostics/track10_coverage_diagnosis.csv && test -f .planning/phases/01-target-extraction-contract/01-11-ORDERING-OUTCOME.md && test -z "$(git diff --stat -- src/targets.py src/nsf_fmrg_data.py scripts/check_targets.py)" && test -z "$(git status --porcelain data/raw)" && echo "REGENERATED AND REPORTED"</automated>
  </verify>
  <acceptance_criteria>
    - A single `run_target_extraction.py` invocation completed, printed `data/raw/ integrity PASS`, and published four `track_{8,10,14,21}_targets.npz` files, 12 QA PNGs, `extraction_params.json`, and `manifest.json` sharing one run id.
    - `processed_data/diagnostics/track10_coverage_diagnosis.csv` still exists AFTER the extraction publish completes — `publish_staging_dir`'s rename-and-rmtree of `processed_data/targets/` did not destroy Task 1's evidence artifact. This is the regression guard against a future change re-burying diagnostics under the published tree.
    - `check_targets.py`'s full output and exit code are recorded verbatim in `01-11-ORDERING-OUTCOME.md`, whatever they are.
    - `01-11-ORDERING-OUTCOME.md` states plainly whether 8 greater than 10 greater than 14 greater than 21 holds, lists all four valid fractions against the 50 percent floor, and names the contract in effect.
    - `git diff --stat -- src/targets.py src/nsf_fmrg_data.py scripts/check_targets.py` is empty at this task's commit — no constant was changed in response to the observed outcome.
    - `grep -n "track_id ==" src/targets.py src/nsf_fmrg_data.py scripts/run_target_extraction.py` returns no matches.
    - `git status --porcelain data/raw` is empty; `snapshot_raw` reported integrity PASS.
    - If the outcome is unfavourable, the report escalates rather than proposing a constant change, and includes the "what this task did NOT do" section.
  </acceptance_criteria>
  <done>All four tracks are regenerated from one atomic run under the current contract, and the ordering and coverage outcome is on record verbatim — tuned in neither direction.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| filesystem read of `data/raw/` -> in-memory arrays | Diagnostics and the extractor read irreplaceable, competition-restricted raw height maps; nothing may write, delete, or modify under `data/raw/`. |
| extraction constants -> persisted ground truth | A constant changed outside `extraction_params()` produces artifacts whose provenance digest no longer describes how they were made. |
| observed outcome -> constant selection | The width ordering and valid fractions are the exact signals that must NOT feed back into constant selection; the task ordering is the control. |
| staging directory -> published `processed_data/targets/` | The atomic publish deletes and renames directories; plan 01-10's symlink guards must still hold during this plan's regeneration run. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-24 | Tampering | no-tuning integrity (constant selection) | high | mitigate | Task 2's criterion is committed to git while `git diff HEAD --stat -- src/targets.py src/nsf_fmrg_data.py` is empty; Task 1's diagnostic is forbidden from computing any width median or ordering verdict; Task 4's commit must leave the extraction diff empty. |
| T-01-25 | Repudiation | provenance completeness | high | mitigate | The new constant is added to `extraction_params()` (16 keys), covered by `test_provenance_digest_is_change_sensitive`, persisted to `extraction_params.json`, and digested into `manifest.json`; Amendment A5 records it in `01-CONTEXT.md`. |
| T-01-26 | Tampering (accidental) | `data/raw/` | high | mitigate | Both the diagnostic and the extractor bracket their runs with `snapshot_raw` / `raw_snapshot_diff`; every task asserts `git status --porcelain data/raw` is empty. |
| T-01-27 | Tampering | atomic publish during regeneration | high | mitigate | Regeneration runs through `publish_staging_dir` with plan 01-10's symlink guards in place; `depends_on: [01-10]` makes that a hard ordering requirement, so this plan cannot regenerate through the CR-03-vulnerable publisher. |
| T-01-28 | Spoofing | per-track file resolution during regeneration | medium | mitigate | `extract_track_targets`'s exact `Heightmap_{track_id}.ASC` guard, plus plan 01-09's tightened `find_track_file`, ensure each regenerated artifact came from its own track's file; the runner prints each resolved source path. |
| T-01-29 | Denial of service | single long extraction run | low | accept | The full four-track run takes several minutes and exceeds the 60-second feedback target; this is the already-accepted one-time full-pipeline cost recorded in `01-VALIDATION.md`. |
| T-01-SC | Tampering | npm/pip/cargo installs | high | mitigate | No packages are installed by this plan; `requirements.txt` is unchanged. No `[ASSUMED]`/`[SUS]` package is introduced, so no legitimacy checkpoint is required. |
</threat_model>

<verification>
- `.venv/bin/python -m py_compile src/*.py scripts/*.py tests/*.py` exits 0.
- All three suites report every test PASS under both `.venv/bin/python` and `.venv/bin/python -O`, including the three new regressions.
- `extraction_params()` returns 16 keys; mutating the new constant changes the SHA-256 digest; `processed_data/targets/extraction_params.json` matches the code constants and `manifest.json`'s digest matches it.
- `grep -n "track_id ==" src/targets.py src/nsf_fmrg_data.py scripts/run_target_extraction.py` returns no matches.
- `01-CONTEXT.md` contains a dated Amendment A5 (or the summary records the non-recoverable branch and no A5 exists).
- `01-11-DIAGNOSIS.md`, `01-11-CRITERION.md`, and `01-11-ORDERING-OUTCOME.md` all exist; the criterion's commit precedes any extraction-source change.
- Four NPZs, 12 QA PNGs, `extraction_params.json`, and `manifest.json` exist under `processed_data/targets/` from a single run id.
- `processed_data/diagnostics/track10_coverage_diagnosis.csv` exists both before and after Task 4's regeneration — the diagnostic evidence lives outside the tree `publish_staging_dir` replaces.
- Review item IN-01 is applied as comment-only annotations on the two module-level constant definitions, with `Y_STRIP_EXTENT_MM == 1.907` and `EXTRACTED_THERMAL_FRAMES == 400` unchanged.
- `git status --porcelain data/raw` is empty.
</verification>

<success_criteria>
- Track 10's coverage collapse is diagnosed with committed code and the recoverable-versus-not question is answered on measured evidence.
- The fix-selection criterion is in git history before any extraction-source change exists.
- At most one uniform, track-independent change is applied, provenance-locked in `extraction_params()`, and canonicalized as Amendment A5.
- All four tracks are regenerated from one atomic run and the ordering plus coverage outcome is reported verbatim, with no constant changed in response.
- No gate weakened, no per-track branch introduced, `data/raw/` untouched.
</success_criteria>

<output>
Create `.planning/phases/01-target-extraction-contract/01-11-SUMMARY.md` when done.
</output>
