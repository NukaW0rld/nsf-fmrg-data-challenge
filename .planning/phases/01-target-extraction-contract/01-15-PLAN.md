---
phase: 01-target-extraction-contract
plan: 15
type: execute
wave: 14
depends_on: ["01-14"]
files_modified:
  - .planning/phases/01-target-extraction-contract/01-SIGNOFF-REQUEST.md
  - .planning/REQUIREMENTS.md
autonomous: true
gap_closure: true
requirements: [TARGET-01, TARGET-02]

must_haves:
  truths:
    - "A human opening 01-SIGNOFF-REQUEST.md reads the live Amendment-A7 numbers — valid bins 361 / 202 / 301 / 308 and median widths 0.7401 / 0.3770 / 0.6174 / 0.3825 mm for tracks 8 / 10 / 14 / 21 — and finds no Amendment-A5-era number presented anywhere as current state."
    - "The document states track 10's coverage as 202/400 = 50.50% against the hard MIN_VALID_FRACTION = 0.5 floor, and says in words that this clears the floor by only 0.5 percentage points (2 bins) — a razor-thin margin, not a comfortable one."
    - "The document states the current 10-vs-14 absolute median gap as 0.2404 mm and says plainly that the gap roughly DOUBLED under Amendments A6/A7 (from 0.1052 mm at the generation this document previously described) rather than narrowing."
    - "The document presents 01-13-ORDERING-OUTCOME.md's and 01-14-ORDERING-OUTCOME.md's shared recommendation of option (a) — accept as a documented known limitation — immediately alongside the worsened gap number, and selects neither option on the human's behalf."
    - "The document carries 01-14-ORDERING-OUTCOME.md's fragmentation result forward as MIXED, not uniformly improved: contiguous-run counts worsened on tracks 8, 10, and 21, and max adjacent-column jump statistics improved on some boundaries and worsened on others. Nowhere does it imply Amendment A7 fixed fragmentation."
    - "The '## Contract in effect' table names all seven locked amendment constants — DETREND_POLY_ORDER, BEAD_MASK_HEIGHT_FRACTION, MAX_TRACKING_GAP_COLUMNS, DETREND_MAX_Y_DEGREE, DETREND_MAX_XY_DEGREE, MAX_RUN_MERGE_GAP_PIXELS, MIN_TRACKED_LENGTH_RATIO — each with its value and the amendment/evidence it was fixed from."
    - "The document is traceable to the exact artifact generation it describes: it cites run_id 99a4e8472f0a4164938363af0725f31b and the extraction_params_sha256 digest from processed_data/targets/manifest.json."
    - "Every checkbox in 01-SIGNOFF-REQUEST.md remains unticked — this plan requests sign-off and never grants, records, or implies it."
    - "REQUIREMENTS.md's TARGET-02 checkbox and its traceability row tell the same story, with no sign-off invented that has not happened."
    - "The extraction pipeline, its locked constants, its tests, and its published artifacts are unchanged by this plan: check_targets.py still exits 0 printing ALL CHECKS PASSED, and git reports no change under src/, tests/, scripts/, or processed_data/."
    - "[SPEC edge/boundary R1] A per-column peak-baseline separation exactly at MIN_PEAK_BASELINE_SEPARATION_MM is valid (only below invalidates), and exactly 10 consecutive NaNs is still interpolated (only exceeding 10 invalidates) — inherited invariant, unchanged by this plan."
    - "[SPEC edge/adjacency R1] SG smoothing is allowed to blend valid points from both sides of a masked gap (standard savgol_filter NaN-skip behavior) — inherited invariant, unchanged by this plan."
    - "[SPEC edge/empty R1] A track with zero valid x-positions after masking raises a hard error rather than silently producing an all-NaN artifact — inherited invariant, unchanged by this plan."
    - "[SPEC edge/precision R1] Percentile calculation uses numpy's default linear-interpolation convention, and a crossed-boundary case (y_upper < y_lower, negative width) marks that x-column invalid rather than reporting a negative or clipped-to-zero width — inherited invariant, unchanged by this plan."
    - "[SPEC edge/boundary R2] Crop-edge behavior at 20mm and 100mm is D-12's SG edge/interp boundary mode, and remains visually inspectable in the four track_*_width.png figures this document points the reviewer at."
    - "[SPEC edge/adjacency R2] Invalid x-positions keep their grid slot with width=NaN and mask=False (D-07) — no ragged or dropped positions at valid/invalid boundaries — inherited invariant, unchanged by this plan."
    - "[SPEC edge/empty R2] Same as R1: an all-invalid track raises a hard error rather than persisting an all-NaN artifact — inherited invariant, unchanged by this plan."
    - "[SPEC edge/precision R2] Persisted target arrays remain float64 dtype — inherited invariant, unchanged by this plan."
  artifacts:
    - ".planning/phases/01-target-extraction-contract/01-SIGNOFF-REQUEST.md"
    - ".planning/REQUIREMENTS.md"
  key_links:
    - "processed_data/targets/manifest.json + live check_targets.py output -> 01-SIGNOFF-REQUEST.md '## Current state': every number must be re-derived by running the checker in THIS execution, never transcribed from this plan's text or from a prior ORDERING-OUTCOME document."
    - "processed_data/targets/extraction_params.json (19 keys) -> 01-SIGNOFF-REQUEST.md '## Contract in effect': every amendment constant the reviewer is asked to accept must appear in the table."
    - "01-14-ORDERING-OUTCOME.md's MIXED fragmentation verdict -> 01-SIGNOFF-REQUEST.md '## Figures to inspect' and '## Decisions requested': the reviewer must be told the aggregate metrics worsened on several tracks before being asked to judge the overlays."
    - "01-VERIFICATION.md's two open human_verification items -> 01-SIGNOFF-REQUEST.md '## Decisions requested': every decision must name which verification item it closes."
    - "01-SIGNOFF-REQUEST.md -> REQUIREMENTS.md TARGET-02 row: TARGET-02 flips to complete only after a human records sign-off via /gsd-verify-work 1, never as a side effect of this plan."
  prohibitions:
    - statement: "Extraction contract constants (noise-floor threshold, gap limit, percentile split, and every A3-A7 amendment constant) must NOT be adjusted based on whether the resulting width ordering or QA plots look correct. This plan changes no constant and re-runs no extraction."
      status: resolved
      verification: judgment
    - statement: "Extraction and persistence code must NOT write to, delete, or modify any file under data/raw/. This plan writes only to .planning/ and never invokes the extraction pipeline."
      status: resolved
      verification: test
    - statement: "No source, test, or script file may be modified: src/targets.py, src/nsf_fmrg_data.py, scripts/run_target_extraction.py, scripts/check_targets.py, every diagnostic script, and everything under tests/ are read-only this round."
      status: resolved
      verification: test
    - statement: "The extraction pipeline must NOT be re-run. No .npz file, QA PNG, manifest.json, or extraction_params.json may be regenerated. The checked-in artifacts (run_id 99a4e8472f0a4164938363af0725f31b) are the current generation; read-only inspection is required, rewriting is forbidden."
      status: resolved
      verification: test
    - statement: "G-01-4 (10-vs-14 width ordering) and G-01-5 (boundary fragmentation) must NOT be reopened. Both had their one authorized diagnose-and-fix cycle (Amendments A6 and A7). This plan's job is to let a human decide them on accurate information, not to decide, re-litigate, or launch another tuning cycle."
      status: resolved
      verification: judgment
    - statement: "The regenerated document must NOT make the numbers look better. No softening, no rounding in a favorable direction, no omission of a bad number, no framing that implies A6/A7 improved what they measurably worsened."
      status: resolved
      verification: judgment
    - statement: "This plan must NOT self-certify visual QA. No checkbox may be ticked, and no task may record an approval or mark TARGET-02 complete on the reviewer's behalf."
      status: resolved
      verification: test
---

<objective>
Regenerate `.planning/phases/01-target-extraction-contract/01-SIGNOFF-REQUEST.md` so the standing human sign-off document describes the artifact generation that is actually on disk, and reconcile `.planning/REQUIREMENTS.md`'s TARGET-02 checkbox with its own traceability row.

`01-SIGNOFF-REQUEST.md` was last written by plan 01-12 against Amendment A5. Two gap-closure plans have landed since (01-13 / Amendment A6, 01-14 / Amendment A7), each regenerating every QA figure and every persisted target. The document's `## Current state` narrative, its numeric table, and its `## Contract in effect` table all quote Amendment-A5-era figures that no longer match the checked-in artifacts. `01-VERIFICATION.md` records this as the single blocking gap for the phase: a human relying on this document for sign-off would be reviewing a materially inaccurate picture.

Purpose: TARGET-02's own acceptance criterion is a human confirmation of the QA figures. That confirmation is only meaningful if the document driving it is accurate — and specifically if it makes two *worse* facts impossible to miss: track 10's coverage margin above the hard 50% floor is now razor-thin (50.50%), and the 10-vs-14 ordering gap roughly doubled under A6/A7 rather than narrowing.
Output: An accurate, unsigned sign-off request and a self-consistent requirements record. Nothing else — no source, no tests, no constants, no pipeline run.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-target-extraction-contract/01-VERIFICATION.md
@.planning/phases/01-target-extraction-contract/01-SPEC.md
@.planning/phases/01-target-extraction-contract/01-14-ORDERING-OUTCOME.md
@.planning/phases/01-target-extraction-contract/01-13-ORDERING-OUTCOME.md
</context>

<artifacts_produced>
Artifacts this phase produces — every file path this plan creates or modifies:

- **Modified:** `.planning/phases/01-target-extraction-contract/01-SIGNOFF-REQUEST.md` (rewritten in place; section shape preserved, content refreshed to Amendment A7)
- **Modified:** `.planning/REQUIREMENTS.md` (TARGET-02 checkbox + traceability reconciliation, plus one dated correction line)
- **Created:** `.planning/phases/01-target-extraction-contract/01-15-SUMMARY.md` (execution summary)

No new code symbols, constants, CLI flags, tests, source files, or data artifacts. This plan touches only `.planning/`.
</artifacts_produced>

<assumptions>
Explicit assumptions and deltas from what CONTEXT.md and prior plans assumed. Each is flagged rather than left as an unstated premise.

1. **Delta from plan 01-12's premise.** 01-12 wrote that track 10's coverage collapse was "resolved" and its margin comfortable. Under Amendment A7 that premise is falsified: track 10 fell to 202/400 = 50.50%, 0.5 percentage points above the hard floor. This plan replaces the "resolved / comfortable" framing with the razor-thin framing. The old framing is not preserved for continuity.

2. **Delta from plan 01-12's premise.** 01-12's document said the 10-vs-14 gap "has narrowed substantially". Under A6/A7 the gap widened, roughly doubling to 0.2404 mm. This plan reverses that narrative direction.

3. **Interpretation of "Decisions requested" alignment.** `01-VERIFICATION.md` lists two open `human_verification` items, while the existing document has five granular checkboxes. This plan treats the five granular checkboxes as the concrete *questions* that verification item 2 refers to ("answer the 01-SIGNOFF-REQUEST.md questions"), so the section keeps its granular shape and each checkbox explicitly names which verification item it closes. This is an interpretation, not a stated fact in either document.

4. **SPEC dismissed edges surfaced, not dropped.** `01-SPEC.md`'s Edge Coverage reports 8/10 covered, 0 unresolved, and 2 dismissed — both `ordering` edges (R1 and R2), dismissed because the output x-grid order is fixed by construction (D-07) and no data-dependent sort exists in the extraction algorithm. They are recorded here rather than silently omitted from `must_haves.truths`.

5. **Schema-push detection.** This plan introduces no schema, no migration, and no persisted-format change. `extraction_params.json`'s 19-key shape, the `.npz` array layout, and `manifest.json`'s field set are all read-only inputs here.

6. **API coverage.** This phase integrates no external API, SDK, or service — it reads local profilometry artifacts and rewrites markdown. The API Coverage checkpoint does not fire and no `COVERAGE.md` is required.

7. **Live artifacts outrank this plan's text.** Every number quoted in this plan was verified against the live artifacts at planning time. If the executor's own live run disagrees with any figure below, the live artifacts win and the disagreement is recorded in the plan's deviation record — never silently reconciled toward this plan's numbers.
</assumptions>

<tasks>

<task type="tracer">
  <name>Task 1: Re-derive the live A7 numbers and land them in "## Current state" — one path, end to end</name>
  <files>.planning/phases/01-target-extraction-contract/01-SIGNOFF-REQUEST.md</files>
  <read_first>
    - `.planning/phases/01-target-extraction-contract/01-SIGNOFF-REQUEST.md` — the whole file. Note its section order (`# Phase 1 Human Sign-Off Request: Target Extraction QA Figures` → `## Current state` → numeric table → `## Contract in effect` → `## Figures to inspect` with three subsections → `## Decisions requested` → `## How to record the outcome` → `## Closing note`) and its header block (`**Requested:**`, `**Requested by:**`, and the bolded "This document REQUESTS sign-off. It does not grant it." line). The shape is preserved; the content is refreshed.
    - `.planning/phases/01-target-extraction-contract/01-VERIFICATION.md` — the frontmatter `gaps:` block (the single blocking gap and its `missing:` items), the `gaps_remaining:` list, and both `human_verification:` entries.
    - `.planning/phases/01-target-extraction-contract/01-14-ORDERING-OUTCOME.md` — the verbatim `check_targets.py` output block, the coverage table with the 50.50% row and its "razor-thin margin" paragraph, and the "Verdict" section stating the 10-vs-14 gap widened.
    - `.planning/phases/01-target-extraction-contract/01-13-ORDERING-OUTCOME.md` — the Amendment A6 coverage table (track 10 at 243/400 = 60.75%) and its "Decision point" section recommending option (a).
    - `processed_data/targets/manifest.json` — `run_id`, `published_at_utc`, `extraction_params_sha256`.
    - `processed_data/targets/extraction_params.json` — the 19 keys.
  </read_first>
  <precondition>`.venv/bin/python` exists and `scripts/check_targets.py` runs against the checked-in `processed_data/targets/` artifacts without re-running extraction. If the checker cannot run, halt — the document must not be regenerated from this plan's transcribed numbers.</precondition>
  <reversibility rating="reversible">A markdown document rewrite in `.planning/`, fully recoverable via git. No code, constant, or artifact is touched.</reversibility>
  <action>
    Run `.venv/bin/python scripts/check_targets.py --project_dir .` once, capture its full stdout and exit code, and read `processed_data/targets/manifest.json`. Derive every number written below from THAT output and THAT file in this execution. Do not transcribe numbers from this plan, from `01-14-ORDERING-OUTCOME.md`, or from the existing document. If any live figure disagrees with the values named below, the live artifact wins — write the live value and record the disagreement in the deviation record of `01-15-SUMMARY.md`.

    Do not run `scripts/run_target_extraction.py`. Do not regenerate any `.npz`, QA PNG, `manifest.json`, or `extraction_params.json`.

    Update the header block: set `**Requested:**` to 2026-07-22 and change the requester line to name plan 01-15 (gap-closure regeneration of the plan 01-12 original). Leave the bolded "This document REQUESTS sign-off. It does not grant it." sentence and its no-checkbox-may-be-inferred-as-approved clause exactly as written.

    Rewrite `## Current state` end to end against the live run. Replace the Amendment-A5 attribution line with an Amendment-A7 one sourced from `01-14-ORDERING-OUTCOME.md`: A7 sits on top of A3/A4/A5/A6 — `DETREND_POLY_ORDER=4`, `fit_mask=bead_exclusion_mask(...)`, `BEAD_MASK_HEIGHT_FRACTION=HALF_MAX_FRACTION=0.5`, `MAX_TRACKING_GAP_COLUMNS=10`, `max_y_degree=DETREND_MAX_Y_DEGREE=2`, `max_xy_degree=DETREND_MAX_XY_DEGREE=2`, plus `halfmax_edges` merging adjacent above-half-max sub-runs separated by at most `MAX_RUN_MERGE_GAP_PIXELS=10` samples and gating tracked selection to candidates at least `MIN_TRACKED_LENGTH_RATIO=0.5` of the largest same-column candidate — applied identically to all four tracks with no per-track branch. State the artifact generation explicitly: `run_id` 99a4e8472f0a4164938363af0725f31b, published 2026-07-22T03:30:49.258468+00:00, `extraction_params_sha256` 773a25a388dae2954495e4b6f67b2a8c1c7664de01bbb56c005a8bc0e3051c08.

    Replace the quoted results block with the live checker output: track 8 / 400 W / 361 valid bins / median 0.7401 / mean 0.7414; track 10 / 350 W / 202 / 0.3770 / 0.3744; track 14 / 300 W / 301 / 0.6174 / 0.6529; track 21 / 200 W / 308 / 0.3825 / 0.4130. Keep the three adjacent-pair verdict lines as the checker prints them: 8 vs 10 PASS, 10 vs 14 FLAG, 14 vs 21 PASS. State the exit code as 0 with `ALL CHECKS PASSED` printed.

    Replace the coverage table's four rows with the live valid fractions against `TARGET_GRID_N=400`: track 8 = 361 bins = 90.25% PASS; track 10 = 202 bins = 50.50% PASS; track 14 = 301 bins = 75.25% PASS; track 21 = 308 bins = 77.00% PASS. On track 10's row and in a sentence immediately beneath the table, say without hedging that 50.50% clears the hard `MIN_VALID_FRACTION = 0.5` floor by 0.5 percentage points — a margin of 2 bins — and that this is a razor-thin margin, materially tighter than the 60.75% track 10 held under Amendment A6. Cite `01-14-ORDERING-OUTCOME.md`'s measured 41-bin drop as the source.

    Rewrite the "Plain-language state" bullets so every claim matches the live run and none softens a bad result:
    - The 8 > 10 > 14 > 21 ordering does not hold. 8-vs-10 and 14-vs-21 PASS; 10-vs-14 FLAGs, with track 10 at 0.3770 mm below track 14 at 0.6174 mm.
    - State the current absolute 10-vs-14 gap as 0.2404 mm and say explicitly that the gap roughly DOUBLED under Amendments A6 and A7 — from 0.1052 mm at the Amendment-A5 generation this document previously described — rather than narrowing. Do not describe the gap as narrowing, closing, or improving anywhere in the document.
    - `check_targets.py` passes because the coverage floor is the only hard gate it enforces; it does not gate on the width-ordering FLAG, which is diagnostic output.
    - Delete the bullet claiming track 10's coverage concern is closed. Replace it with the honest current position: track 10's coverage moved from a collapse, through a comfortable margin under A5/A6, to 50.50% under A7 — still passing, but now the tightest of the four tracks and the one most at risk from any future change.
    - Add a bullet carrying `01-14-ORDERING-OUTCOME.md`'s fragmentation verdict forward as MIXED, not uniformly improved: contiguous valid-run counts worsened on tracks 8 (22→25), 10 (65→67), and 21 (34→43), improved only marginally on track 14 (63→61), and max adjacent-column jump statistics improved on some boundaries while worsening on others including both of track 8's. Note the baseline caveat that outcome report itself discloses — the pre-fix numbers were measured under Amendment A5, so part of the delta reflects Amendment A6's already-accepted effect rather than A7 alone.

    Do not tick any checkbox. Do not edit any other section in this task. Do not edit any file outside `.planning/`.
  </action>
  <verify>
    <automated>T=$(mktemp); .venv/bin/python scripts/check_targets.py --project_dir . > "$T" 2>&1; test $? -eq 0 || exit 1; grep -q "ALL CHECKS PASSED" "$T" || exit 1; D=.planning/phases/01-target-extraction-contract/01-SIGNOFF-REQUEST.md; for v in $(awk 'NF==5 && ($1=="8"||$1=="10"||$1=="14"||$1=="21"){print $3; print $4; print $5}' "$T"); do grep -qF "$v" "$D" || { echo "LIVE VALUE MISSING FROM DOC: $v"; exit 1; }; done; for row in $(awk 'NF==5 && ($1=="8"||$1=="10"||$1=="14"||$1=="21"){print $1":"$3":"$4":"$5}' "$T"); do tr=${row%%:*}; r=${row#*:}; b=${r%%:*}; r=${r#*:}; md=${r%%:*}; mn=${r##*:}; grep -qE "(^|[ |])$tr[ |].*$b.*$md.*$mn" "$D" || { echo "ROW MISMATCH track $tr: bins/median/mean not co-located on one line"; exit 1; }; done; grep -qF "99a4e8472f0a4164938363af0725f31b" "$D" || exit 1; grep -qF "50.50%" "$D" || exit 1; grep -qF "0.2404" "$D" || exit 1; test "$(grep -c -F -e '0.3713' -e '0.4765' -e '0.7528' -e '0.1998' -e '0.7370' -e '0.3234' -e '0.4246' -e '0.2579' -e '60.5%' -e '91.0%' -e '81.3%' -e '75.0%' "$D")" -eq 0 || exit 1; test -z "$(git status --porcelain src tests scripts processed_data data/raw)" || exit 1; echo "CURRENT STATE IS LIVE"</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/bin/python scripts/check_targets.py --project_dir .` was run during this task, exits 0, and prints `ALL CHECKS PASSED`.
    - Every valid-bin count, median, and mean the live checker prints for tracks 8, 10, 14, and 21 appears verbatim in `01-SIGNOFF-REQUEST.md` (asserted by the awk-driven loop in `<verify>` that reads the checker's own stdout, not by comparison against this plan's text).
    - Each track's valid-bin count, median, and mean are co-located on a SINGLE line of the document belonging to that track — asserted by the row-level loop in `<verify>`, which is also derived from the live checker output. A rewrite that transposes two tracks' medians fails this check even though every individual value is present.
    - `01-SIGNOFF-REQUEST.md` contains `361`, `202`, `301`, and `308` as the four valid-bin counts, and `90.25%`, `50.50%`, `75.25%`, `77.00%` as the four valid fractions.
    - `01-SIGNOFF-REQUEST.md` contains `99a4e8472f0a4164938363af0725f31b` and `773a25a388dae2954495e4b6f67b2a8c1c7664de01bbb56c005a8bc0e3051c08`.
    - `01-SIGNOFF-REQUEST.md` contains `0.2404` and the word `doubled` (or `roughly doubled`) describing the 10-vs-14 gap, and contains `0.1052` as the prior gap it is compared against.
    - `01-SIGNOFF-REQUEST.md` contains the string `razor-thin` and states the 0.5-percentage-point / 2-bin margin for track 10.
    - `01-SIGNOFF-REQUEST.md` contains `MIXED` (or `mixed, not uniformly improved`) describing the fragmentation outcome, and names the worsened contiguous-run counts for tracks 8, 10, and 21.
    - `grep -c -F -e '0.3713' -e '0.4765' -e '0.7528' -e '0.1998' -e '0.7370' -e '0.3234' -e '0.4246' -e '0.2579' -e '60.5%' -e '91.0%' -e '81.3%' -e '75.0%' 01-SIGNOFF-REQUEST.md` returns 0 — every Amendment-A5-era figure is gone, including all four stale MEAN values (`0.7370`, `0.3234`, `0.4246`, `0.2579`), so a partial rewrite cannot leave a stale mean sitting beside a live median in the results block.
    - `grep -c -F '[x]' 01-SIGNOFF-REQUEST.md` returns 0 — no checkbox was ticked.
    - `git status --porcelain src tests scripts processed_data data/raw` is empty — no source, test, script, artifact, or raw-data file was touched.
  </acceptance_criteria>
  <done>`## Current state` describes the artifact generation actually on disk, sourced from a live checker run in this execution, with the razor-thin coverage margin, the doubled ordering gap, and the MIXED fragmentation outcome all stated plainly and none of them softened.</done>
</task>

<task type="auto">
  <name>Task 2: Refresh the contract table, figure questions, and decision set for Amendment A7</name>
  <files>.planning/phases/01-target-extraction-contract/01-SIGNOFF-REQUEST.md</files>
  <read_first>
    - `.planning/phases/01-target-extraction-contract/01-SIGNOFF-REQUEST.md` — the state left by Task 1, plus the four remaining sections to refresh: `## Contract in effect` (currently a 4-row constant / value / fixed-from table), `## Figures to inspect` with its three subsections, `## Decisions requested` with its five unticked checkboxes, `## How to record the outcome`, and `## Closing note`.
    - `.planning/phases/01-target-extraction-contract/01-CONTEXT.md` — Amendment A6 (lines ~66-74: the `DETREND_MAX_XY_DEGREE = 2` rationale, the 01-13-CRITERION.md pre-registered 0.012 mm x-direction shape-gap tolerance, and the cap-3-vs-cap-2 measured evidence) and Amendment A7 (lines ~76-88: `MAX_RUN_MERGE_GAP_PIXELS = MAX_GAP_PIXELS = 10` reusing the D-05/D-06 judgment, and `MIN_TRACKED_LENGTH_RATIO = HALF_MAX_FRACTION = 0.5` reusing the D-01/D-03 half-max convention — both reused from already-locked contract values, neither chosen from the resulting ordering or fragmentation count).
    - `processed_data/targets/extraction_params.json` — confirm all 19 keys and the exact values of the three constants being added to the table.
    - `.planning/phases/01-target-extraction-contract/01-VERIFICATION.md` — both `human_verification:` entries verbatim, so each checkbox can name the item it closes; and the `gaps_remaining:` entry noting the boundary-overlay sign-off has still not occurred.
    - `.planning/phases/01-target-extraction-contract/01-14-ORDERING-OUTCOME.md` — the "Visual QA" section (track 10's post-70mm narrow lock is gone but the trace remains heavily fragmented across 28-80mm; tracks 8 and 14 retain frequent abrupt near-zero-width excursions; no track shows silently-bridged invalid columns) and the "Human verification round context" section.
    - `.planning/phases/01-target-extraction-contract/01-13-ORDERING-OUTCOME.md` — the "Decision point" section's explicit recommendation of option (a) and its statement that no further open-ended tuning cycle is authorized.
    - `.planning/phases/01-target-extraction-contract/01-SPEC.md` — the Prohibitions table's judgment-tier item, which the reviewer is the confirming authority for.
  </read_first>
  <precondition>Task 1 has already rewritten `## Current state`; this task edits the remaining sections of the same file and must not revert Task 1's numbers.</precondition>
  <reversibility rating="reversible">Markdown edits within `.planning/`, fully recoverable via git.</reversibility>
  <action>
    Expand `## Contract in effect` from four rows to seven. Keep the existing four rows (`DETREND_POLY_ORDER` = 4 from Amendment A3; `BEAD_MASK_HEIGHT_FRACTION` = 0.5 from Amendment A4; `MAX_TRACKING_GAP_COLUMNS` = 10 from Amendment A4; `DETREND_MAX_Y_DEGREE` = 2 from Amendment A5) with their fixed-from rationale intact, and add three rows sourced from `01-CONTEXT.md`:
    - `DETREND_MAX_XY_DEGREE` = 2 — Amendment A6. Fixed from `01-13-CRITERION.md`'s pre-registered 0.012 mm x-direction shape-gap tolerance measured on all four tracks' real data: cap 2 is the largest cross-term x-exponent cap that clears it (cap 3 leaves track 10 at 0.0212 mm, above tolerance; cap 2 brings it to 0.0118 mm). Fixed before the ordering outcome was inspected.
    - `MAX_RUN_MERGE_GAP_PIXELS` = 10 — Amendment A7. Not a new number: reused from the already-locked `MAX_GAP_PIXELS`, applying the D-05/D-06 judgment that a stretch of at most 10 native pixels below criterion is not a genuine break, now to post-threshold run fragmentation instead of raw NaN gaps.
    - `MIN_TRACKED_LENGTH_RATIO` = 0.5 — Amendment A7. Not a new number: reused from the already-locked `HALF_MAX_FRACTION`, applying the D-01/D-03 half-max convention's own fraction to candidate plausibility — a tracked candidate shorter than half the largest same-column alternative is not a plausible competing feature.

    Update the paragraph beneath the table so it refers to seven constants across Amendments A3 through A7 rather than four, keeps the claim that each was fixed from residual, physical, numerical, or already-locked-contract evidence before the ordering outcome was inspected, and keeps the judgment-tier prohibition from `01-SPEC.md` that the reviewer is asked to confirm. Note that `extraction_params()` now returns 19 keys and that the document's `extraction_params_sha256` is the provenance digest of exactly that set.

    Rewrite the intro paragraph of `## Figures to inspect` to tell the reviewer, before they open anything, what `01-14-ORDERING-OUTCOME.md` and `01-VERIFICATION.md` already found: gap handling is correct on every track (explicit grey shading, no silently bridged invalid regions), but fragmentation and abrupt single-column jumps persist, and the aggregate fragmentation and jump metrics did not uniformly improve under Amendment A7. Frame the reviewer's job as judging whether that residual jitter is acceptable under the D-14 / success-criterion-3 standard, not as confirming a fix.

    Refresh each of the three figure subsections' questions against the current generation, keeping all 12 explicit paths:
    - Residual maps: keep the existing question about coherent track-wide bow and the y-strip-edge artifact, and add that all four surfaces are now produced under the A6 cross-term cap, so the reviewer is judging the A6/A7 surface, not the A5 one.
    - Boundary overlays: keep the continuity and explicit-grey-shading questions, and add the specific things `01-14-ORDERING-OUTCOME.md` reports so the reviewer can confirm or dispute them — track 10's post-70mm sustained near-zero-width lock is gone but its trace remains heavily fragmented across the full 28-80mm span; tracks 8 and 14 retain frequent abrupt near-zero-width excursions (track 8 near x=24-27mm and x=97-99mm).
    - Width curves: keep the crop-edge question and add that tracks 14 and 21 were substantially reshaped under A7 (track 14's median rose to 0.6174 mm, track 21's to 0.3825 mm), so their curves differ materially from the generation previously reviewed.

    Rewrite `## Decisions requested`, keeping all five checkboxes unticked and keeping the preamble stating that ticking is a human action taken outside this document. Make each checkbox name which of `01-VERIFICATION.md`'s two open `human_verification` items it closes — the first four collectively answer item 2 (visual sign-off across all 12 QA figures), and the fifth is item 1 (the 10-vs-14 decision). Update the fifth checkbox's body: replace the stale narrative with the current position — track 10's median remains below track 14's, the absolute gap is 0.2404 mm and roughly doubled rather than narrowing, track 10's coverage is now 202/400, and BOTH `01-13-ORDERING-OUTCOME.md` and `01-14-ORDERING-OUTCOME.md` explicitly recommend option (a), accept as a documented investigated known limitation. Present that recommendation and the worsened gap immediately next to each other. Note under option (b) that the phase's HONEST-OUTCOME GUARD states no further open-ended tuning cycle is authorized absent a new independently-diagnosed root cause. Still select neither option — record that both are presented and the document recommends neither on the human's behalf.

    Update `## How to record the outcome` to point at `/gsd-verify-work 1` explicitly, and to state that TARGET-02's checkbox and traceability row in `.planning/REQUIREMENTS.md` flip to complete only after that record exists — this plan (01-15) does not and cannot make that change itself.

    Update `## Closing note` so its list of locked constants includes all seven, and keep its rule intact: a rejected item is answered by a new gap-closure plan, never by adjusting a locked constant, the `MIN_VALID_FRACTION` floor, or the boundary-clipped-run exclusion in `halfmax_edges` to make a figure look better.

    Do not tick any checkbox. Do not revert or restate any Amendment-A5-era figure. Do not edit `01-UAT.md`, `01-VERIFICATION.md`, or any file outside `.planning/`.
  </action>
  <verify>
    <automated>D=.planning/phases/01-target-extraction-contract/01-SIGNOFF-REQUEST.md; for s in DETREND_POLY_ORDER BEAD_MASK_HEIGHT_FRACTION MAX_TRACKING_GAP_COLUMNS DETREND_MAX_Y_DEGREE DETREND_MAX_XY_DEGREE MAX_RUN_MERGE_GAP_PIXELS MIN_TRACKED_LENGTH_RATIO "/gsd-verify-work 1"; do grep -qF "$s" "$D" || { echo "MISSING: $s"; exit 1; }; done; test "$(grep -c -F 'processed_data/targets/qa/track_' "$D")" -ge 12 || exit 1; test "$(grep -c -F '[x]' "$D")" -eq 0 || exit 1; test "$(grep -cE '^- \[ \] \*\*' "$D")" -eq 5 || { echo "EXPECTED EXACTLY 5 UNTICKED DECISION CHECKBOXES"; exit 1; }; test "$(grep -c -F -e '0.3713' -e '0.4765' -e '0.7528' -e '0.1998' -e '0.7370' -e '0.3234' -e '0.4246' -e '0.2579' -e '60.5%' -e '91.0%' -e '81.3%' -e '75.0%' "$D")" -eq 0 || exit 1; test -z "$(git status --porcelain src tests scripts processed_data data/raw)" || exit 1; echo "CONTRACT AND DECISIONS REFRESHED"</automated>
    <human-check>Open the 12 figures listed in `01-SIGNOFF-REQUEST.md` and record approvals, rejections, or the option (a)/(b) ordering decision via `/gsd-verify-work 1`. This plan cannot close the phase: TARGET-02's acceptance criterion and `01-VERIFICATION.md`'s two open human_verification items both require a person, and no automated check substitutes for either.</human-check>
  </verify>
  <acceptance_criteria>
    - `01-SIGNOFF-REQUEST.md` contains all seven constant names: `DETREND_POLY_ORDER`, `BEAD_MASK_HEIGHT_FRACTION`, `MAX_TRACKING_GAP_COLUMNS`, `DETREND_MAX_Y_DEGREE`, `DETREND_MAX_XY_DEGREE`, `MAX_RUN_MERGE_GAP_PIXELS`, `MIN_TRACKED_LENGTH_RATIO`.
    - The `## Contract in effect` table has seven data rows, and each of the three new rows names its amendment (A6 or A7) and the evidence or already-locked constant it was fixed from.
    - `grep -c -F 'processed_data/targets/qa/track_' 01-SIGNOFF-REQUEST.md` returns at least 12 — all four tracks by three figure types remain explicitly listed.
    - The `## Figures to inspect` intro states that gap handling is correct on every track AND that fragmentation/jump metrics did not uniformly improve under Amendment A7.
    - The fifth decision checkbox contains `0.2404`, names option (a) and option (b), states that both `01-13-ORDERING-OUTCOME.md` and `01-14-ORDERING-OUTCOME.md` recommend option (a), and explicitly selects neither.
    - Each decision checkbox names which of `01-VERIFICATION.md`'s two open `human_verification` items it closes.
    - `01-SIGNOFF-REQUEST.md` contains the literal `/gsd-verify-work 1`.
    - `grep -c -F '[x]' 01-SIGNOFF-REQUEST.md` returns 0 (no forged approval) and `grep -cE '^- \[ \] \*\*' 01-SIGNOFF-REQUEST.md` returns exactly 5 — five distinct unticked decision checkboxes survived, anchored at line start so an incidental bracket pair elsewhere cannot satisfy the count.
    - `grep -c -F -e '0.3713' -e '0.4765' -e '0.7528' -e '0.1998' -e '0.7370' -e '0.3234' -e '0.4246' -e '0.2579' -e '60.5%' -e '91.0%' -e '81.3%' -e '75.0%' 01-SIGNOFF-REQUEST.md` still returns 0.
    - `git status --porcelain src tests scripts processed_data data/raw` is empty.
  </acceptance_criteria>
  <done>The reviewer sees the full Amendment-A7 contract they are being asked to accept, is told the MIXED fragmentation result before opening a figure, and is handed both ordering options with the recommendation and the worsened number side by side — with nothing pre-decided.</done>
</task>

<task type="auto">
  <name>Task 3: Reconcile REQUIREMENTS.md's TARGET-02 checkbox with its own traceability row</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - `.planning/REQUIREMENTS.md` — lines ~12-13 (the `- [x] **TARGET-01**` and `- [x] **TARGET-02**` checkboxes), lines ~79-92 (the Traceability table, where TARGET-01 reads `Complete` and TARGET-02 reads "Awaiting human visual sign-off on regenerated QA figures — see `01-SIGNOFF-REQUEST.md`"), line ~94 (the existing dated 2026-07-21 correction note), and lines ~96-100 (the Coverage block).
    - `.planning/phases/01-target-extraction-contract/01-VERIFICATION.md` — the Anti-Patterns table row flagging lines 12-13 vs 81-82 as an Info-severity inconsistency, and the Requirements Coverage table (TARGET-01 SATISFIED, TARGET-02 NEEDS HUMAN).
    - `.planning/phases/01-target-extraction-contract/01-SPEC.md` — Requirement 2's acceptance clause (c), the reason TARGET-02 cannot be complete before a human reviewer confirms the QA plots regardless of any numeric outcome.
  </read_first>
  <precondition>`.planning/REQUIREMENTS.md`'s Traceability table still lists TARGET-02 as awaiting human visual sign-off; if a sign-off record has appeared since planning, halt rather than overwriting it.</precondition>
  <reversibility rating="reversible">A checkbox state and one added line in a planning document, fully recoverable via git. No requirement is added, removed, or remapped.</reversibility>
  <action>
    Edit `.planning/REQUIREMENTS.md` with scoped replacements only — do not rewrite the file and do not use a whole-file write.

    Change TARGET-02's checkbox on line ~13 from ticked to unticked (an empty single-space markdown checkbox) so it agrees with its own Traceability row, which already correctly reads "Awaiting human visual sign-off on regenerated QA figures". This resolves the Info-severity inconsistency `01-VERIFICATION.md`'s Anti-Patterns table flags between lines 12-13 and lines 81-82. Do not invent a sign-off: no human has recorded one, so the checkbox becomes unticked and the traceability row keeps its awaiting-sign-off wording. Do not flip the traceability row to Complete.

    Leave TARGET-01 exactly as it is — checkbox still ticked, traceability row still `Complete`. `01-VERIFICATION.md` records truth 1 as VERIFIED and TARGET-01 as SATISFIED; the contract is documented through Amendments A1-A7. Do not downgrade it.

    Leave the requirement text of both TARGET-01 and TARGET-02 unchanged. Leave the Traceability row wording for TARGET-02 unchanged apart from any reference update needed to keep it pointing at `01-SIGNOFF-REQUEST.md`.

    Add one dated line beneath the Traceability table, alongside the existing 2026-07-21 correction note, recording that on 2026-07-22 TARGET-02's checkbox was set to unticked to match its traceability row, citing `01-VERIFICATION.md`'s Anti-Patterns finding as the basis and `01-SIGNOFF-REQUEST.md` as the artifact the sign-off is pending against. Do not delete or rewrite the existing 2026-07-21 note.

    Leave the Coverage block's counts untouched — mapping coverage is unchanged at 12 v1 requirements, 12/12 mapped, 0 unmapped. Only a completion checkbox changes. Do not touch the v2 or Out of Scope sections, any other requirement, or any file outside `.planning/`.
  </action>
  <verify>
    <automated>R=.planning/REQUIREMENTS.md; grep -qE '^- \[ \] \*\*TARGET-02\*\*' "$R" || exit 1; grep -qE '^- \[x\] \*\*TARGET-01\*\*' "$R" || exit 1; grep -E '^\| TARGET-01 \| Phase 1 \|' "$R" | grep -qF 'Complete' || exit 1; grep -E '^\| TARGET-02 \| Phase 1 \|' "$R" | grep -qiF 'awaiting human visual sign-off' || exit 1; grep -qF '01-SIGNOFF-REQUEST.md' "$R" || exit 1; grep -qF '2026-07-22' "$R" || exit 1; grep -qF '2026-07-21' "$R" || exit 1; grep -qF '12/12' "$R" || exit 1; test -z "$(git status --porcelain src tests scripts processed_data data/raw)" || exit 1; .venv/bin/python scripts/check_targets.py --project_dir . >/dev/null 2>&1 || exit 1; echo "REQUIREMENTS RECONCILED"</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/REQUIREMENTS.md` shows `- [ ] **TARGET-02**` and still shows `- [x] **TARGET-01**`.
    - The Traceability row for TARGET-02 still names the outstanding human visual sign-off and still references `01-SIGNOFF-REQUEST.md`; it does NOT read `Complete`.
    - The Traceability row for TARGET-01 still reads `Complete`.
    - A dated 2026-07-22 line beneath the Traceability table records the checkbox reconciliation and cites `01-VERIFICATION.md`; the pre-existing 2026-07-21 correction note is still present and unmodified.
    - The Coverage block still reports 12 v1 requirements with 12/12 mapped and 0 unmapped.
    - No sign-off is asserted anywhere: the file contains no claim that a human approved the QA figures.
    - `.venv/bin/python scripts/check_targets.py --project_dir .` still exits 0.
    - `git status --porcelain src tests scripts processed_data data/raw` is empty.
  </acceptance_criteria>
  <done>The TARGET-02 checkbox and its traceability row tell the same story — pending human visual sign-off — with no sign-off invented and TARGET-01 untouched.</done>
</task>

</tasks>

<threat_model>
This round is documentation-only. It introduces no new input handling, writes no file outside `.planning/`, invokes the extraction pipeline only in read-only checker mode, and changes nothing in the already-hardened atomic publish path. Rather than inventing threats, the register below covers the real ones: an inaccurate sign-off artifact, a forged approval, and accidental tampering with locked constants or artifacts. ASVS level 1; block on `high`.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| checked-in `processed_data/targets/` artifacts → regenerated markdown | The document's factual claims must be derived from the live artifacts in this run, not transcribed from a plan prompt or a prior report; a transcription error becomes a false statement a human signs off on. |
| automated plan output → human sign-off record | A plan that ticks a checkbox or records an approval would forge the one gate TARGET-02 reserves for a human. |
| project record (`REQUIREMENTS.md`) → downstream phase trust | Phase 2 planning reads this file to decide whether Phase 1's output is trustworthy; a checkbox that disagrees with its own traceability row is an ambiguous safety signal. |
| doc-only plan → locked extraction contract and published artifacts | The phase's integrity rests on constants never being tuned toward a desired outcome; a doc round must not become a covert tuning round. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-34 | Repudiation | `01-SIGNOFF-REQUEST.md` factual accuracy | high | mitigate | Task 1 derives every number from a live `check_targets.py` run in this execution; `<verify>` parses that run's own stdout with awk and asserts each printed median/mean appears in the document, so a transcription from this plan's text cannot pass. The document also pins `run_id` and `extraction_params_sha256` so the generation it describes is identifiable. |
| T-01-35 | Spoofing | human visual sign-off | high | mitigate | No task ticks a checkbox; `grep -c -F '[x]'` returning 0 is asserted in both Task 1 and Task 2. Task 2 carries a `<human-check>` routing approval to `/gsd-verify-work 1`. Task 3 explicitly forbids inventing a sign-off and asserts the file contains no approval claim. |
| T-01-36 | Tampering (information suppression) | worsened numbers | high | mitigate | Acceptance criteria positively require `50.50%`, `razor-thin`, `0.2404`, `doubled`, and `MIXED` to be present, and negatively require every Amendment-A5-era literal to be absent — so softening or omitting a bad number fails the gate mechanically, not just by review. |
| T-01-37 | Tampering | locked extraction constants and published artifacts | high | mitigate | Every task asserts `git status --porcelain src tests scripts processed_data data/raw` is empty. The pipeline is never invoked in write mode; `run_target_extraction.py` is not run. The `must_haves.prohibitions` block names the source, test, constant, and pipeline freeze explicitly. |
| T-01-38 | Tampering (accidental) | `data/raw/` | high | mitigate | No task reads or writes under `data/raw/`; `git status --porcelain data/raw` is asserted empty in all three tasks. `check_targets.py` operates on `processed_data/targets/` only. |
| T-01-39 | Elevation of privilege | scope of a doc-only round | medium | mitigate | The plan forbids reopening G-01-4 and G-01-5 and forbids any tuning cycle; the only writes are two `.planning/` markdown files, enforced by the empty-`git status` assertions above. |
| T-01-40 | Information disclosure | new data exposure | low | accept | No new data path, no new file outside `.planning/`, no network access, no credential handling. The document quotes only figures already committed in `01-13-ORDERING-OUTCOME.md`, `01-14-ORDERING-OUTCOME.md`, and `manifest.json`. Nothing new is exposed. |
| T-01-SC | Tampering | npm/pip/cargo installs | high | mitigate | No packages are installed by this plan and `requirements.txt` is unchanged. No `[ASSUMED]` or `[SUS]` package is introduced, so no legitimacy checkpoint is required. |
</threat_model>

<verification>
- `.planning/phases/01-target-extraction-contract/01-SIGNOFF-REQUEST.md` quotes the live Amendment-A7 run — valid bins 361/202/301/308, medians 0.7401/0.3770/0.6174/0.3825 mm, fractions 90.25%/50.50%/75.25%/77.00% — with `run_id` 99a4e8472f0a4164938363af0725f31b cited, and contains none of the Amendment-A5-era literals.
- The document states the razor-thin 50.50% margin (0.5 percentage points / 2 bins above the hard floor), the 0.2404 mm 10-vs-14 gap and that it roughly doubled from 0.1052 mm, and the MIXED fragmentation outcome with the worsened contiguous-run counts for tracks 8, 10, and 21.
- `## Contract in effect` lists all seven amendment constants including `DETREND_MAX_XY_DEGREE`, `MAX_RUN_MERGE_GAP_PIXELS`, and `MIN_TRACKED_LENGTH_RATIO`.
- All 12 QA figure paths remain explicitly listed, every checkbox is unticked, and `/gsd-verify-work 1` is named as the recording route.
- `.planning/REQUIREMENTS.md` shows TARGET-02 as `[ ]` with a traceability row that agrees, TARGET-01 untouched at `[x]` / `Complete`, and a dated 2026-07-22 reconciliation line beside the existing 2026-07-21 note.
- `.venv/bin/python scripts/check_targets.py --project_dir .` exits 0 printing `ALL CHECKS PASSED` after the plan, exactly as before it.
- `git status --porcelain src tests scripts processed_data data/raw` is empty for the whole plan — no source, test, script, constant, artifact, or raw-data file changed.
- The phase's two open human_verification items are left open for `/gsd-verify-work 1` rather than self-certified.
</verification>

<success_criteria>
- A human reading `01-SIGNOFF-REQUEST.md` sees the current, honest, and materially worse picture — the razor-thin track-10 coverage margin, the doubled 10-vs-14 gap, and the MIXED fragmentation outcome — rather than the stale Amendment-A5 picture that made the state look better than it is.
- The reviewer is handed the option (a) / option (b) ordering decision with both outcome reports' recommendation and the worsened number presented together, with neither option selected for them.
- `REQUIREMENTS.md`'s TARGET-02 checkbox and traceability row agree, with no sign-off invented.
- Nothing outside `.planning/` changed, and the extraction contract, its constants, its tests, and its published artifacts are provably untouched.
</success_criteria>

<output>
Create `.planning/phases/01-target-extraction-contract/01-15-SUMMARY.md` when done. Record in its deviation section any case where a live artifact disagreed with a figure named in this plan — the live artifact wins and the disagreement is stated, never silently reconciled.
</output>
