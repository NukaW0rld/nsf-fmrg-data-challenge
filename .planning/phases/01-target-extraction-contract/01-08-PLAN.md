---
phase: 01-target-extraction-contract
plan: 08
type: execute
wave: 8
depends_on: [01-07]
files_modified:
  - processed_data/targets/track_8_targets.npz
  - processed_data/targets/track_10_targets.npz
  - processed_data/targets/track_14_targets.npz
  - processed_data/targets/track_21_targets.npz
  - processed_data/targets/extraction_params.json
  - processed_data/targets/manifest.json
  - processed_data/targets/qa/
  - .planning/phases/01-target-extraction-contract/01-08-ORDERING-OUTCOME.md
autonomous: true
gap_closure: true
requirements: [TARGET-01, TARGET-02]
must_haves:
  truths:
    - "All four NPZs, the 12 QA PNGs (residual maps, raw/detrended boundary overlays, width plots), extraction_params.json, and manifest.json are regenerated in ONE atomic staged run via scripts/run_target_extraction.py, carrying the complete change-sensitive provenance."
    - "scripts/check_targets.py passes: persisted extraction_params.json equals the completed extraction_params(), the manifest SHA-256 matches, and all four artifacts pass the float64/NaN-mask/strict-ordering/positive-width structural contract."
    - "The width ordering 8>10>14>21 is re-checked and REPORTED as an outcome of the corrected uniform extraction; it is never forced by tuning."
    - "The data/raw/ integrity audit passes: the regeneration created, modified, and deleted zero files under data/raw/."
    - "If the corrected uniform extraction does NOT restore 8>10>14>21, execution STOPS and escalates for a separate human-override decision rather than changing any extraction parameter."
  artifacts:
    - "processed_data/targets/track_{8,10,14,21}_targets.npz"
    - "processed_data/targets/extraction_params.json"
    - "processed_data/targets/manifest.json"
    - "processed_data/targets/qa/*.png"
    - ".planning/phases/01-target-extraction-contract/01-08-ORDERING-OUTCOME.md"
  key_links:
    - "run_target_extraction.py -> extraction_params()/manifest digest: the regenerated JSON and manifest carry the completed dict, so check_targets.py provenance and digest assertions pass."
    - "regenerated qa/*.png -> human re-inspection: the 12 figures back the outstanding VERIFICATION Human Verification items #1 (residual maps), #2 (boundary/gap overlays), #3 (crop-edge widths)."
  prohibitions:
    - "No outcome-driven per-track tuning to force the ordering; the only honest fallback is a documented, separate human override, never a parameter change."
    - "No writes/deletes/modifications under data/raw/ (verified by the runner's before/after snapshot audit)."
    - "The published processed_data/targets/ tree must never mix generations; publication is atomic via the staged run only on a fully successful, raw-audit-clean run."
---

<objective>
Produce the corrected Phase 1 output and report the roadmap ordering honestly. Regenerate all four target NPZs, the 12 QA figures, extraction_params.json, and manifest.json in one atomic staged run using the existing runner so the published artifacts carry plan 01-07's corrected uniform extraction and its complete, change-sensitive provenance. Re-run the artifact/provenance checker. Then apply the HONEST-OUTCOME GUARD: read the 8>10>14>21 ordering as an outcome; if the principled fix restored it, record that; if it did NOT, STOP and escalate for a separate human-override decision rather than tuning any parameter.

Purpose: TARGET-01 requires reproducible, provenance-locked output; TARGET-02 requires visually-QA'd artifacts and re-checks the expected width ordering. This plan closes the persisted-provenance half of Gap 1 (complete JSON + manifest digest) and the outcome half of Gap 2 (ordering reported, never forced), and regenerates the figures the human verifier still needs to sign off.
Output: A regenerated, provenance-complete processed_data/targets/ tree and a committed honest ordering-outcome record.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-target-extraction-contract/01-VERIFICATION.md
@.planning/phases/01-target-extraction-contract/01-CONTEXT.md
@.planning/phases/01-target-extraction-contract/01-06-DIAGNOSIS.md
@src/targets.py
@scripts/run_target_extraction.py
@scripts/check_targets.py
</context>

<artifacts_produced>
New symbols and paths introduced by this plan (for downstream consumers):
- Regenerated data artifacts: `processed_data/targets/track_{8,10,14,21}_targets.npz`, `extraction_params.json` (now includes MAX_TRACKING_GAP_COLUMNS + the new fix param), `manifest.json` (digest over the completed params), `qa/track_{8,10,14,21}_{residual_map,overlay,width}.png` (12 figures).
- Committed record: `.planning/phases/01-target-extraction-contract/01-08-ORDERING-OUTCOME.md` (regenerated per-track median widths, the three adjacent PASS/FLAG verdicts, and either a restored-ordering statement or a human-override escalation).
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: Atomically regenerate all four-track artifacts and re-run the checker</name>
  <files>processed_data/targets/track_8_targets.npz, processed_data/targets/track_10_targets.npz, processed_data/targets/track_14_targets.npz, processed_data/targets/track_21_targets.npz, processed_data/targets/extraction_params.json, processed_data/targets/manifest.json, processed_data/targets/qa/</files>
  <read_first>
    - scripts/run_target_extraction.py (run_pipeline staged/atomic publish; snapshot_raw before/after; QA figure writers)
    - scripts/check_targets.py (persisted==code params assertion; manifest digest; per-track structural contract; print_results ordering verdicts)
    - src/targets.py (the corrected extraction path and completed extraction_params() from plan 01-07)
    - .planning/phases/01-target-extraction-contract/01-VERIFICATION.md (the incomplete-provenance and ordering failures being closed)
  </read_first>
  <action>
    Run the full published pipeline once: `.venv/bin/python scripts/run_target_extraction.py --project_dir .`. Confirm it prints the "data/raw/ integrity PASS" line and the per-track summary table, and that it atomically publishes a fresh processed_data/targets/ tree (four NPZs, extraction_params.json, manifest.json, and 12 qa/*.png). Do NOT hand-edit any generated artifact and do NOT run per-track invocations — the single staged run is the only publication path so generations never mix.

    Then run `.venv/bin/python scripts/check_targets.py --project_dir .` and confirm it prints ALL CHECKS PASSED, which requires: persisted extraction_params.json equals the completed extraction_params() (now including MAX_TRACKING_GAP_COLUMNS and the new fix param), the manifest extraction_params_sha256 matches, and every track passes the float64 dtype, NaN-only-invalid-slot, finite==mask, strict boundary ordering, and positive-width-below-y-strip structural checks. Also re-run `.venv/bin/python -O scripts/check_targets.py --project_dir .` and both test suites (`.venv/bin/python tests/test_targets.py`, `.venv/bin/python tests/test_nsf_fmrg_data.py`) to confirm the corrected contract holds under optimization. Confirm the 12 qa/*.png files exist with post-run mtimes so the human verifier can re-inspect VERIFICATION Human Verification #1/#2/#3.
  </action>
  <verify>
    <automated>.venv/bin/python scripts/run_target_extraction.py --project_dir . && .venv/bin/python scripts/check_targets.py --project_dir . && test $(ls processed_data/targets/qa/*.png | wc -l) -eq 12 && .venv/bin/python -c "import json,hashlib; p=json.load(open('processed_data/targets/extraction_params.json')); assert 'MAX_TRACKING_GAP_COLUMNS' in p, p; m=json.load(open('processed_data/targets/manifest.json')); assert m['extraction_params_sha256']==hashlib.sha256(json.dumps(p,sort_keys=True).encode()).hexdigest()" && test -z "$(git -C . status --porcelain -- data/raw)"</automated>
  </verify>
  <acceptance_criteria>
    - The single staged run publishes four NPZs, extraction_params.json, manifest.json, and 12 qa/*.png, and prints the data/raw integrity PASS line.
    - scripts/check_targets.py prints ALL CHECKS PASSED normally and under `python -O`; persisted params equal the completed extraction_params() and the manifest digest matches.
    - extraction_params.json contains 'MAX_TRACKING_GAP_COLUMNS' and the new fix parameter (persisted provenance is now complete).
    - `git status --porcelain -- data/raw` is empty (no raw file created, modified, or deleted).
  </acceptance_criteria>
  <done>The corrected, provenance-complete artifacts and 12 QA figures are atomically regenerated and pass the structural/provenance checker, with data/raw/ untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Report the ordering as an honest outcome; escalate if not restored (no tuning)</name>
  <files>.planning/phases/01-target-extraction-contract/01-08-ORDERING-OUTCOME.md</files>
  <read_first>
    - .planning/phases/01-target-extraction-contract/01-08-ORDERING-OUTCOME.md (the file being created)
    - scripts/check_targets.py (print_results ordering verdicts) and its printed medians from Task 1
    - .planning/ROADMAP.md (Phase 1 Success Criterion 2: 400W(8) > 350W(10) > 300W(14) > 200W(21))
    - .planning/phases/01-target-extraction-contract/01-VERIFICATION.md (Gap 2 non-negotiable ordering and the failing spot-check)
    - .planning/phases/01-target-extraction-contract/01-06-DIAGNOSIS.md (the pre-registered criterion — confirm the fix followed it, not the ordering)
  </read_first>
  <action>
    From the regenerated artifacts, capture the four per-track valid median widths and the three adjacent ordering verdicts (8-vs-10, 10-vs-14, 14-vs-21) exactly as check_targets.py / print_results report them. Write .planning/phases/01-target-extraction-contract/01-08-ORDERING-OUTCOME.md recording those numbers verbatim and stating plainly whether 8>10>14>21 is restored.

    If the ordering is fully restored: state it, confirm no extraction constant was changed after regeneration to achieve it, and note the fix parameter was fixed from the plan-06 pre-registered residual/physical criterion.

    If the ordering is NOT fully restored: STOP. Do NOT change BEAD_MASK_HEIGHT_FRACTION (or any fix/extraction constant), do NOT add a per-track branch, and do NOT re-tune. In 01-08-ORDERING-OUTCOME.md, surface the residual failure explicitly and frame the remaining choice as a SEPARATE human decision: either (a) a documented human override of the roadmap ordering criterion with rationale, or (b) a new diagnosed defect to investigate in a follow-up plan. Recommend halting Phase 1 verification for that human decision. This escalation, not further tuning, is the required behavior.

    Do not modify src/targets.py, extraction_params(), or any locked constant in this task under any circumstance.
  </action>
  <verify>
    <automated>test -f .planning/phases/01-target-extraction-contract/01-08-ORDERING-OUTCOME.md && grep -Eqi "8.*10.*14.*21|ordering" .planning/phases/01-target-extraction-contract/01-08-ORDERING-OUTCOME.md && test -z "$(git -C . diff --stat -- src/targets.py)"</automated>
  </verify>
  <acceptance_criteria>
    - 01-08-ORDERING-OUTCOME.md records the four regenerated median widths and the three adjacent PASS/FLAG verdicts verbatim.
    - It states plainly whether 8>10>14>21 is restored; if restored, it confirms no constant was changed post-regeneration to achieve it.
    - If not restored, it escalates to a separate human-override decision and recommends halting, with NO extraction parameter changed (git diff on src/targets.py is empty for this task).
    - No per-track branch or outcome-driven tuning was introduced by this task.
  </acceptance_criteria>
  <done>The ordering outcome is reported honestly; a non-restored ordering triggers human escalation rather than any tuning.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| filesystem read of data/raw/ -> in-memory arrays | The production regeneration reads all four raw height maps; it must never write or delete under data/raw/. |
| in-memory results -> processed_data/targets/ atomic publish | Regeneration must publish atomically so the live tree never mixes generations, and must land only under processed_data/. |
| ordering outcome -> parameter constants | The observed ordering must not feed back into any extraction constant (no outcome-driven tuning). |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-08-01 | Tampering | data/raw/ | high | mitigate | Use the runner's existing snapshot_raw before/after audit that fails closed on any raw diff; verify `git status --porcelain -- data/raw` is empty. |
| T-01-08-02 | Tampering | published targets tree | medium | mitigate | Publish only via the single atomic staged run; never hand-edit generated artifacts or run per-track publications. |
| T-01-08-03 | Repudiation | no-tuning / ordering integrity | high | mitigate | Report ordering as an outcome; on failure escalate to a separate human override with zero parameter changes (git diff on src/targets.py empty). |
</threat_model>

<verification>
- `.venv/bin/python scripts/run_target_extraction.py --project_dir .` publishes four NPZs, complete extraction_params.json, matching manifest.json, and 12 qa/*.png, printing the data/raw integrity PASS line.
- `.venv/bin/python scripts/check_targets.py --project_dir .` prints ALL CHECKS PASSED normally and under `python -O`.
- extraction_params.json contains MAX_TRACKING_GAP_COLUMNS and the new fix param; the manifest digest matches.
- `git status --porcelain -- data/raw` is empty.
- 01-08-ORDERING-OUTCOME.md records the honest ordering verdict (restored, or escalated with no parameter change).
</verification>

<success_criteria>
- Corrected, provenance-complete artifacts and 12 QA figures are regenerated atomically and pass the checker.
- The 8>10>14>21 ordering is reported as an honest outcome; a non-restored ordering is escalated for a separate human decision, never tuned.
- data/raw/ is provably untouched.
</success_criteria>

<output>
Create `.planning/phases/01-target-extraction-contract/01-08-SUMMARY.md` when done.
</output>
