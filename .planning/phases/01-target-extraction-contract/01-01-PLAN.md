---
phase: 01-target-extraction-contract
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/targets.py
  - tests/test_targets.py
autonomous: false
requirements: [TARGET-01, TARGET-02]
tags: [profilometry, width-extraction, numpy, scientific-data]
must_haves:
  truths:
    - "src/targets.py defines the entire extraction parameterization as a single block of module-level constants with zero conditionals keyed on the track identifier"
    - "A gap of exactly 10 consecutive NaNs in a binned profile is still interpolated; only a run exceeding 10 invalidates the slot"
    - "A column whose peak-baseline separation is exactly at the 0.005mm minimum is valid; only strictly-below-minimum invalidates"
    - "nan_savgol output is finite exactly where its input is finite — invalid slots stay NaN, valid slots are never NaN-poisoned by neighbors"
    - "The smoother blends valid points from both sides of a masked gap (window is not restricted to one side)"
    - "Percentile baseline/peak use numpy's default linear-interpolation percentile method (np.percentile with no method argument)"
    - "Crossed or degenerate boundaries (y_upper <= y_lower) mark the slot invalid rather than reporting a negative or zero width"
    - "A track producing zero valid x-positions raises a ValueError hard error rather than returning an all-NaN result"
    - "Contract-invariant assertion script tests/test_targets.py exits 0 against the implemented module"
    - "Amendments A1 and A2 are presented to the user for ratification at a blocking checkpoint inside this plan — before plan 01-02 computes any real-track width number"
  artifacts:
    - path: "src/targets.py"
      provides: "Contract constants + pure extraction functions (bin_profile, fill_small_gaps, halfmax_edges, nan_savgol, extract_targets_from_arrays, extract_track_targets, target_grid, extraction_params)"
      contains: "MIN_PEAK_BASELINE_SEPARATION_MM"
    - path: "tests/test_targets.py"
      provides: "Synthetic contract-invariant tests runnable as plain Python (no pytest dependency)"
      contains: "def test_"
    - path: ".venv/bin/python"
      provides: "Project virtualenv with numpy/scipy/matplotlib/pandas installed (Wave 0)"
  key_links:
    - from: "src/targets.py"
      to: "src/nsf_fmrg_data.py"
      via: "flat sibling import of load_wyko_asc, robust_plane_detrend, largest_true_run"
      pattern: "from nsf_fmrg_data import"
    - from: "tests/test_targets.py"
      to: "src/targets.py"
      via: "sys.path bootstrap to src/ then import targets"
      pattern: "import targets"
  prohibitions:
    - statement: "Extraction contract constants (noise-floor minimum, gap limit, percentile split, half-max fraction, SG window/order) must NOT be adjusted based on whether the resulting width ordering or QA plots look correct — they are locked in this plan from measured substrate statistics, before any per-track width number is computed"
      status: resolved
      verification: judgment
---

<objective>
Create the locked TARGET-01 extraction contract as executable code: a new library module `src/targets.py` implementing decisions D-01 through D-16 from CONTEXT.md as one shared parameterization, plus a synthetic-data assertion suite that pins every contract invariant before any real height map is processed. Also performs Wave 0 environment setup (no venv exists; system Python lacks numpy).

Purpose: TARGET-01 requires the contract locked and TARGET-02 requires one identical, inspection-verifiable parameterization. Encoding the contract as a constants block + pure functions with invariant tests makes both mechanically checkable, and locks all constants BEFORE any track's width ordering is observed (prohibition P1).

Output: `.venv/` environment, `src/targets.py`, `tests/test_targets.py`, all tests green.

**Contract amendments adopted a priori (ratified at this plan's Task 3 blocking checkpoint — BEFORE plan 01-02's 4-track extraction run — per RESEARCH Open Questions 1 and 3):**
- **Amendment A1 (bin-first interpretation of D-05/D-06):** the gap rule operates on the 0.2mm-binned median cross-section profile, not per native ~4µm column. RESEARCH Finding 1 proved (by direct execution on all 4 raw tracks) that the literal per-native-column rule invalidates 99.97–100% of columns on EVERY track, triggering the SPEC's all-invalid hard error phase-wide. Bin-median aggregation is aggregation within the output resolution cell, not cross-column interpolation — no information moves further along x than the 0.2mm slot the output already represents. Adopted with evidence; presented for user ratification at Task 3 before any real-track extraction is run.
- **Amendment A2 (y-boundary clip rule, new validity criterion):** a slot whose largest above-threshold run touches the first or last y-pixel of the 1.907mm measured strip is invalid — the true bead edge lies outside the measured strip and its width is unknowable. Physically motivated, adopted BEFORE any ordering numbers are computed (RESEARCH Open Question 3), affecting 26/117/13/39 bins on tracks 8/10/14/21 per the probe.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-target-extraction-contract/01-CONTEXT.md
@.planning/phases/01-target-extraction-contract/01-RESEARCH.md
@.planning/phases/01-target-extraction-contract/01-PATTERNS.md
@src/nsf_fmrg_data.py
</context>

## Artifacts this phase produces

New symbols created across this phase (both plans) — the source-grounding pass should treat these as new, not drift:

- **Module `src/targets.py`** with constants: `TARGET_GRID_START_MM`, `TARGET_GRID_STEP_MM`, `TARGET_GRID_N`, `BASELINE_PCT`, `PEAK_PCT`, `HALF_MAX_FRACTION`, `MIN_PEAK_BASELINE_SEPARATION_MM`, `MAX_GAP_PIXELS`, `SG_WINDOW_PTS`, `SG_POLYORDER`, `MIN_VALID_Y_POINTS`, `MIN_COLUMNS_PER_BIN`
- **Functions in `src/targets.py`**: `target_grid()`, `extraction_params()`, `bin_profile()`, `fill_small_gaps()`, `halfmax_edges()`, `nan_savgol()`, `extract_targets_from_arrays()`, `extract_track_targets()`
- **Test file `tests/test_targets.py`** with `test_*` functions and a `__main__` runner
- **Script `scripts/run_target_extraction.py`** (plan 02) with `snapshot_raw()`, `run_track()`, `main()`, CLI flag `--project_dir`, script constant `TRACK_POWER_W`
- **Script `scripts/check_targets.py`** (plan 02) with `check_track()`, `main()`, CLI flag `--project_dir`
- **Data artifacts** (plan 02, runtime outputs): `processed_data/targets/track_{8,10,14,21}_targets.npz` (keys `x_grid_mm`, `w_mm`, `y_upper_mm`, `y_lower_mm`, `valid_mask`), `processed_data/targets/extraction_params.json`, `processed_data/targets/qa/track_{id}_residual_map.png`, `track_{id}_overlay.png`, `track_{id}_width.png`
- **Environment**: `.venv/` virtualenv (untracked)

<tasks>

<task type="auto">
  <name>Task 1: Wave 0 — create project environment and tests directory</name>
  <files>.venv/ (untracked), tests/ (new directory)</files>
  <read_first>
    - requirements.txt (the five deps to install: numpy, scipy, matplotlib, pillow, pandas)
    - .planning/phases/01-target-extraction-contract/01-RESEARCH.md §Environment Availability (uv at /snap/bin/uv verified; system Python 3.12.3 lacks numpy)
  </read_first>
  <action>
    Create the virtualenv and install existing dependencies exactly as verified in research: run `uv venv .venv` then `uv pip install -p .venv/bin/python -r requirements.txt`. Install ONLY the packages in requirements.txt — all five are pre-approved in RESEARCH's Package Legitimacy Audit; introduce no new packages (this phase uses plain-Python assertion scripts, not pytest, because REQUIREMENTS.md scopes out a test-suite framework and RESEARCH's Validation Architecture specifies runnable assertion scripts). Create an empty `tests/` directory (it will be populated in Task 2 — do not commit a placeholder file; the Task 2 commit carries it). Do not modify requirements.txt.
  </action>
  <verify>
    <automated>.venv/bin/python -c "import numpy, scipy, matplotlib, pandas; print(numpy.__version__, scipy.__version__)"</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/bin/python` exists and the import command above exits 0 printing numpy and scipy versions
    - `requirements.txt` is byte-identical to its pre-task state (git diff shows no change)
    - No package outside requirements.txt was installed (`.venv/bin/python -m pip list` contains no pytest)
  </acceptance_criteria>
  <done>Virtualenv exists with all five requirements.txt packages importable; no new dependencies introduced.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement src/targets.py contract module with synthetic invariant tests (tests first)</name>
  <files>src/targets.py, tests/test_targets.py</files>
  <read_first>
    - src/nsf_fmrg_data.py (entire file — reuse `largest_true_run` at lines 88-97, `load_wyko_asc` at 160-202, `robust_plane_detrend` at 205-227; mirror its import style, constants block at lines 8-13, dict-return convention at 199-202, fail-fast ValueError style at line 71; note float32 allocation at line 167 and the thermal center formula at line 123)
    - .planning/phases/01-target-extraction-contract/01-RESEARCH.md §Code Examples (verified `nan_savgol` windowed-polyfit implementation and `halfmax_edges` skeleton), §Critical Empirical Findings 1-4, §Architecture Patterns 1-3
    - .planning/phases/01-target-extraction-contract/01-PATTERNS.md §src/targets.py section (import pattern, constants pattern, naming/style rules: no type hints, no docstrings, sparse comments)
    - .planning/phases/01-target-extraction-contract/01-CONTEXT.md §Implementation Decisions (D-01 through D-16 — the contract this module implements)
  </read_first>
  <behavior>
    Write tests/test_targets.py FIRST (plain-Python assertion script: `test_*` functions using bare `assert`, a `__main__` block that calls every test function and prints a PASS line per test; also importable by pytest later but requiring no pytest to run). All tests use synthetic arrays only — never raw data. Bootstrap `sys.path` with the REPO_ROOT pattern from scripts/run_thermal_video_export.py lines 7-11 (parents[1] from tests/), then `import targets`. Required tests:
    - Test 1 (grid, D-07): `targets.target_grid()` has exactly 400 slots, first value 20.1, last 99.9, uniform 0.2 step; numerically equals the sorted thermal frame-center formula built from `nsf_fmrg_data.COMMON_X_END_MM` and `THERMAL_MM_PER_FRAME` (centers 20.1…99.9).
    - Test 2 (gap rule exact boundary, D-05/D-06, SPEC edge R1-boundary): a 480-length profile with an interior run of exactly 10 NaNs → `fill_small_gaps` returns fully finite interior and ok=True; the same profile with an 11-NaN run → ok=False; a leading run of 8 NaNs → ok=True with the leading run left NaN (boundary runs are never extrapolated); a leading run of 11 → ok=False.
    - Test 3 (half-max edges, D-01/D-02/D-03): synthetic profile, baseline 0, one interior rectangular bump of height 0.02 spanning a known y index range → returned y_lower/y_upper each within one pixel-spacing of the true half-max crossings, and y_upper > y_lower.
    - Test 4 (noise-floor exact boundary, D-04, SPEC edge R1-boundary): interior bump sized so p95−p5 of the profile equals exactly `MIN_PEAK_BASELINE_SEPARATION_MM` → valid (edges returned); same construction scaled by 0.8 → invalid (None). Only strictly-below invalidates.
    - Test 5 (clip rule, Amendment A2): bump whose above-threshold run touches index 0 (and separately the last index) → invalid (None).
    - Test 6 (smoother mask fidelity, D-08/D-11, RESEARCH Pitfall 3): input vector with NaNs at known positions → `np.isfinite(nan_savgol(v))` equals `np.isfinite(v)` elementwise.
    - Test 7 (smoother exactness + crop-edge truncation, D-09/D-12): a pure quadratic input with no NaNs → output allclose to input everywhere including the first/last two positions (shrinking one-sided windows still fit a quadratic exactly with polyorder 2).
    - Test 8 (blending across masked gaps, SPEC edge R1-adjacency): quadratic input with 2 interior NaNs → finite outputs adjacent to the gap remain allclose to the quadratic, proving the window uses valid points from both sides of the gap.
    - Test 9 (single parameterization, TARGET-02 inspection): read the source text of src/targets.py via pathlib; assert a regex search for conditional equality/membership comparisons against the track identifier variable finds zero matches (pattern concept: an `if` guard or comparison operator applied to the track identifier name).
    - Test 10 (params provenance): `targets.extraction_params()` returns a dict containing exactly the 12 constants named in the action below, with the locked values.
    - Test 11 (all-invalid hard error, SPEC edges R1-empty/R2-empty): a synthetic all-flat Zd array (no bump anywhere, separations below the floor) passed to `extract_targets_from_arrays` → raises ValueError.
    - Test 12 (synthetic end-to-end): a synthetic Zd (y=480 rows, x=2000 columns at 0.004mm spacing spanning 20-28mm) containing a clean straight ridge of known width 0.6mm and height 0.02mm → `extract_targets_from_arrays` returns arrays of length 400 where slots inside x-coverage are valid, `w_mm` at valid slots is within 0.05mm of 0.6, `np.isfinite(w_mm)` equals `valid_mask`, and `w_mm` equals `y_upper_mm − y_lower_mm` at valid slots; slots outside the synthetic x-coverage are NaN with mask False (fixed-grid slots retained, D-07).
  </behavior>
  <action>
    After tests are written and failing (module absent = RED), implement `src/targets.py` to make them pass (GREEN). Follow codebase conventions exactly (PATTERNS.md): flat imports with `from nsf_fmrg_data import load_wyko_asc, robust_plane_detrend, largest_true_run`, no type hints, no docstrings, SCREAMING_SNAKE_CASE unit-suffixed constants grouped at top, snake_case verb-first functions, fail-fast built-in exceptions with explicit messages, dict-return convention.

    Constants block (locked NOW, per prohibition P1 — grounded in RESEARCH Finding 4 measured substrate statistics, never revisited after width numbers are seen):
    `TARGET_GRID_START_MM = 20.1`, `TARGET_GRID_STEP_MM = 0.2`, `TARGET_GRID_N = 400`, `BASELINE_PCT = 5.0`, `PEAK_PCT = 95.0`, `HALF_MAX_FRACTION = 0.5` (D-02/D-03), `MIN_PEAK_BASELINE_SEPARATION_MM = 0.005` (D-04; 5µm ≈ 2.5× the 2µm measured binned-profile substrate std, RESEARCH Finding 4), `MAX_GAP_PIXELS = 10` (D-05/D-06), `SG_WINDOW_PTS = 5`, `SG_POLYORDER = 2` (D-08/D-09; scipy requires polyorder < window length; order 2 chosen because a cubic on 5 points with missing samples is under-determined too often), `MIN_VALID_Y_POINTS = 50`, `MIN_COLUMNS_PER_BIN = 10` (RESEARCH Open Question 4 bin-aggregation minimums).

    Functions:
    - `target_grid()`: returns `TARGET_GRID_START_MM + TARGET_GRID_STEP_MM * np.arange(TARGET_GRID_N)` — matches the thermal frame-center formula at src/nsf_fmrg_data.py:123 by construction (RESEARCH Pattern 1), so Phase 2 pairs thermal frame k with target slot k with no interpolation.
    - `extraction_params()`: returns a dict of the 12 constants above (provenance source for extraction_params.json in plan 02).
    - `bin_profile(Zd, x_actual_mm, x_center)`: boolean-select native columns in `[x_center − 0.1, x_center + 0.1)`; return None when fewer than `MIN_COLUMNS_PER_BIN` columns; else return `np.nanmedian` across those columns at each y (a 480-length binned median cross-section). One short comment noting the bin-first amendment: D-05/D-06 operate on this binned profile because the per-native-column rule invalidates ~100% of columns on every track (RESEARCH Finding 1); aggregation stays within the 0.2mm output cell.
    - `fill_small_gaps(prof)`: detect all NaN runs (adapt the index/diff/run-boundary technique of `largest_true_run` to enumerate every run, not just the largest). Any run longer than `MAX_GAP_PIXELS` (interior OR leading/trailing) → return `(prof, False)` marking the slot invalid (D-06). Interior runs of length ≤ `MAX_GAP_PIXELS` → fill by linear interpolation along y via `np.interp` restricted to that run's anchor neighbors (D-05: 1-D along y only, never cross-column). Leading/trailing runs ≤ `MAX_GAP_PIXELS` are left NaN (no extrapolation — RESEARCH anti-pattern) without invalidating. Return `(filled, True)`.
    - `halfmax_edges(prof, y_mm)`: operate on finite values only. Return None (invalid) when finite count < `MIN_VALID_Y_POINTS`. Compute `base = np.percentile(vals, BASELINE_PCT)` and `peak = np.percentile(vals, PEAK_PCT)` using numpy's default linear method — no `method=` argument (SPEC edge R1-precision). Return None when `(peak − base) < MIN_PEAK_BASELINE_SEPARATION_MM` (strict less-than: exactly-at-minimum is valid, D-04 / SPEC edge R1-boundary). Threshold `thr = base + HALF_MAX_FRACTION * (peak − base)` (D-01). Boolean above-mask (NaN → False), largest contiguous run via `largest_true_run` (D-01 half-max convention). Return None when no run exists, or when the run touches index 0 or the last index (Amendment A2 clip rule — one short comment: true edge lies outside the measured 1.907mm strip, rule fixed a priori per RESEARCH Open Question 3). Edge positions: linearly interpolate the exact threshold-crossing y between the run's boundary pixel and its outside neighbor; fall back to the boundary pixel's own y when the neighbor sample is NaN. Return None when `y_upper <= y_lower` (crossed/degenerate → invalid, never a negative or clipped-to-zero width — SPEC edge R1-precision). Else return `(y_lower, y_upper)`.
    - `nan_savgol(v)`: the RESEARCH §Code Examples windowed-polyfit implementation verbatim in spirit — for each position: skip (leave NaN) when input is not finite (D-11); gather the `SG_WINDOW_PTS` window truncated at array ends (D-12 — shrinking window at the 20/100mm crop edges, which resolves the CONTEXT discretion item about scipy boundary modes: scipy's `savgol_filter` cannot be used at all because it is not NaN-aware, RESEARCH Finding 2); drop NaN entries (window may therefore blend valid points from both sides of a masked gap — SPEC edge R1-adjacency); pass through unsmoothed when fewer than 3 finite points; else `np.polyfit` of degree `min(SG_POLYORDER, count−1)` on window offsets and `np.polyval` at 0.
    - `extract_targets_from_arrays(Zd, x_actual_mm, y_mm)`: the pure array-level pipeline (testable with synthetic data). Build `x_grid = target_grid()`; allocate `y_upper_raw`/`y_lower_raw` as NaN float64 length 400. Per slot: `bin_profile` → None means slot stays invalid; else `fill_small_gaps` → not-ok means invalid (D-06); else `halfmax_edges` → None means invalid; else record both edges. Smooth `y_upper_raw` and `y_lower_raw` SEPARATELY with `nan_savgol` (D-10 — width derived from smoothed boundaries afterward, keeping boundary curves available for the deferred TARGET-03), then `w = y_upper_s − y_lower_s`, `valid_mask = np.isfinite(w)`. Assert `np.array_equal(np.isfinite(y_upper_s), np.isfinite(y_upper_raw))` (smoothing neither fabricates nor drops validity — D-11, RESEARCH Pitfall 3). Raise `ValueError` with an explicit message when `valid_mask.sum() == 0` (SPEC all-invalid hard error). Return dict `{'x_grid_mm', 'w_mm', 'y_upper_mm', 'y_lower_mm', 'valid_mask'}` (smoothed boundaries persisted; dict-return convention).
    - `extract_track_targets(height_dir, track_id)`: I/O wrapper. `load_wyko_asc(height_dir, track_id)`; raise `ValueError` unless the resolved file's basename equals the expected `Heightmap_<id>.ASC` name built by f-string from the track identifier (guards `find_track_file` substring fallback, RESEARCH threat table / PITFALLS Pitfall 4); raise `ValueError` when the returned header dict lacks the `pixel_size_mm` key (loader would have silently used its hardcoded fallback — PITFALLS Pitfall 10, ASVS V5 loud input validation); call `robust_plane_detrend(Z_mm, x_actual_mm, y_mm)` with its DEFAULT strides, unchanged (D-13/D-16), exactly once per full cropped track before any edge detection (D-15); raise `ValueError` when the returned plane coefficients are None (degenerate fallback at src/nsf_fmrg_data.py:213-214). Call `extract_targets_from_arrays` on the detrended map and return its dict extended with `'track_id'`, `'file'`, `'Z_mm'` (raw), `'Zd_mm'` (detrended), `'x_actual_mm'`, `'y_mm'` — raw and detrended maps ride along so plan 02's QA plots never reload the ~265MB files.

    Commit discipline (task-level TDD): first commit the failing tests (`test(01-01): add failing contract-invariant tests for targets module`), then the implementation (`feat(01-01): implement TARGET-01 extraction contract in src/targets.py`).
  </action>
  <verify>
    <automated>.venv/bin/python tests/test_targets.py</automated>
  </verify>
  <!-- The two constant-assignment literals below are POSITIVE existence greps (>= 1), and the
       constants must legitimately appear verbatim in src/targets.py — allowlisted per #429: -->
  <!-- planner-discipline-allow: MIN_PEAK_BASELINE_SEPARATION_MM = 0.005 -->
  <!-- planner-discipline-allow: MAX_GAP_PIXELS = 10 -->
  <acceptance_criteria>
    - `.venv/bin/python tests/test_targets.py` exits 0 and prints one PASS line per test (12 tests)
    - `src/targets.py` contains all 12 constants: `grep -c 'MIN_PEAK_BASELINE_SEPARATION_MM = 0.005' src/targets.py` ≥ 1 and `grep -c 'MAX_GAP_PIXELS = 10' src/targets.py` ≥ 1
    - Single shared parameterization: `grep -vE '^\s*#' src/targets.py | grep -cE 'if +track_id|track_id +(==|!=|<|>| in )'` returns 0 (no track-conditional extraction logic)
    - `src/targets.py` defines all 8 functions: `grep -cE '^def (target_grid|extraction_params|bin_profile|fill_small_gaps|halfmax_edges|nan_savgol|extract_targets_from_arrays|extract_track_targets)\(' src/targets.py` returns 8
    - No scipy savgol usage: `grep -vE '^\s*#' src/targets.py | grep -c 'savgol_filter'` returns 0 (RESEARCH Finding 2 — it NaN-poisons masked data)
    - `np.percentile` is called without a `method=` argument (numpy default linear interpolation — SPEC edge R1-precision): `grep -c 'method=' src/targets.py` returns 0
    - No docstrings and no library type hints (codebase convention): `grep -c '"""' src/targets.py` returns 0
  </acceptance_criteria>
  <done>All 12 synthetic contract-invariant tests pass; the module implements D-01–D-16 plus amendments A1/A2 with one shared constants block; two atomic commits (test then feat) exist.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Pre-compute gate — user ratifies contract amendments A1/A2 before plan 01-02's extraction run</name>
  <what-built>
    src/targets.py implements the locked D-01–D-16 contract plus two a-priori amendments (A1 bin-first gap rule, A2 y-boundary clip rule), with all 12 synthetic contract-invariant tests green. No real height map has been processed yet. This gate runs mid-phase deliberately, as an exception to the end-of-phase human_verify_mode: A1 reinterprets locked decisions D-05/D-06 and A2 adds a validity criterion not present in D-01–D-16, and plan 01-02's full 4-track extraction run (~5-10 minutes plus QA review) would be invalidated if ratification were deferred to end-of-phase and then refused. This checkpoint must NOT be auto-approved (auto_advance/yolo does not apply to contract-amendment ratification).
  </what-built>
  <how-to-verify>
    Review each amendment against the research evidence, then ratify or amend the contract wording:

    1. Amendment A1 — bin-first interpretation of D-05/D-06: the gap rule operates on the 0.2mm-binned median cross-section profile, not per native ~4µm column. Evidence (01-RESEARCH.md Finding 1, measured by direct execution on all 4 raw tracks):

       | Track | Bad-pixel fraction | Native columns violating the >10-NaN rule | Valid 0.2mm bins under the bin-first rule |
       |-------|--------------------|-------------------------------------------|-------------------------------------------|
       | 8 (400W) | 36.9% | 99.97% | 374/400 |
       | 10 (350W) | 51.6% | 100.00% | 288/400 |
       | 14 (300W) | 51.1% | 100.00% | 314/400 |
       | 21 (200W) | 55.6% | 99.99% | 360/400 |

       The literal per-native-column reading triggers the SPEC's all-invalid hard error on EVERY track. Bin-median aggregation stays within the 0.2mm output cell — no information moves further along x than the slot the output already represents (01-RESEARCH.md Open Question 1).

    2. Amendment A2 — y-boundary clip rule (new validity criterion): a slot whose largest above-threshold run touches the first or last y-pixel of the 1.907mm measured strip is invalid — the true bead edge lies outside the measured strip and its width is unknowable. Affected bins per the research probe (01-RESEARCH.md Open Question 3): track 8: 26, track 10: 117, track 14: 13, track 21: 39.

    3. What this gate is NOT: no width or ordering number is presented or considered here. All extraction constants stay locked from measured substrate statistics (prohibition P1) and will not be tuned regardless of plan 01-02's ordering outcome — this gate ratifies contract WORDING only, before any ordering evidence exists in this phase's official run.
  </how-to-verify>
  <resume-signal>Type "approved" to ratify A1 and A2 as accepted contract interpretations, or describe the amendment you want instead — the executor then updates src/targets.py and tests/test_targets.py, re-runs tests/test_targets.py to green, and records the amended wording before plan 01-02 executes.</resume-signal>
  <action>Present the A1/A2 evidence from the how-to-verify block above to the user verbatim (both tables) and STOP — wait for the user's explicit response. Do not proceed to any further work, do not start plan 01-02, and do not treat auto-advance/yolo as approval. If the user amends either rule: apply the amendment to src/targets.py and tests/test_targets.py, re-run the test suite to green, and record the amended wording in the SUMMARY; constants still remain locked against ordering output either way (prohibition P1).</action>
  <verify>
    <automated>.venv/bin/python tests/test_targets.py</automated>
    <human-check>The user explicitly ratified A1/A2 (typed "approved") or specified an amendment that has been implemented and re-tested — recorded before plan 01-02 starts.</human-check>
  </verify>
  <done>User ratification (or amendment) of A1/A2 is recorded; tests remain green; plan 01-02 is unblocked with settled contract wording and zero real-track compute spent.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| raw `.ASC` files → parser | irreplaceable, unversioned scientific input crosses into parsing/extraction code |
| filename pattern → track identity | `find_track_file` substring fallback can silently resolve the wrong track's file |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-01 | Tampering (accidental) | anything touching `data/raw/` | high | mitigate | This plan's tests use synthetic arrays exclusively and never open raw files; library opens raw files read-only via `load_wyko_asc`; mechanical before/after snapshot enforcement lands in plan 02 |
| T-01-02 | Spoofing (identity confusion) | `extract_track_targets` file resolution | medium | mitigate | Hard `ValueError` unless resolved basename exactly matches the expected per-track Heightmap filename (PITFALLS Pitfall 4) |
| T-01-03 | Information disclosure | git history vs. competition-restricted data | medium | mitigate | Task commits stage only `src/targets.py` and `tests/test_targets.py`; nothing under `data/raw/` is ever staged; `git log --all -- data/raw` emptiness is re-verified in Phase 5 (SUBMIT-02 path) |
| T-01-04 | Tampering (malformed input) | `.ASC` header parsing | low | mitigate | `ValueError` when header lacks `pixel_size_mm` instead of silently accepting the loader's hardcoded fallback (ASVS V5, PITFALLS Pitfall 10) |
| T-01-SC | Tampering | pip installs | high | mitigate | Only requirements.txt packages installed — all five carry OK verdicts in RESEARCH's Package Legitimacy Audit; zero new/[ASSUMED]/[SUS] packages, so no legitimacy checkpoint is required |
</threat_model>

<verification>
- `.venv/bin/python tests/test_targets.py` exits 0 (all 12 invariant tests green)
- `.venv/bin/python -c "import sys; sys.path.append('src'); import targets; print(len(targets.target_grid()))"` prints 400
- Code inspection greps in Task 2 acceptance criteria all pass (single parameterization, no scipy savgol, no percentile method override)
- Task 3 checkpoint resolved: the user ratified A1/A2 (or the amended wording is implemented and tests re-run green) before plan 01-02 begins
</verification>

<success_criteria>
- TARGET-01 contract (D-01–D-16 + documented amendments A1/A2) exists as executable, inspectable code with every constant locked before any real-track width is computed
- Amendments A1/A2 user-ratified at the Task 3 blocking gate before plan 01-02 spends compute on the 4-track run
- Every SPEC R1 edge (boundary/adjacency/empty/precision) has a passing synthetic test
- Environment ready for plan 02's full-pipeline run
</success_criteria>

<output>
Create `.planning/phases/01-target-extraction-contract/01-01-SUMMARY.md` when done. Record: constants locked (with grounding), the Task 3 ratification outcome for amendments A1/A2 (approved as-is, or the amended wording the user chose), test count and results.
</output>
