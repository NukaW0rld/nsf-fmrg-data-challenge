---
phase: 01-target-extraction-contract
plan: 13
type: execute
wave: 12
depends_on: ["01-12"]
files_modified:
  - scripts/diagnose_track10_tail_collapse.py
  - processed_data/diagnostics/track10_tail_collapse_diagnosis.csv
  - src/nsf_fmrg_data.py
  - src/targets.py
  - tests/test_nsf_fmrg_data.py
  - tests/test_targets.py
  - .planning/phases/01-target-extraction-contract/01-13-CRITERION.md
  - .planning/phases/01-target-extraction-contract/01-13-ORDERING-OUTCOME.md
  - .planning/phases/01-target-extraction-contract/01-CONTEXT.md
autonomous: true
gap_closure: true
gap_ids: [G-01-4]
requirements: [TARGET-01, TARGET-02]
tags: [profilometry, detrending, polynomial-fit, numpy]
must_haves:
  truths:
    - "01-13-CRITERION.md pre-registers a falsifiable, outcome-independent criterion targeting the x-direction manufactured shape-gap growth toward the domain's far edge (x=100mm), committed to git before any change exists in src/nsf_fmrg_data.py or src/targets.py -- mirroring 01-11-CRITERION.md's discipline exactly."
    - "Exactly one uniform, track-independent change (a cap on the x-exponent of interaction/cross-track monomial terms in robust_plane_detrend, DETREND_MAX_XY_DEGREE) is applied, selected because it clears the pre-registered criterion on measured evidence -- never because it flips the 10-vs-14 width ordering."
    - "The new cap leaves every pure along-track (y-exponent=0) term fully intact at every x-degree up to DETREND_POLY_ORDER, so Amendment A3's quartic bow-removal capacity is structurally unaffected -- test_order_four_removes_quartic_bow stays green."
    - "The new cap does not regress Amendment A5's y-edge fix -- test_detrend_does_not_diverge_at_strip_edge stays green with DETREND_MAX_XY_DEGREE applied on top of DETREND_MAX_Y_DEGREE."
    - "extraction_params() (17 keys) and its SHA-256 provenance digest are sensitive to the new constant; Amendment A6 records it in 01-CONTEXT.md, dated, citing the a priori evidence and reaffirming the no-outcome-driven-tuning prohibition."
    - "All 4 tracks are regenerated atomically from one run_target_extraction.py invocation; the 8>10>14>21 ordering outcome and every track's valid fraction are reported honestly regardless of result, with no locked constant changed in response."
    - "If no shared DETREND_MAX_XY_DEGREE value clears the pre-registered criterion without regressing the bow-removal regression or requiring a per-track value, no fix is applied, and the report explicitly recommends accepting the 10-vs-14 FLAG as a documented, investigated, known limitation (option a) -- per the user's explicit 'no further open-ended tuning cycle' mandate for this defect class."
  artifacts:
    - "scripts/diagnose_track10_tail_collapse.py"
    - "processed_data/diagnostics/track10_tail_collapse_diagnosis.csv"
    - ".planning/phases/01-target-extraction-contract/01-13-CRITERION.md"
    - ".planning/phases/01-target-extraction-contract/01-13-ORDERING-OUTCOME.md"
    - "src/nsf_fmrg_data.py"
    - "src/targets.py"
    - ".planning/phases/01-target-extraction-contract/01-CONTEXT.md (Amendment A6)"
    - "processed_data/targets/track_{8,10,14,21}_targets.npz"
    - "processed_data/targets/qa/ (12 regenerated PNGs)"
    - "processed_data/targets/extraction_params.json"
    - "processed_data/targets/manifest.json"
  key_links:
    - "01-13-CRITERION.md -> the applied fix: the criterion must exist in git history before the fix commit, or the no-outcome-driven-tuning prohibition loses its structural enforcement."
    - "robust_plane_detrend's new max_xy_degree cap -> extract_track_targets's shared detrend call -> halfmax_edges' per-column candidate set: whether the fitted surface manufactures an x-localized edge feature determines whether track 10's true bead candidate survives in the x=70-100mm tail."
    - "new contract constant -> extraction_params() -> extraction_params.json -> manifest.json SHA-256 digest: a mechanism outside the provenance digest is an undocumented contract change."
    - "run_target_extraction.py atomic publish -> check_targets.py MIN_VALID_FRACTION gate: the regenerated artifacts must not regress track 10's already-cleared 60.5% valid fraction established by Amendment A5."
    - "diagnostic output directory -> run_target_extraction.publish_staging_dir: the CSV must live in a sibling directory (processed_data/diagnostics/), never under processed_data/targets/, or Task 3's regeneration destroys it."
  prohibitions:
    - statement: "Extraction contract constants (DETREND_MAX_XY_DEGREE and every already-locked constant) must NOT be adjusted based on whether the resulting 10-vs-14 width ordering looks correct."
      status: resolved
      verification: judgment
    - statement: "No per-track branches or per-track-tuned constants may exist in src/targets.py, src/nsf_fmrg_data.py, or scripts/run_target_extraction.py -- one shared parameterization across all 4 tracks."
      status: resolved
      verification: test
    - statement: "Do NOT relax D-01/D-03's boundary-clipped-run exclusion, and do NOT lower DETREND_MAX_Y_DEGREE, DETREND_POLY_ORDER, or any other already-locked Amendment A3/A4/A5 constant in an attempt to chase this defect."
      status: resolved
      verification: judgment
    - statement: "Extraction and diagnostic code must NOT write to, delete, or modify any file under data/raw/."
      status: resolved
      verification: test
---

<objective>
Close UAT gap G-01-4 (status: failed, severity: major): after Amendment A5, the 8>10>14>21 width-ordering criterion still FLAGs on the 10-vs-14 pair (track 10 median 0.3713mm < track 14 median 0.4765mm). `.planning/debug/track10-14-ordering-tail-collapse.md` -- the ONE bounded, diagnosis-only investigation the user explicitly authorized -- confirmed this is an EXTRACTION ARTIFACT, not genuine physical narrowing: track 10's raw within-column bead contrast stays flat across the whole 20-100mm scan, but its raw along-track baseline drift (~1.7mm, ~40-85x larger than the other 3 tracks) forces the shared quartic-in-x detrend surface to nearly reproduce that entire drift, and the fit's own y-shape prediction error (isolated directly, not just residual noise) grows systematically toward the domain's far edge (x=100mm) -- a Runge-type polynomial edge effect along x, analogous to the y-direction risk `DETREND_MAX_Y_DEGREE` already caps, but here uncapped. This manufactures a low-y artifact that increasingly rivals track 10's true (much smaller) bead signal specifically in the x=70-100mm tail, raising the per-column threshold until the true bead candidate shrinks to a few points or vanishes entirely -- confirmed by full sequential reproduction that `halfmax_edges`/continuity tracking behave exactly as designed on this corrupted input.

Per the phase's HONEST-OUTCOME GUARD and the user's explicit authorization of exactly one further bounded, criterion-driven cycle (no further open-ended tuning), this plan: (1) measures the a priori evidence needed to pre-register a falsifiable, outcome-independent fix-selection criterion targeting the x-direction shape-gap growth, committed to git before any source change; (2) implements exactly one uniform, track-independent mechanism selected by that criterion; (3) regenerates all 4 tracks atomically and reports the 10-vs-14 outcome honestly, explicitly discussing the option-(a) fallback (accept as a documented known limitation) if the FLAG persists.

Purpose: TARGET-02 requires one shared, non-per-track-tuned extraction method whose expected 8>10>14>21 width ordering is either honestly restored on unforced evidence, or honestly and finally documented as an accepted, investigated limitation -- never chased with an open-ended sequence of constant tweaks.
Output: A committed criterion, at most one uniform contract change recorded as Amendment A6, atomically regenerated artifacts for all 4 tracks, and an honest ordering-outcome report with an explicit fallback recommendation if unresolved.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-target-extraction-contract/01-CONTEXT.md
@.planning/phases/01-target-extraction-contract/01-UAT.md
@.planning/phases/01-target-extraction-contract/01-11-CRITERION.md
@.planning/phases/01-target-extraction-contract/01-11-ORDERING-OUTCOME.md
@.planning/debug/track10-14-ordering-tail-collapse.md
@src/targets.py
@src/nsf_fmrg_data.py
@scripts/run_target_extraction.py
@scripts/diagnose_track10_coverage.py
@tests/test_targets.py
@tests/test_nsf_fmrg_data.py

<interfaces>
Current state after Amendment A5 (plan 01-11) -- do not regress:

- `robust_plane_detrend(Z_mm, x_mm, y_mm, stride_x=40, stride_y=2, order=1, fit_mask=None, max_y_degree=None)` (`src/nsf_fmrg_data.py:224-274`) builds a total-degree monomial basis `exponents = [(i, degree-i) for degree in range(order+1) for i in range(degree+1)]` (241-245), then filters `exponents = [(i, j) for i, j in exponents if j <= max_y_degree]` when `max_y_degree is not None` (246-247). Coordinates are centered but not scaled (248-251). Returns `(Z_mm - plane, coef)` or `(Z_mm.copy(), None)` on the `<100`-valid-sample degenerate fallback.
- `src/targets.py` locks `DETREND_POLY_ORDER = 4` and `DETREND_MAX_Y_DEGREE = 2` (constants block, lines 7-41), passed identically for all 4 tracks from `extract_track_targets` (lines 299-315) alongside `fit_mask = bead_exclusion_mask(data['Z_mm'])`.
- `extraction_params()` (lines 51-69) currently returns 16 keys; `test_extraction_params_provenance` in `tests/test_targets.py` pins the exact dict.
- At `order=4, max_y_degree=2`, the surviving basis has 12 terms; the surviving CROSS terms (y-exponent >= 1) are `(1,1), (2,1), (3,1), (1,2), (2,2)` -- their x-exponents (1,2,3,1,2) are NOT capped by `max_y_degree`, since that filter only bounds the y-exponent `j`, not the x-exponent `i`. This is the gap G-01-4's root cause exploits: terms like `(3,1)` let the y-linear-slope coefficient vary as a cubic in x, which can diverge disproportionately near the domain's far edge for a track whose fit must reproduce an unusually large x-magnitude drift.
</interfaces>
</context>

<assumption_delta_decision>
This plan adds a second, narrower cross-track degree control alongside the existing `max_y_degree`. The primary noun does not change identity (the detrend surface still has one along-track degree of record, `order`) -- this is an additive refinement, not a singular-to-plural identity transition. Decision: **extend**, not promote. `max_xy_degree` caps the x-exponent only for terms whose y-exponent is >= 1 (cross/interaction terms), leaving `order` and `max_y_degree`'s existing meaning completely unchanged; a caller that never sets `max_xy_degree` is bit-for-bit unaffected. `tests/test_nsf_fmrg_data.py::test_polynomial_basis_sizes_are_stable` already pins basis sizes at `order=1` and `order=4`/`max_y_degree=2`; extend it (do not replace it) to also pin the further-reduced sizes at `max_xy_degree` in `{0, 1, 2}`.
</assumption_delta_decision>

## Source Coverage Audit

| Source | ID | Plan coverage | Task | Status |
|---|---|---|---|---|
| GOAL | UAT Test 5 / G-01-4 | Pre-registered criterion + exactly-one-cycle fix for the 10-vs-14 tail collapse, honest report with fallback discussion | Task 1, 2, 3 | COVERED |
| REQ | TARGET-01 | The new cap applies as one shared parameterization (no per-track branching) | Task 2 | COVERED |
| REQ | TARGET-02 | Real 4-track regeneration re-establishes (or honestly fails to re-establish) the expected width ordering under one shared, non-per-track-tuned method | Task 3 | COVERED |
| RESEARCH / debug | track10-14-ordering-tail-collapse.md's confirmed root cause (x-direction Runge-type edge effect in cross terms) | Directly targeted by the DETREND_MAX_XY_DEGREE mechanism | Task 1, 2 | COVERED |
| CONTEXT | Amendment A3 (along-track quartic bow removal) | Preserved by construction (pure along-track terms never capped) and regression-tested | Task 2 | COVERED |
| CONTEXT | Amendment A5 (y-edge fix, DETREND_MAX_Y_DEGREE=2) | Preserved and regression-tested (`test_detrend_does_not_diverge_at_strip_edge` stays green) | Task 2 | COVERED |
| CONTEXT | HONEST-OUTCOME GUARD / no-outcome-driven-tuning prohibition | Criterion committed before any src change; Task 3 reports honestly and never re-runs to compare outcomes | Task 1, 3 | COVERED |
| CONTEXT | User's "no further open-ended tuning cycle" mandate (01-UAT.md Test 5) | Task 3 explicitly discusses and recommends the option-(a) fallback if the criterion cannot be cleared | Task 3 | COVERED |

G-01-5 (boundary fragmentation) is out of scope for this plan -- see `01-14-PLAN.md`, the next wave.

<tasks>

<task type="auto">
  <name>Task 1: Measure the x-direction shape-gap evidence and pre-register the fix-selection criterion</name>
  <files>scripts/diagnose_track10_tail_collapse.py, processed_data/diagnostics/track10_tail_collapse_diagnosis.csv, .planning/phases/01-target-extraction-contract/01-13-CRITERION.md</files>
  <read_first>
    - .planning/debug/track10-14-ordering-tail-collapse.md (full diagnosis; the Evidence entries defining the "low-y-band vs bead-band" plane-value metric and the bands `y<=0.5mm` / `0.7mm<=y<=1.3mm` this task's script must reproduce as committed, re-runnable code, and the confirmed root-cause narrative)
    - .planning/phases/01-target-extraction-contract/01-11-CRITERION.md (the exact precedent structure and discipline this task mirrors: criterion committed strictly before any src change, "REJECTS" language, non-recoverable branch, fixed tie-break rule)
    - src/nsf_fmrg_data.py:224-274 (`robust_plane_detrend` -- exponents construction 241-245, existing `max_y_degree` filter 246-247, centered coordinates 248-251)
    - src/targets.py:7-41 (constants block, `DETREND_POLY_ORDER`/`DETREND_MAX_Y_DEGREE` and their Amendment A3/A5 comments as the style to match), :233-256 (`bead_exclusion_mask`), :299-315 (`extract_track_targets`'s current detrend call)
    - scripts/diagnose_track10_coverage.py (structural precedent to mirror exactly: sys.path bootstrap comment, `snapshot_raw`/`raw_snapshot_diff` bracketing, argparse `--project_dir` main, print-only feedback, no docstrings, SCREAMING_SNAKE_CASE constants)
  </read_first>
  <action>
    Create `scripts/diagnose_track10_tail_collapse.py` mirroring `diagnose_track10_coverage.py`'s structure exactly. Bracket the run with `snapshot_raw`/`raw_snapshot_diff` and print `data/raw/ integrity PASS` exactly as the existing diagnostics do.

    For EACH track in `TRACK_IDS`, using the exact CURRENT production path -- `fit_mask = bead_exclusion_mask(Z_mm)`; `Zd, coef = robust_plane_detrend(Z_mm, x_actual_mm, y_mm, order=DETREND_POLY_ORDER, fit_mask=fit_mask, max_y_degree=DETREND_MAX_Y_DEGREE)` (the shipped Amendment A5 contract, no new parameter, since none exists yet) -- compute `plane = Z_mm - Zd`.

    Define module constants `LOW_Y_BAND_MM = (0.0, 0.5)` and `BEAD_BAND_MM = (0.7, 1.3)`, matching the debug session's already-validated bands (identical native y-grid and `Y_STRIP_EXTENT_MM = 1.907` across all four tracks -- these are grid-geometry constants, not per-track tuning). For x positions nearest to `{30, 40, 50, 60, 70, 80, 90, 95, 99}` mm (resolve each via `argmin` against `x_actual_mm`), compute `shape_gap(x) = median(plane[low_y_rows, x_col]) - median(plane[bead_rows, x_col])`.

    Report, per track: `shape_gap` at every sampled x position, `interior_gap` = `shape_gap` at x~60mm (the domain's interior midpoint), `far_edge_gap` = `shape_gap` at x~99mm (the domain's far edge), and `departure = abs(far_edge_gap - interior_gap)`. Write one row per track to `processed_data/diagnostics/track10_tail_collapse_diagnosis.csv` (create the directory with `mkdir(parents=True, exist_ok=True)`; it is a SIBLING of `processed_data/targets/`, never a child, so Task 3's `publish_staging_dir` rename-and-rmtree cannot destroy it -- validate through `resolve_output_path` exactly once). Print the table.

    HARD CONSTRAINT -- no fix-outcome observation: this script computes only the EXISTING production detrend's shape-gap departure. It must NOT implement any new cap, must NOT compute any candidate-cap variant, and must NOT compute a median width, mean width, ordering verdict, or valid fraction. This is a property of the currently-shipped fitted surface, not of a fix outcome. `grep -Ein "median_width|ordering|verdict|max_xy" scripts/diagnose_track10_tail_collapse.py` must find nothing.

    Using this measured evidence, write and commit `.planning/phases/01-target-extraction-contract/01-13-CRITERION.md` BEFORE Task 2 begins -- at that commit, `git status --porcelain src/targets.py src/nsf_fmrg_data.py` and `git diff HEAD --stat -- src/targets.py src/nsf_fmrg_data.py` must both be empty. The document must contain, in this order, mirroring `01-11-CRITERION.md`'s structure:

    1. The criterion: the fitted background surface's own y-shape dependence on x (measured as `shape_gap(x)`, defined identically to the script's metric) must not depart, at the domain's far edge (x~99mm), from its own value at the domain's interior midpoint (x~60mm) by more than a named numeric tolerance, on all four tracks, under one shared constant. Derive the tolerance from THIS task's measured departures: separate the healthy tracks' departures from track 10's with comfortable margin on both sides, mirroring exactly how `01-11-CRITERION.md` derived its `0.05mm` value from the analogous y-edge-vs-midpoint population -- do not reuse `0.05mm` verbatim without checking it against these freshly measured numbers first, since this metric's natural scale (the fitted plane's own y-shape value) may differ from `01-11-CRITERION.md`'s row-median-based quantity.
    2. The rejection clause: a candidate is not acceptable merely because it restores `8>10>14>21`, merely because it raises any track's valid fraction, or merely because a QA plot looks better. Restate that D-01/D-03's boundary-clipped-run exclusion is not an admissible candidate to relax.
    3. The candidate mechanism: capping the x-exponent of monomial terms whose y-exponent is >= 1 (cross/interaction terms only) in `robust_plane_detrend`'s basis, leaving every pure along-track term (y-exponent=0) fully intact at every x-degree up to `DETREND_POLY_ORDER` so Amendment A3's bow removal is structurally unaffected, and leaving pure cross-track terms (x-exponent=0) governed only by the existing `DETREND_MAX_Y_DEGREE` cap. Name the constant of record `DETREND_MAX_XY_DEGREE` and the keyword `max_xy_degree` on `robust_plane_detrend`, defaulting to `None` (today's behavior). State explicitly that the "basis conditioning" mechanism is not re-considered here: `01-11-CRITERION.md` already established by measurement that column rescaling cannot change a full-rank least-squares fit's fitted values for ANY axis, so there is one viable candidate mechanism, not a manufactured choice among several.
    4. Evidence that would REJECT the candidate: no shared `DETREND_MAX_XY_DEGREE` value clears the tolerance on all four tracks simultaneously; OR the chosen value regresses `test_order_four_removes_quartic_bow`'s >=100x residual-reduction assertion; OR it regresses `test_detrend_does_not_diverge_at_strip_edge` (Amendment A5's own y-edge fix); OR it requires a per-track value.
    5. The non-recoverable branch: state plainly that `.planning/debug/track10-14-ordering-tail-collapse.md` already confirmed this is a detrend-fitting artifact (option b), not genuine physical narrowing -- the "is it physical" non-recoverable branch does not apply here. Define this plan's OWN non-recoverable condition instead: if Task 2 cannot find any `DETREND_MAX_XY_DEGREE` value satisfying Section 1 under Section 4's constraints, Task 2 applies no fix, and Task 3 reports the unresolved outcome. State explicitly, citing `01-UAT.md` Test 5's exact language, that this is the phase's LAST authorized bounded, criterion-driven cycle for this defect class -- if the criterion cannot be cleared, Task 3 must recommend accepting the 10-vs-14 FLAG as a documented, investigated, known limitation (option a) rather than proposing a further cycle.
    6. The tie-break/selection rule, fixed now: since candidate values of `DETREND_MAX_XY_DEGREE` form an ordered scale (smaller values remove more fitting capacity), prefer the LARGEST value that clears the criterion -- mirroring Amendment A5's own reasoning for `DETREND_MAX_Y_DEGREE=2` -- and this rule is not to be revisited by comparing resulting widths or ordering.

    Implement nothing in this task. Do not edit `src/`. Do not run the extractor.
  </action>
  <verify>
    <automated>.venv/bin/python -m py_compile scripts/diagnose_track10_tail_collapse.py && .venv/bin/python scripts/diagnose_track10_tail_collapse.py --project_dir . && test -f processed_data/diagnostics/track10_tail_collapse_diagnosis.csv && .venv/bin/python -c "from pathlib import Path; c=Path('processed_data/diagnostics/track10_tail_collapse_diagnosis.csv').resolve(); t=Path('processed_data/targets').resolve(); assert not c.is_relative_to(t), c; print('DIAGNOSTICS OUTSIDE PUBLISHED TREE')" && test -f .planning/phases/01-target-extraction-contract/01-13-CRITERION.md && test -z "$(git status --porcelain src/targets.py src/nsf_fmrg_data.py)" && test -z "$(git diff HEAD --stat -- src/targets.py src/nsf_fmrg_data.py)" && grep -qi "reject" .planning/phases/01-target-extraction-contract/01-13-CRITERION.md && grep -q "DETREND_MAX_XY_DEGREE" .planning/phases/01-target-extraction-contract/01-13-CRITERION.md && test -z "$(grep -Ein 'median_width|ordering|verdict|max_xy' scripts/diagnose_track10_tail_collapse.py)" && test -z "$(git status --porcelain data/raw)" && echo "CRITERION PRE-REGISTERED, EXTRACTION SOURCE CLEAN"</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/diagnose_track10_tail_collapse.py` exists, compiles, and runs to completion printing `data/raw/ integrity PASS`.
    - `processed_data/diagnostics/track10_tail_collapse_diagnosis.csv` contains one row per track (8, 10, 14, 21) with `shape_gap` at every sampled x position, `interior_gap`, `far_edge_gap`, and `departure`.
    - The CSV's resolved path is NOT inside `processed_data/targets/`, proven by the `is_relative_to` assertion printing `DIAGNOSTICS OUTSIDE PUBLISHED TREE` and exiting 0.
    - `grep -Ein "median_width|ordering|verdict|max_xy" scripts/diagnose_track10_tail_collapse.py` finds nothing -- the diagnostic observes only the currently-shipped fit, no fix outcome.
    - `01-13-CRITERION.md` exists and is committed; at that commit, `git status --porcelain src/targets.py src/nsf_fmrg_data.py` and `git diff HEAD --stat -- src/targets.py src/nsf_fmrg_data.py` are both empty.
    - The criterion names a concrete numeric tolerance derived from this task's own measured departures (not reused blindly from `01-11-CRITERION.md`'s 0.05mm without checking it against these numbers), names `DETREND_MAX_XY_DEGREE`/`max_xy_degree` as the sole candidate mechanism with its rejection evidence, states this plan's own non-recoverable condition and the option-(a)-fallback instruction, and fixes the largest-value tie-break rule.
    - `git status --porcelain data/raw` is empty.
  </acceptance_criteria>
  <done>The x-direction shape-gap evidence is measured by committed, re-runnable code, and an outcome-independent selection criterion exists in git history before any extraction-source change -- so the eventual fix cannot have been chosen by its effect on the 10-vs-14 ordering.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement the DETREND_MAX_XY_DEGREE cap, verify against the criterion, and record Amendment A6</name>
  <files>src/nsf_fmrg_data.py, src/targets.py, tests/test_nsf_fmrg_data.py, tests/test_targets.py, .planning/phases/01-target-extraction-contract/01-CONTEXT.md</files>
  <read_first>
    - .planning/phases/01-target-extraction-contract/01-13-CRITERION.md (Task 1's committed criterion -- implement exactly what it selects and nothing else)
    - src/nsf_fmrg_data.py:224-274 (`robust_plane_detrend`: exponents construction 241-245, existing `max_y_degree` filter 246-247 -- the new filter applies identically in spirit right after it; order validation 234-237; degenerate `<100`-valid-sample fallback 238-239; three-pass percentile-trimmed `lstsq` loop 257-267; full-resolution surface evaluation 268-274, which must use the SAME post-filter exponents list as the fit)
    - src/targets.py:7-41 (constants block; `DETREND_MAX_Y_DEGREE`'s Amendment A5 comment as the style to match), :51-69 (`extraction_params`, currently 16 keys), :299-315 (`extract_track_targets`'s detrend call)
    - tests/test_nsf_fmrg_data.py (`synthetic_grid`, `test_order_four_removes_quartic_bow`, `test_polynomial_basis_sizes_are_stable`, `test_detrend_edge_fix_preserves_default_behavior`, `_track10_shaped_edge_scenario`/`_fitted_surface_edge_departures`, `test_detrend_does_not_diverge_at_strip_edge` -- mirror this exact test-writing style and reuse these helpers where possible)
    - tests/test_targets.py (`test_edge_divergence_fix_is_track_independent` and its `_edge_divergence_scenario` helper -- the targets.py-level analog to mirror; `test_extraction_params_provenance`'s 16-key dict; `test_provenance_digest_is_change_sensitive`)
    - .planning/phases/01-target-extraction-contract/01-CONTEXT.md (Amendment A5's heading style, placement, and prose discipline -- Amendment A6 goes immediately after it, before "Claude's Discretion")
  </read_first>
  <behavior>
    - A synthetic scenario combining (a) an along-track magnitude comparable to track 10's real ~1.7mm raw drift (reuse/scale the existing `quartic_bow` construction pattern from `test_order_four_removes_quartic_bow`, sized roughly 40x larger than that test's amplitude) with (b) the existing y-edge shelf-feature construction from `_track10_shaped_edge_scenario`, plus an interior masked bead. Under today's uncapped order=4/max_y_degree=2 fit, `shape_gap`'s far-edge-vs-midpoint departure (defined identically to `01-13-CRITERION.md`) exceeds the pre-registered tolerance; under some `DETREND_MAX_XY_DEGREE` value, it does not.
    - The fix is off by default: `max_xy_degree` omitted or explicitly `None` reproduces today's output bit-for-bit (the `order=1` affine path, the notebooks, and every other caller unchanged).
    - `test_order_four_removes_quartic_bow`'s >=100x residual-reduction assertion still holds when `DETREND_MAX_XY_DEGREE` is applied on top (pure along-track terms are untouched).
    - `test_polynomial_basis_sizes_are_stable` is extended: at `order=4, max_y_degree=2`, `max_xy_degree` in `{0, 1, 2}` must each produce the correctly reduced basis size (7/9/11 terms respectively -- dropping only cross terms whose x-exponent exceeds the cap), and `max_xy_degree=3` (or higher) must be a no-op versus `max_xy_degree=None` (the highest surviving cross-term x-exponent after `max_y_degree=2` is already 3).
    - `test_detrend_does_not_diverge_at_strip_edge` (Amendment A5's y-edge regression) stays green with the new cap applied on top -- capping cross-track x-exponents must not reintroduce the already-fixed y-edge divergence.
    - `extraction_params()` returns 17 keys including the new constant, and mutating it changes the SHA-256 digest of `json.dumps(extraction_params(), sort_keys=True)`.
  </behavior>
  <action>
    Add a `max_xy_degree=None` keyword to `robust_plane_detrend`, placed after `max_y_degree`. When not `None`, after the existing `max_y_degree` filter, further filter `exponents` to drop any `(i, j)` where `j >= 1` and `i > max_xy_degree` -- terms with `j == 0` (pure along-track) are never touched regardless of `i`. Validate `max_xy_degree` as a non-negative integer or `None`, using the same style as the existing `order`/`max_y_degree` validation. Preserve every other existing behavior: the `<100`-valid-sample degenerate fallback, the three-pass percentile trimming, `stride_x=40`/`stride_y=2`, and the full-resolution surface evaluation using the identical (post-both-filters) exponents list used for the fit.

    Determine the `DETREND_MAX_XY_DEGREE` value empirically: implement the keyword first, then test candidate values (0, 1, 2) against `01-13-CRITERION.md`'s tolerance on the real 4-track data (reuse or extend `scripts/diagnose_track10_tail_collapse.py` locally to compare candidates, or exercise the synthetic scenario -- whichever demonstrates it fastest and most reliably), and select the LARGEST value that clears the criterion on all four tracks while keeping `test_order_four_removes_quartic_bow` and `test_detrend_does_not_diverge_at_strip_edge` green -- mirroring Amendment A5's own selection reasoning. This determination is made against the criterion (a property of the fitted surface), never against the resulting width ordering; do not run `run_target_extraction.py` or inspect any width/ordering result while choosing this value.

    Add `DETREND_MAX_XY_DEGREE` to `src/targets.py`'s constants block with a comment recording it was fixed from `01-13-CRITERION.md`'s tolerance test, not from the resulting ordering, matching Amendment A5's comment style. Pass `max_xy_degree=DETREND_MAX_XY_DEGREE` from `extract_track_targets` into the shared `robust_plane_detrend` call, identically for all four tracks. Add the constant to `extraction_params()` (17 keys). Update `test_extraction_params_provenance` to the new complete key set, extend `test_provenance_digest_is_change_sensitive` to also mutate the new constant (restoring it in a `finally` block), and extend `test_polynomial_basis_sizes_are_stable` per the `<behavior>` spec above.

    Add the RED-then-GREEN regressions from `<behavior>`: a new test `test_detrend_caps_xy_interaction_degree_at_domain_edge` in `tests/test_nsf_fmrg_data.py`, and a targets.py-level track-independence test `test_xy_interaction_cap_is_track_independent` in `tests/test_targets.py` mirroring `test_edge_divergence_fix_is_track_independent`, proving the same shared constant clears `01-13-CRITERION.md`'s tolerance at both a track-10-like magnitude and a healthy-track-like magnitude (~40x smaller). Append each to its module's `__main__` runner list.

    Introduce no per-track conditional anywhere; `test_single_parameterization_has_no_track_conditionals` must stay green.

    Then append Amendment A6 to the decisions section of `01-CONTEXT.md`, immediately after Amendment A5 and before "Claude's Discretion", matching A3/A4/A5's heading style and prose discipline. A6 must: date itself; name the diagnosed defect (track 10's x=70-100mm tail collapse and its measured cause, citing `.planning/debug/track10-14-ordering-tail-collapse.md`); name the mechanism adopted (`DETREND_MAX_XY_DEGREE`) and its value, with the a priori evidence that fixed it; state it was fixed from `01-13-CRITERION.md`'s pre-registered criterion, not from the resulting width ordering; state it applies identically to all four tracks with no per-track constant; state it supersedes nothing in A3, A4, or A5; and reaffirm the no-outcome-driven-tuning prohibition.

    NON-RECOVERABLE BRANCH: if no shared `DETREND_MAX_XY_DEGREE` value clears `01-13-CRITERION.md`'s criterion under its Section 4 constraints, apply NO fix -- introduce no constant, change no behavior, write no Amendment A6. Record this explicitly in the plan summary and proceed to Task 3, which reports the unresolved outcome and recommends accepting the 10-vs-14 FLAG as a documented known limitation per the phase's explicit mandate. Do not substitute a different mechanism to avoid an empty diff.

    Commit discipline (task-level TDD): first commit the failing tests (`test(01-13): add failing xy-interaction-degree regressions`), then the implementation (`fix(01-13): cap cross-track interaction degree in robust_plane_detrend`).
  </action>
  <verify>
    <automated>.venv/bin/python tests/test_nsf_fmrg_data.py && .venv/bin/python tests/test_targets.py && .venv/bin/python -O tests/test_nsf_fmrg_data.py && .venv/bin/python -O tests/test_targets.py && .venv/bin/python -c "import sys,json,hashlib; sys.path.insert(0,'src'); import targets; p=targets.extraction_params(); print('keys',len(p)); assert len(p)==17, p" && test -z "$(grep -n 'track_id ==' src/targets.py src/nsf_fmrg_data.py scripts/run_target_extraction.py)" && grep -q "Amendment A6" .planning/phases/01-target-extraction-contract/01-CONTEXT.md && echo "XY-INTERACTION-DEGREE CAP + A6 OK"</automated>
  </verify>
  <acceptance_criteria>
    - `DETREND_MAX_XY_DEGREE` exists in `src/targets.py` and matches `01-13-CRITERION.md`'s selected mechanism (or, in the non-recoverable branch, does not exist at all and the summary records that explicitly).
    - `robust_plane_detrend` gained exactly one new keyword, positioned after `max_y_degree`, whose default reproduces today's behavior bit-for-bit.
    - `extraction_params()` returns 17 keys including the new constant; `test_provenance_digest_is_change_sensitive` covers it.
    - `test_detrend_caps_xy_interaction_degree_at_domain_edge` and `test_xy_interaction_cap_is_track_independent` both pass and both fail if the new keyword is reverted to its default.
    - `test_order_four_removes_quartic_bow` and `test_detrend_does_not_diverge_at_strip_edge` both still pass -- the new cap regresses neither Amendment A3's bow removal nor Amendment A5's y-edge fix.
    - `grep -n "track_id ==" src/targets.py src/nsf_fmrg_data.py scripts/run_target_extraction.py` returns no matches, and `test_single_parameterization_has_no_track_conditionals` is green.
    - `01-CONTEXT.md` contains a dated Amendment A6 immediately after A5, naming the mechanism, its constant and value, the a-priori evidence, and the reaffirmed no-outcome-driven-tuning prohibition -- or, in the non-recoverable branch, no Amendment A6 exists and the summary records that conclusion explicitly.
    - All test suites report every test PASS under both `.venv/bin/python` and `.venv/bin/python -O`.
    - `git status --porcelain data/raw` is empty.
  </acceptance_criteria>
  <done>Exactly one uniform, track-independent correction selected by the pre-registered criterion is implemented, provenance-locked, and canonicalized as Amendment A6 -- or the non-recoverable conclusion is recorded with no source change at all.</done>
</task>

<task type="auto">
  <name>Task 3: Regenerate all 4 tracks and report the 10-vs-14 ordering outcome honestly, with the option-(a) fallback discussion</name>
  <files>.planning/phases/01-target-extraction-contract/01-13-ORDERING-OUTCOME.md</files>
  <read_first>
    - scripts/run_target_extraction.py (`run_pipeline`, `publish_staging_dir` -- one atomic invocation regenerates all four tracks, 12 QA PNGs, `extraction_params.json`, and `manifest.json`)
    - scripts/check_targets.py (the `MIN_VALID_FRACTION` gate)
    - .planning/phases/01-target-extraction-contract/01-11-ORDERING-OUTCOME.md (the exact report structure and HONEST-OUTCOME discipline to mirror: verbatim output, verdict, material context, "what this task did NOT do")
    - .planning/phases/01-target-extraction-contract/01-13-CRITERION.md and .planning/debug/track10-14-ordering-tail-collapse.md (what was predicted and why)
    - .planning/phases/01-target-extraction-contract/01-UAT.md (G-01-4's exact user language: "authorized exactly one further bounded... no further open-ended tuning cycle is authorized")
  </read_first>
  <action>
    Run `.venv/bin/python scripts/run_target_extraction.py --project_dir .` exactly once (a single atomic staged publish -- all four tracks, 12 QA PNGs, `extraction_params.json`, and `manifest.json` from one run id). Expect several minutes of runtime. Then run `.venv/bin/python scripts/check_targets.py --project_dir .` and capture both its full output and exit code verbatim. Do not suppress a non-zero exit and do not retry with modified inputs.

    Write `01-13-ORDERING-OUTCOME.md` mirroring `01-11-ORDERING-OUTCOME.md`'s structure: the extraction contract in effect (Amendment A6 and its value, or "no fix applied -- non-recoverable branch" if Task 2 took that branch); the verbatim regenerated summary table and the three adjacent-pair ordering verdicts; the verbatim `check_targets.py` output and exit code; every track's valid-bin count and fraction against the 50% floor (confirm track 10 does not regress below Amendment A5's established 60.5%); a plain verdict on whether `8>10>14>21` holds.

    HONEST-OUTCOME GUARD -- non-negotiable: whatever the 10-vs-14 verdict is, do NOT change `DETREND_MAX_XY_DEGREE`, `DETREND_MAX_Y_DEGREE`, `DETREND_POLY_ORDER`, `BEAD_MASK_HEIGHT_FRACTION`, `MAX_TRACKING_GAP_COLUMNS`, `MIN_VALID_FRACTION`, or any other locked constant in response. Do NOT relax D-01/D-03's boundary-clipped-run exclusion. Do NOT add a per-track branch. Do NOT re-run the extractor with a different value to compare outcomes -- that is exactly the outcome-shopping the criterion prohibits. A second run is permissible only to recover from an infrastructure failure, disclosed as such.

    Then write an explicit final section, "Decision point per the phase's authorized bounded cycle": if the ordering now holds (`8>10>14>21`), state so plainly as the honest, unforced outcome of a criterion-driven fix. If it still FLAGs, state plainly that this was the phase's LAST authorized bounded, diagnosis-and-criterion-driven cycle for this defect class (citing `01-UAT.md` Test 5 / G-01-4's exact language), that no further open-ended tuning cycle is authorized, and RECOMMEND accepting the 10-vs-14 FLAG as a documented, investigated, known limitation (option a) for the next human verification round to ratify -- do not silently re-attempt or propose another cycle.

    Confirm `data/raw/ integrity PASS` was printed, 12 PNGs exist under `processed_data/targets/qa/`, `processed_data/diagnostics/track10_tail_collapse_diagnosis.csv` still exists after the publish (Task 1's sibling-directory placement survives `publish_staging_dir`'s rename-and-rmtree), `git diff --stat -- src/targets.py src/nsf_fmrg_data.py` is empty for this task's own commit, and `git status --porcelain data/raw` is empty.
  </action>
  <verify>
    <automated>.venv/bin/python scripts/run_target_extraction.py --project_dir . && test $(ls processed_data/targets/qa/*.png | wc -l) -eq 12 && test -f processed_data/targets/manifest.json && test -f processed_data/targets/extraction_params.json && for t in 8 10 14 21; do test -f processed_data/targets/track_${t}_targets.npz || exit 1; done && test -f processed_data/diagnostics/track10_tail_collapse_diagnosis.csv && .venv/bin/python scripts/check_targets.py --project_dir . > /tmp/check_targets_01_13.txt 2>&1; echo "check_targets exit=$?"; cat /tmp/check_targets_01_13.txt; test -f .planning/phases/01-target-extraction-contract/01-13-ORDERING-OUTCOME.md && test -z "$(git diff --stat -- src/targets.py src/nsf_fmrg_data.py)" && test -z "$(git status --porcelain data/raw)" && echo "REGENERATED AND REPORTED"</automated>
  </verify>
  <acceptance_criteria>
    - A single `run_target_extraction.py` invocation completed, printed `data/raw/ integrity PASS`, and published four `track_{8,10,14,21}_targets.npz` files, 12 QA PNGs, `extraction_params.json`, and `manifest.json` sharing one run id.
    - `processed_data/diagnostics/track10_tail_collapse_diagnosis.csv` still exists after the publish.
    - `check_targets.py`'s full output and exit code are recorded verbatim in `01-13-ORDERING-OUTCOME.md`, whatever they are.
    - `01-13-ORDERING-OUTCOME.md` states plainly whether `8>10>14>21` holds, lists all four valid fractions against the 50% floor, names the contract in effect, and contains the "Decision point" section with the option-(a) recommendation if the FLAG persists.
    - `git diff --stat -- src/targets.py src/nsf_fmrg_data.py` is empty at this task's commit.
    - `grep -n "track_id ==" src/targets.py src/nsf_fmrg_data.py scripts/run_target_extraction.py` returns no matches.
    - `git status --porcelain data/raw` is empty.
  </acceptance_criteria>
  <done>All four tracks are regenerated from one atomic run under the current contract, and the 10-vs-14 ordering outcome is on record verbatim -- tuned in neither direction, with an explicit, honest fallback recommendation if the FLAG persists.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| filesystem read of `data/raw/` -> in-memory arrays | The diagnostic and extractor read irreplaceable, competition-restricted raw height maps; nothing may write, delete, or modify under `data/raw/`. |
| extraction constants -> persisted ground truth | A constant changed outside `extraction_params()` produces artifacts whose provenance digest no longer describes how they were made. |
| observed 10-vs-14 outcome -> constant selection | The width ordering is the exact signal that must NOT feed back into `DETREND_MAX_XY_DEGREE`'s selection; task ordering (criterion committed before any src change) is the control. |
| staging directory -> published `processed_data/targets/` | The atomic publish deletes and renames directories; plan 01-10's symlink guards must still hold during this plan's regeneration run. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-34 | Tampering | no-tuning integrity (constant selection) | high | mitigate | Task 1's criterion is committed to git while `git diff HEAD --stat -- src/targets.py src/nsf_fmrg_data.py` is empty; the diagnostic script is forbidden (by grep gate) from computing any width median or ordering verdict; Task 3's commit must leave the extraction diff empty. |
| T-01-35 | Repudiation | provenance completeness | high | mitigate | `DETREND_MAX_XY_DEGREE` is added to `extraction_params()` (17 keys), covered by `test_provenance_digest_is_change_sensitive`, persisted to `extraction_params.json`, and digested into `manifest.json`; Amendment A6 records it in `01-CONTEXT.md`. |
| T-01-36 | Tampering (accidental) | `data/raw/` | high | mitigate | Both the diagnostic and the extractor bracket their runs with `snapshot_raw`/`raw_snapshot_diff`; every task asserts `git status --porcelain data/raw` is empty. |
| T-01-37 | Tampering | atomic publish during regeneration | high | mitigate | Regeneration runs through `publish_staging_dir` with plan 01-10's symlink guards in place; `depends_on: ["01-12"]` (which itself depends on 01-11, which depends on 01-10) keeps this plan strictly after those guards were installed. |
| T-01-38 | Denial of service | single long extraction run | low | accept | The full four-track run takes several minutes and exceeds the 60-second feedback target; this is the already-accepted one-time full-pipeline cost recorded in `01-VALIDATION.md`. |
| T-01-SC | Tampering | npm/pip/cargo installs | high | mitigate | No packages are installed by this plan; `requirements.txt` is unchanged. No `[ASSUMED]`/`[SUS]` package is introduced, so no legitimacy checkpoint is required. |
</threat_model>

<verification>
- `.venv/bin/python -m py_compile src/*.py scripts/*.py tests/*.py` exits 0.
- All test suites report every test PASS under both `.venv/bin/python` and `.venv/bin/python -O`, including the two new regressions (or, in the non-recoverable branch, all existing tests still pass unmodified).
- `extraction_params()` returns 17 keys; mutating `DETREND_MAX_XY_DEGREE` changes the SHA-256 digest; `processed_data/targets/extraction_params.json` matches the code constants and `manifest.json`'s digest matches it.
- `grep -n "track_id ==" src/targets.py src/nsf_fmrg_data.py scripts/run_target_extraction.py` returns no matches.
- `01-CONTEXT.md` contains a dated Amendment A6 (or the summary records the non-recoverable branch and no A6 exists).
- `01-13-CRITERION.md` and `01-13-ORDERING-OUTCOME.md` both exist; the criterion's commit precedes any extraction-source change.
- Four NPZs, 12 QA PNGs, `extraction_params.json`, and `manifest.json` exist under `processed_data/targets/` from a single run id.
- `processed_data/diagnostics/track10_tail_collapse_diagnosis.csv` exists both before and after Task 3's regeneration.
- `git status --porcelain data/raw` is empty.
</verification>

<success_criteria>
- The x-direction shape-gap evidence is measured by committed, re-runnable code, and the fix-selection criterion is in git history before any extraction-source change exists.
- At most one uniform, track-independent change (`DETREND_MAX_XY_DEGREE`) is applied, provenance-locked in `extraction_params()`, and canonicalized as Amendment A6 -- or the non-recoverable conclusion is recorded with no source change.
- All four tracks are regenerated from one atomic run and the 10-vs-14 ordering outcome is reported verbatim, with no constant changed in response, and an explicit option-(a) fallback recommendation if the FLAG persists.
- No gate weakened, no per-track branch introduced, `data/raw/` untouched.
</success_criteria>

<output>
Create `.planning/phases/01-target-extraction-contract/01-13-SUMMARY.md` when done.
</output>