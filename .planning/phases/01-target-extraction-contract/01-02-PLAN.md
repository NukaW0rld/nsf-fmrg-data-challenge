---
phase: 01-target-extraction-contract
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - scripts/run_target_extraction.py
  - scripts/check_targets.py
autonomous: true
requirements: [TARGET-02]
tags: [profilometry, width-extraction, qa-plots, matplotlib]
must_haves:
  truths:
    - "Running scripts/run_target_extraction.py produces one .npz per track (8, 10, 14, 21) under processed_data/targets/ plus a single extraction_params.json"
    - "All persisted float arrays (x_grid_mm, w_mm, y_upper_mm, y_lower_mm) are float64 and every track shares the identical 400-slot 0.2mm grid spanning 20.1-99.9mm"
    - "Invalid x positions keep their grid slot with width=NaN and mask=False — no ragged or dropped positions at valid/invalid boundaries"
    - "np.isfinite(w_mm) equals valid_mask elementwise, and y_upper_mm > y_lower_mm at every valid slot in every track artifact"
    - "QA figures exist for all 4 tracks: post-detrend residual map (D-14), raw+detrended overlay with extracted boundaries, and width curve with the 20mm/100mm crop-edge regions visibly marked (D-12)"
    - "Per-track width summaries and pairwise ordering results (8 vs 10, 10 vs 14, 14 vs 21) are printed as PASS/FLAG and recorded in the SUMMARY without any constant adjustment"
    - "The raw-data snapshot check confirms no file under data/raw/ was created, modified, or deleted by the pipeline run"
    - "A track yielding zero valid slots aborts the run with a hard error rather than persisting an all-NaN artifact"
    - "scripts/check_targets.py exits 0 against the four persisted artifacts"
  artifacts:
    - path: "scripts/run_target_extraction.py"
      provides: "CLI batch runner: 4-track extraction, .npz + params persistence, QA figures, raw-data prohibition check, summary/ordering table"
      contains: "snapshot_raw"
    - path: "scripts/check_targets.py"
      provides: "Assertion script over persisted artifacts (keys, shapes, dtypes, grid identity, mask consistency, params provenance, ordering report)"
      contains: "def check_track"
    - path: "processed_data/targets/track_8_targets.npz"
      provides: "Persisted target artifact for track 8 (repeated for tracks 10, 14, 21)"
    - path: "processed_data/targets/extraction_params.json"
      provides: "Single shared parameterization provenance, written once per run"
    - path: "processed_data/targets/qa/track_21_overlay.png"
      provides: "Overlay QA figure incl. track-21 gap-heavy regions (12 QA pngs total: residual_map/overlay/width x 4 tracks)"
  key_links:
    - from: "scripts/run_target_extraction.py"
      to: "src/targets.py"
      via: "sys.path bootstrap then import of extract_track_targets and extraction_params"
      pattern: "from targets import"
    - from: "scripts/check_targets.py"
      to: "processed_data/targets/track_8_targets.npz"
      via: "np.load of each per-track artifact"
      pattern: "np\\.load"
    - from: "processed_data/targets/track_8_targets.npz"
      to: "src/nsf_fmrg_data.py"
      via: "x_grid_mm matches the thermal frame-center formula (Phase 2 alignment contract, D-07)"
      pattern: "x_grid_mm"
  prohibitions:
    - statement: "Extraction contract constants (noise-floor minimum, gap limit, percentile split, half-max fraction, SG window/order) must NOT be adjusted based on whether the resulting width ordering or QA plots look correct — if the 8>10>14>21 ordering FLAGs, the outcome is documented and escalated, never tuned away"
      status: resolved
      verification: judgment
    - statement: "Extraction and persistence code must NOT write to, delete, or modify any file under data/raw/"
      status: resolved
      verification: test
---

<objective>
Run the locked contract against the real data: a CLI batch script extracts `w_i(x)` for all 4 tracks via plan 01's `src/targets.py`, persists per-track `.npz` artifacts plus a shared-parameterization provenance file, renders the mandatory QA figures (residual maps per D-14, raw+detrended overlays, width curves), mechanically proves `data/raw/` untouched, and reports the width-ordering investigation honestly (PASS/FLAG per pair, no tuning). A second assertion script verifies every persisted artifact property.

Purpose: TARGET-02 acceptance — artifacts on the shared 0.2mm grid that Phase 2 consumes as ground truth, visually QA'd on all 4 tracks before anything downstream trusts them.

Output: `scripts/run_target_extraction.py`, `scripts/check_targets.py`, 4 `.npz` artifacts + `extraction_params.json` + 12 QA pngs under `processed_data/targets/`.

**Precondition (wave gate):** amendments A1 (bin-first D-05/D-06 interpretation) and A2 (y-boundary clip rule) were ratified — or amended and re-tested to green — at plan 01-01's Task 3 blocking checkpoint. The contract wording is settled BEFORE this plan spends compute; do not start the extraction run if that checkpoint is unresolved.

**Ordering investigation (RESEARCH Open Question 2, Finding 3):** the roadmap's expected ordering 400W(8) > 350W(10) > 300W(14) > 200W(21) is empirically AT RISK — the research probe (without the A2 clip rule) measured track 10 WIDER than track 8 (median 0.838 vs 0.790mm), with 117/288 track-10 bins y-boundary-clipped. The a-priori clip rule may or may not restore the ordering. The executor MUST treat the ordering check as an investigation with a documented outcome: report PASS/FLAG per pair, never modify any constant in response (prohibition P1), and record the numbers + interpretation in the SUMMARY for the end-of-phase human decision on the roadmap gate. RESEARCH Pitfall 2: track 10 (not 21) is the first-class extraction-quality suspect — give it equal QA scrutiny.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01-target-extraction-contract/01-RESEARCH.md
@.planning/phases/01-target-extraction-contract/01-PATTERNS.md
@.planning/phases/01-target-extraction-contract/01-01-SUMMARY.md
@scripts/run_thermal_video_export.py
@src/targets.py

<interfaces>
<!-- Contract created by plan 01-01 in src/targets.py. Use directly — no exploration needed. -->

From src/targets.py (no type hints, per codebase convention):

- `extract_track_targets(height_dir, track_id)` -> dict with keys:
  `'track_id'`, `'file'`, `'x_grid_mm'` (400,), `'w_mm'` (400,), `'y_upper_mm'` (400,),
  `'y_lower_mm'` (400,), `'valid_mask'` (400, bool), `'Z_mm'` (raw 480 x ~20k map),
  `'Zd_mm'` (detrended map), `'x_actual_mm'` (native x coords), `'y_mm'` (480,).
  Raises ValueError on: wrong resolved filename, missing pixel_size_mm header key,
  degenerate detrend, or zero valid slots.
- `extraction_params()` -> dict of the 12 locked contract constants.
- `target_grid()` -> the fixed 400-slot grid, 20.1 + 0.2*k.
- Module constants importable for reporting: TARGET_GRID_N, TARGET_GRID_STEP_MM, etc.
</interfaces>
</context>

## Artifacts this phase produces

See the identically-titled section in `01-01-PLAN.md` for the full phase symbol list. This plan creates: `scripts/run_target_extraction.py` (`snapshot_raw()`, `run_track()`, `main()`, `--project_dir` flag, `TRACK_POWER_W` script constant), `scripts/check_targets.py` (`check_track()`, `main()`, `--project_dir` flag), and runtime data artifacts `processed_data/targets/track_{8,10,14,21}_targets.npz`, `processed_data/targets/extraction_params.json`, `processed_data/targets/qa/track_{id}_{residual_map,overlay,width}.png`.

<tasks>

<task type="auto">
  <name>Task 1: Build the 4-track extraction runner with persistence, QA figures, and raw-data prohibition check — then run it</name>
  <files>scripts/run_target_extraction.py</files>
  <read_first>
    - scripts/run_thermal_video_export.py (entire file — copy its sys.path bootstrap lines 7-11 verbatim including the comment, argparse `--project_dir` pattern lines 73-85, output-dir mkdir pattern lines 25-28, figure lifecycle with plt.close lines 39-51/68, print-only feedback, double-blank-line style)
    - src/targets.py (plan 01 output — the interface consumed; confirm return-dict keys before use)
    - .planning/phases/01-target-extraction-contract/01-RESEARCH.md §Finding 5 (per-track x-coverage starts: 10=20.00, 8=20.26, 21=20.73, 14=21.46mm — leading invalid slots are EXPECTED, annotate not "fix"), §Code Examples raw-data prohibition pattern, §Architecture Patterns (persistence keys/layout)
  </read_first>
  <action>
    Create `scripts/run_target_extraction.py` following the analog script's structure. Set the matplotlib Agg backend before importing pyplot (headless run). Script-level constant `TRACK_POWER_W = {8: 400, 10: 350, 14: 300, 21: 200}` (reporting metadata only — all extraction logic stays parameter-free per track inside src/targets.py).

    `snapshot_raw(project_dir)`: dict mapping every file path under `data/raw/` (via `Path.rglob`) to `(st_mtime_ns, st_size)`.

    `run_track(project_dir: Path, track_id: int) -> dict` (typed signature — script entry-point convention):
    1. Call `extract_track_targets(project_dir / 'data' / 'raw' / 'height_maps', track_id)`.
    2. Persist `processed_data/targets/track_{track_id}_targets.npz` via `np.savez` with EXACTLY the keys `x_grid_mm`, `w_mm`, `y_upper_mm`, `y_lower_mm` (each cast `.astype(np.float64)` — the loader allocates float32 upstream, SPEC mandates float64 persistence) and `valid_mask` (bool). Create output dirs with `mkdir(parents=True, exist_ok=True)`.
    3. Render 3 QA figures to `processed_data/targets/qa/`:
       - `track_{id}_residual_map.png` (D-14): imshow of the detrended `Zd_mm` with extent from `x_actual_mm`/`y_mm`, aspect auto, symmetric color limits at ±the 99th percentile of |Zd| finite values, colorbar labeled in mm, title naming the track and "post-detrend residual (D-14 bow/curvature check)".
       - `track_{id}_overlay.png`: two stacked panels — raw `Z_mm` (top) and detrended `Zd_mm` (bottom), each with the smoothed `y_upper_mm`/`y_lower_mm` curves plotted against `x_grid_mm`; invalid slots marked visibly (e.g. light vertical shading spans along the x-axis where mask is False) so track 21's gap-heavy regions and every track's leading no-coverage region read as EXPLICITLY masked, not silently dropped; annotate the native data coverage start (`x_actual_mm[0]`) per track (RESEARCH Finding 5 — track 14 has no data below 21.46mm).
       - `track_{id}_width.png`: `w_mm` vs `x_grid_mm` (NaN slots break the line naturally), annotated with median/mean width over valid slots and valid-count/400, with the first and last 0.5mm of the grid shaded to make D-12 crop-edge smoothing behavior visually inspectable.
       Close every figure after saving.
    4. Return per-track summary dict: track_id, laser power, valid count, median width, mean width.

    `main()`: argparse with `--project_dir` (type Path, default `Path('.')`). Sequence: take the raw-data snapshot; write `processed_data/targets/extraction_params.json` ONCE via `json.dump` of `extraction_params()` (single shared parameterization provenance — TARGET-01); loop tracks 8, 10, 14, 21 calling `run_track` (keep per-track results in memory; the returned dict already carries the loaded/detrended arrays so nothing is loaded twice); retake the snapshot and raise `ValueError` listing offending paths if any file under `data/raw/` was added, removed, or changed, else print a `data/raw/` integrity PASS line (prohibition P2, SPEC acceptance); print a summary table (track, power W, valid bins, median mm, mean mm) and three pairwise ordering lines — 8 vs 10, 10 vs 14, 14 vs 21 by median width — each ending PASS or FLAG; print an explicit closing line that FLAG outcomes are documented, never tuned (prohibition P1). All feedback via print(), no logging module.

    Then execute the full run: `.venv/bin/python scripts/run_target_extraction.py --project_dir .` (expect ~5-10 minutes — the pure-Python ASC loader takes 1-2 min/track; use an extended Bash timeout of 600000ms). If any track raises the zero-valid-slot hard error, STOP and surface it — do not weaken any rule to proceed.
  </action>
  <verify>
    <automated>.venv/bin/python -c "from pathlib import Path; import numpy as np; ps=[Path(f'processed_data/targets/track_{t}_targets.npz') for t in (8,10,14,21)]; assert all(p.exists() for p in ps), 'missing npz'; assert Path('processed_data/targets/extraction_params.json').exists(); qa=list(Path('processed_data/targets/qa').glob('track_*.png')); assert len(qa)==12, f'expected 12 QA pngs, got {len(qa)}'; print('artifacts OK')"</automated>
    <human-check>End-of-phase visual sign-off: (1) residual maps for all 4 tracks show no obvious remaining bow/curvature (D-14 — if curvature IS visible, escalate for a detrend fix decision, per CONTEXT); (2) overlay plots show boundaries tracking the bead sanely on raw AND detrended maps with no sawtooth/high-frequency jitter and no silently dropped gaps, including track 21's gap regions and track 10's boundary-clip-heavy regions; (3) crop-edge smoothing behavior in the shaded 20/100mm regions looks sane (D-12).</human-check>
  </verify>
  <acceptance_criteria>
    - Full run completes: 4 `.npz` files + `extraction_params.json` + exactly 12 QA pngs exist under `processed_data/targets/`
    - Run output contains a `data/raw/` integrity PASS line, a 4-row summary table, and 3 pairwise ordering lines each ending PASS or FLAG
    - `scripts/run_target_extraction.py` contains `snapshot_raw` and writes no path outside `processed_data/targets/`
    - `git status --porcelain data/raw/` shows no changes after the run
    - No extraction constant in src/targets.py was modified by this task: `git diff --stat -- src/targets.py` is empty
  </acceptance_criteria>
  <done>Pipeline runs end-to-end on all 4 real tracks producing persisted artifacts and QA figures; raw-data prohibition check PASSes; ordering results printed as PASS/FLAG without touching any constant.</done>
</task>

<task type="auto">
  <name>Task 2: Artifact assertion script + ordering investigation record</name>
  <files>scripts/check_targets.py</files>
  <read_first>
    - scripts/run_thermal_video_export.py (sys.path bootstrap + argparse + print conventions to copy)
    - src/targets.py (import `extraction_params` and `target_grid` for provenance comparison)
    - processed_data/targets/ (task 1 outputs — the artifacts under test)
    - .planning/phases/01-target-extraction-contract/01-RESEARCH.md §Finding 3 (probe width numbers to compare the real run against when writing the SUMMARY investigation record)
  </read_first>
  <action>
    Create `scripts/check_targets.py` (same sys.path bootstrap + `--project_dir` argparse + print-only conventions). `check_track(targets_dir: Path, track_id: int) -> dict` loads `track_{track_id}_targets.npz` and asserts, fail-fast with explicit messages:
    - key set is exactly `{'x_grid_mm', 'w_mm', 'y_upper_mm', 'y_lower_mm', 'valid_mask'}`
    - all four float arrays have dtype float64 and shape (400,); `valid_mask` has bool dtype and shape (400,) (SPEC edge R2-precision, D-07)
    - grid: first slot 20.1, uniform step 0.2 (allclose), and identical (allclose) to `targets.target_grid()` — hence identical across all tracks (D-07)
    - `np.isfinite(w_mm)` equals `valid_mask` elementwise, and likewise for `y_upper_mm`/`y_lower_mm` (SPEC edge R2-adjacency: invalid slots hold NaN in place — no ragged arrays)
    - `w_mm` allclose to `y_upper_mm - y_lower_mm` at valid slots
    - `y_upper_mm > y_lower_mm` at every valid slot (no crossed/degenerate boundary survived — SPEC edge R1-precision enforced end-to-end)
    - all valid widths are positive and below the 1.907mm y-strip extent (physical sanity bound)
    - `valid_mask.sum() > 0` (all-invalid artifact can never exist — SPEC edge R2-empty backstops the upstream hard error)
    - print a non-fatal FLAG line when the valid fraction is below 50% (expected possible on track 10 after the clip rule — RESEARCH Pitfall 2; report, don't fail)
    `main()` additionally asserts `extraction_params.json` content equals `targets.extraction_params()` (provenance matches code — TARGET-01 single parameterization), prints the per-track summary + the three pairwise ordering PASS/FLAG lines recomputed from artifacts, then prints ALL CHECKS PASSED. Run it: `.venv/bin/python scripts/check_targets.py --project_dir .` (must exit 0, <10s).

    Then write the ordering-investigation record into this plan's SUMMARY: the actual per-track median/mean widths and valid counts, each pairwise result, comparison against the research probe's pre-clip-rule numbers (8: 0.790, 10: 0.838, 14: 0.593, 21: 0.585 median mm), and — if any pair FLAGs — an explicit statement that constants were NOT modified (prohibition P1) and that the roadmap's ordering gate needs a human decision at phase verification (amend the gate vs. investigate detrending, per RESEARCH Open Question 2). Also record amendments A1 (bin-first gap rule) and A2 (y-boundary clip rule) as contract interpretations ratified at plan 01-01's Task 3 blocking checkpoint, including any wording amendment the user made there.
  </action>
  <verify>
    <automated>.venv/bin/python scripts/check_targets.py --project_dir .</automated>
    <human-check>End-of-phase decisions: (1) re-affirm the A1/A2 ratification given the real QA figures — the binding ratification already happened at plan 01-01's Task 3 pre-compute checkpoint; this item only checks that nothing in the figures contradicts it; (2) review the ordering investigation — if 8>10 (or any pair) FLAGged, decide whether the roadmap success-criterion wording is amended or further detrend investigation is scheduled; constants stay locked either way; (3) confirm constants were set before ordering inspection (prohibition P1 judgment review).</human-check>
  </verify>
  <acceptance_criteria>
    - `.venv/bin/python scripts/check_targets.py --project_dir .` exits 0 and prints ALL CHECKS PASSED
    - `scripts/check_targets.py` contains `def check_track(` and asserts dtype, shape, grid, mask-consistency, width-identity, and crossed-boundary properties listed above
    - The plan SUMMARY contains the per-track width table, the three pairwise ordering outcomes, and the A1/A2 ratification record (pointing at plan 01-01's Task 3 checkpoint outcome)
    - `git diff --stat -- src/targets.py` remains empty (no constant touched after seeing ordering output)
  </acceptance_criteria>
  <done>Every persisted artifact property is mechanically asserted green; the ordering investigation outcome is recorded honestly, with the pre-run A1/A2 ratification outcome carried into the SUMMARY.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pipeline → `data/raw/` | batch script iterates over irreplaceable, unversioned raw files while writing outputs elsewhere |
| filename pattern → track identity | wrong-file resolution would silently produce mislabeled ground truth for Phase 2+ |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-05 | Tampering (accidental) | `run_target_extraction.py` file I/O | high | mitigate | All writes confined to `processed_data/targets/`; before/after `snapshot_raw` mtime/size comparison raises ValueError on any `data/raw/` delta (SPEC-mandated prohibition P2) |
| T-01-06 | Spoofing (identity confusion) | per-track file resolution | medium | mitigate | Inherited from plan 01: `extract_track_targets` hard-fails on unexpected basename; runner prints each resolved path for the run log |
| T-01-07 | Information disclosure | git vs. competition-restricted data | medium | mitigate | Commits stage only the two scripts; nothing under `data/raw/` is ever staged; derived `processed_data/` artifacts are runtime outputs and are not required in commits |
| T-01-08 | Repudiation (untraceable ground truth) | persisted artifacts vs. parameters | low | mitigate | `extraction_params.json` provenance written every run and asserted equal to the code constants by `check_targets.py` |
| T-01-SC | Tampering | pip installs | high | mitigate | No installs occur in this plan (environment fixed in plan 01 Wave 0 from audited requirements.txt) |
</threat_model>

<verification>
- Full pipeline: `.venv/bin/python scripts/run_target_extraction.py --project_dir .` completes with integrity PASS
- Artifact checks: `.venv/bin/python scripts/check_targets.py --project_dir .` exits 0
- Contract invariants still green after all work: `.venv/bin/python tests/test_targets.py` exits 0
- End-of-phase human sign-off items collected from both tasks' human-check blocks (residual maps D-14, overlay sanity, A1/A2 ratification re-affirmation, ordering-gate decision, P1 judgment review)
</verification>

<success_criteria>
- TARGET-02 satisfied: extractor runs the locked contract identically on all 4 tracks, artifacts persisted on the shared grid Phase 2 will consume, QA figures produced including track 21's gaps and track 10's suspect regions
- Ordering investigation documented with real numbers; no constant modified after outcome inspection
- data/raw/ provably untouched
</success_criteria>

<output>
Create `.planning/phases/01-target-extraction-contract/01-02-SUMMARY.md` when done. Must include: per-track width/valid-count table, pairwise ordering PASS/FLAG outcomes with interpretation, the A1/A2 ratification record from plan 01-01's Task 3 checkpoint, raw-data integrity result, and pointers to all 12 QA figures for the end-of-phase visual sign-off.
</output>
